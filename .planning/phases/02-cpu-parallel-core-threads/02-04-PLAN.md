---
phase: 02-cpu-parallel-core-threads
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java
  - .planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md
autonomous: false
requirements: [CPU-01, CPU-02]
user_setup: []
must_haves:
  truths:
    - "ParallelRegressionTest.largeArray_isAtLeast2xFaster passes: parallel sum path completes in <= 50% of forced-sequential path time on n = 10^7"
    - "ParallelRegressionTest.smallArray_under10PercentRegression passes: parallel elementwise path at n = 10_000 and n = 100_000 takes <= 110% of forced-sequential path time"
    - "Full mvn test exits 0 (all 8+5 existing tests still green per STATE.md)"
    - "02-BASELINE-AFTER.md committed with fresh JMH numbers showing >=2x on 10^6/10^7 and <10% regression on 10^4/10^5"
  artifacts:
    - path: "modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java"
      provides: "Timing-based regression gate for the >=2x large-array criterion and the <10% small-array criterion"
      contains: "largeArray_isAtLeast2xFaster"
    - path: ".planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md"
      provides: "Fresh JMH numbers proving Phase 2 success criteria"
      contains: "## Kết quả"
  key_links:
    - from: "modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java"
      to: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      via: "ParallelOps.setThresholdForTesting to force sequential branch for baseline measurement"
      pattern: "ParallelOps\\.setThresholdForTesting"
    - from: ".planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md"
      to: ".planning/phases/01-baseline-benchmark/BASELINE.md"
      via: "JMH benchmark numbers from CoreBench (after Phase 2 changes)"
      pattern: "Phase 2|ParallelOps|ForkJoin"
---

<objective>
Close out Phase 2 with three deliverables: (1) ParallelRegressionTest in the numja module that asserts the success criteria as JUnit timing tests (parallel/sequential ratio gates), (2) a full mvn test run proving no regression to existing 8+5 tests, (3) a fresh JMH benchmark sweep and the 02-BASELINE-AFTER.md document comparing Phase 2 numbers against the Phase 1 BASELINE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md
@.planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md
@.planning/phases/02-cpu-parallel-core-threads/02-VALIDATION.md
@.planning/STATE.md
@.planning/phases/01-baseline-benchmark/BASELINE.md
@modules/numja/src/main/java/numja/core/ParallelOps.java
@bench/src/main/java/bench/CoreBench.java
</context>

<interfaces>
From modules/numja/src/main/java/numja/core/ParallelOps.java (Plans 01/02/03):
- public static final int THRESHOLD = 100_000; gate value.
- static void setThresholdForTesting(int n) and static void resetThresholdForTesting() — used by ParallelRegressionTest to force sequential path for the baseline measurement.

From bench/src/main/java/bench/CoreBench.java (after Plan 02):
- ElemState @Param {1000000, 10000000} seeds 44L/45L (large arrays for >=2x criterion).
- SmallArrayState @Param {10000, 100000} seeds 47L/48L (small arrays for <10% regression criterion).
- sum_reduce and mean_reduce benchmarks on n = 10^7 (reduce speedup target).

From .planning/phases/01-baseline-benchmark/BASELINE.md:
- add_elementwise 10^7 = 89.38ms; multiply_elementwise 10^7 = 98.68ms; sum_reduce 10^7 = 27.21ms; mean_reduce 10^7 = 42.73ms. Phase 2 target: >=2x faster (i.e., <= 44.7ms add, <= 49.3ms multiply, <= 13.6ms sum).
- Machine metadata table (lines 12-21) — same machine for after-baseline.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 02-04-01: ParallelRegressionTest (timing gates for >=2x large and <10% small</name>
  <files>modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java</files>
  <read_first>
    - modules/numja/src/main/java/numja/core/ParallelOps.java (THRESHOLD and the setThresholdForTesting hook from Plan 02)
    - modules/numja/src/main/java/numja/core/NDArray.java (line 151-160 add; line 329-335 sum — what the test will measure)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pitfall 3 (hybrid P/E-core variance — take MEDIAN of N trials, not a single run)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md ParallelRegressionTest (NEW) skeleton
    - bench/src/main/java/bench/CoreBench.java line 32-37 randomVec helper
    - .planning/phases/01-baseline-benchmark/BASELINE.md (the speedup targets: 89.38ms -> 44.7ms for add, 27.21ms -> 13.6ms for sum)
</read_first>
  <action>
    Create modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java:
    1. Package numja.core. Path modules/numja/src/test/java/com/numja/core/.
    2. Imports: numja.NumJa; numja.core.NDArray; numja.core.ParallelOps; org.junit.After; org.junit.Before; org.junit.Test; org.junit.Assert.*; java.util.Arrays; java.util.Random.
    3. Helper private static NDArray randomVec(int n, long seed) copying CoreBench.java:32-37. Helper private static long medianNanos(long[] xs) that sorts and returns xs[xs.length / 2] (handles odd length; for even length average the middle two). The helper is critical for stability — RESEARCH Pitfall 3 (hybrid CPU variance, single-run timing is unreliable).
    4. Helper private static long timeOp(Runnable op, int warmupIters, int measureIters) that runs op warmupIters times (no measurement), then measureIters times measuring System.nanoTime() before and after each call. Returns the median nanoTime in nanoseconds. measureIters = 9 (odd, gives a clean median); warmupIters = 3.
    5. Test 1: largeArray_isAtLeast2xFaster — for n = 10_000_000 (above THRESHOLD): warm up JIT with 5 unmeasured NumJa.add calls. Measure sequential baseline by setting ParallelOps.setThresholdForTesting(Integer.MAX_VALUE), calling timeOp with the add, resetting via ParallelOps.resetThresholdForTesting(), then measuring parallel path with default threshold. Assert parallelNanos <= sequentialNanos / 2.0. Seeds 47L/48L (must match Plan 02 SmallArrayState — same input data → timing comparison stays apples-to-apples with the bench baseline).
    6. Test 2: smallArray_under10PercentRegression_at10k — for n = 10_000 (BELOW THRESHOLD): same timing harness. With threshold=MAX (forces sequential even below natural threshold) AND with default threshold, both paths should take the sequential branch. Assert parallelNanos <= sequentialNanos * 1.10. Seeds 47L/48L (must match Plan 02 SmallArrayState).
    7. Test 3: smallArray_under10PercentRegression_at100k — same shape as test 2 but n = 100_000 (exactly at THRESHOLD boundary; sub-threshold branch will run by default since 100_000 is NOT < 100_000). Assert ratio <= 1.10. Seeds 47L/48L (must match Plan 02 SmallArrayState).
    8. Test 4: reduceLarge_isAtLeast2xFaster — for n = 10_000_000: time NumJa.sum(arr) under default threshold vs threshold=MAX. Assert ratio <= 0.5 (i.e., parallel is at least 2x faster).
    9. Add @After public void reset() { ParallelOps.resetThresholdForTesting(); }.
    10. Important JVM-warmup note: JUnit does NOT do JIT warmup beyond what `timeOp` does internally. If a test is flaky in CI, increase warmupIters to 5 (still keeps test time bounded — at 10^7 doubles the add takes ~90ms sequential / ~45ms parallel per iteration, so 5 + 9 = 14 iterations ~= 1.3 seconds per test, well under 60s).

    DO NOT use soft-fail — these tests are the hard success criterion gate (RESEARCH §Success Criteria). On CI noise, raise the warmup iters before lowering the ratio threshold.
</action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl modules/numja -am test -Dtest=ParallelRegressionTest -DfailIfNoTests=false</automated>
</verify>
  <acceptance_criteria>
    - mvn -pl modules/numja -am test -Dtest=ParallelRegressionTest exits 0 with all 4 in-class tests passing.
    - modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java contains the test method names largeArray_isAtLeast2xFaster, smallArray_under10PercentRegression_at10k, smallArray_under10PercentRegression_at100k, reduceLarge_isAtLeast2xFaster.
    - On the dev machine (i7-1255U), test 1 (largeArray_isAtLeast2xFaster) median parallel time at n = 10^7 is <= 50% of median sequential time (verify by reading the test output — expected add median is ~45ms parallel vs ~89ms sequential).
    - On the dev machine, tests 2 and 3 (smallArray) median parallel time at n = 10_000 and n = 100_000 is <= 110% of median sequential time (expected ratio near 1.0 since both paths run sequential below threshold).
    - On the dev machine, test 4 (reduceLarge_isAtLeast2xFaster) median parallel sum time at n = 10^7 is <= 50% of median sequential time (expected sum median is ~13ms parallel vs ~27ms sequential).
    - The @After reset guarantees threshold override does NOT leak to other tests.
</acceptance_criteria>
  <done>
    The timing-based regression gate is in place. Success criteria #1 (>=2x large) and #2 (<10% small) are now hard-fail JUnit assertions, not just JMH observations.
</done>
</task>

<task type="auto">
  <name>Task 02-04-02: Full suite regression check (no existing test broken</name>
  <files</files>
  <read_first>
    - .planning/STATE.md (8+5 tests: full suite reference)
    - .planning/phases/02-cpu-parallel-core-threads/02-VALIDATION.md §Wave 0 Requirements (full suite gate)
    - .planning/phases/01-baseline-benchmark/BASELINE.md (the speedup targets)
</read_first>
  <action>
    Run the full suite to confirm Phase 2 did not regress anything Phase 1 established as green. The command below is the standard STATE.md invocation. If any non-Phase-2 test fails (unrelated to this phase), STOP and report — do not modify unrelated tests.
</action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn test</automated>
</verify>
  <acceptance_criteria>
    - mvn test exits 0 from repo root.
    - All 8+5 tests per STATE.md are reported green.
    - modules/sklearn/target/golden-report.txt still contains PASS lines for sum(1e6) and mean(1e6) (Phase 1 accuracy gate held against the new parallel path — re-verified since Plan 03).
    - modules/numja/target/surefire-reports contains a green report for ThreadPoolConfigTest, ParallelOpsTest, ParallelElementwiseTest, ParallelReduceTest, ParallelRegressionTest, NDArrayTest.
</acceptance_criteria>
  <done>
    No regression to existing tests. Phase 2 changes are additive — elementwise/reduce on small arrays still go through the original sequential EJML/hand-rolled loops (bit-identical), and existing tests use small inputs so they were never on the parallel path.
</done>
</task>

<task type="auto">
  <name>Task 02-04-03: JMH bench run + 02-BASELINE-AFTER.md (large + small + reduce numbers</name>
  <files>.planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md</files>
  <read_first>
    - .planning/phases/01-baseline-benchmark/BASELINE.md (template — copy Machine metadata table + Kết quả structure)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pitfall 3 (hybrid P/E-core variance; methodology section requirement)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md §02-BASELINE-AFTER.md (NEW) section list
    - bench/src/main/java/bench/CoreBench.java (after Plan 02: SmallArrayState + add_elementwise_small available)
</read_first>
  <action>
    Step A Run the JMH benchmark sweep:
    1. Build the bench JAR: `mvn -pl bench -am package -DskipTests`.
    2. Run the FULL CoreBench suite at the same settings as Phase 1 baseline (BASELINE.md line 66): `java -jar bench/target/benchmarks.jar -f 1 -wi 3 -i 3 -w 1s -r 1s`. Capture stdout to a temp file.
    3. Also run the small-array benchmark specifically to capture n=10_000 and n=100_000: `java -jar bench/target/benchmarks.jar "CoreBench.add_elementwise_small" -p n=10000,100000`. Capture stdout.
    4. Also run the reduce benchmarks specifically: `java -jar bench/target/benchmarks.jar "CoreBench.sum_reduce" "CoreBench.mean_reduce"`. Capture stdout.

    Step B Write .planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md following BASELINE.md structure verbatim where possible:
    1. Title: BASELINE — NumJa Performance After Phase 2 (CPU Parallel Core)
    2. Date line: Measured 2026-08-XX with JMH (`bench/` module, `-f 1 -wi 3 -i 3 -w 1s -r 1s`).
    3. Intro paragraph: replace BASELINE.md intro with one explaining this is the AFTER measurement (parallel path active for n >= 100_000). Note hybrid P/E-core variance and the same-power-profile methodology requirement (RESEARCH Pitfall 3).
    4. Machine metadata table (copy verbatim from BASELINE.md lines 12-21).
    5. Kết quả section with Mode avgt, ms/op, Error = 99.9% CI, (n) = @Param.
    6. ### Core ops (parallel path) subsection: rows for add_elementwise 10^6 and 10^7, multiply_elementwise 10^6 and 10^7, sum_reduce 10^7, mean_reduce 10^7. Each row: `Score (ms/op)` from the JMH output AND a `Delta vs Phase 1` column showing ratio (e.g., 89.38 -> 44.2 = 1.98x speedup).
    7. ### Small-array regression subsection: rows for add_elementwise_small n=10_000 and n=100_000. Include explicit PASS/FAIL marker against the 10% regression threshold.
    8. ### Methodology subsection: 3-4 sentences on hybrid P/E-core variance (RESEARCH Pitfall 3), same power profile between baseline and Phase 2 runs, single fork (matches Phase 1 to keep the comparison fair).
    9. Reproduce section: copy the PowerShell block from BASELINE.md lines 63-68 plus add the new bench invocations for the small-array and reduce subsets.

    The actual numbers come from the JMH run; do NOT make up values. If the speedup ratio is below 2x on any of the success criteria targets, RECORD the actual number and flag it in the Methodology section — do not paper over. That is what verification is for.
</action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl bench -am package -DskipTests && java -jar bench/target/benchmarks.jar -f 1 -wi 3 -i 3 -w 1s -r 1s 2>&1 | tee .planning/phases/02-cpu-parallel-core-threads/jmh-phase2.log</automated>
</verify>
  <acceptance_criteria>
    - mvn -pl bench -am package -DskipTests exits 0 and java -jar bench/target/benchmarks.jar runs all CoreBench benchmarks including add_elementwise_small (n=10000, 100000).
    - .planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md exists and contains the literal section headers: ## Machine metadata, ## Kết quả (with subsections ### Core ops (parallel path) and ### Small-array regression), ## Methodology, ## Cách reproduce.
    - For each add_elementwise 10^7 row, the file shows BOTH the Phase 2 score AND the Phase 1 baseline (89.38ms) for direct comparison.
    - For each add_elementwise_small row, the file shows the ratio vs the Phase 1 sub-100k sequential path, with an explicit pass/fail against the 10% threshold.
    - If the JMH run produced a median < 2x speedup on any of the target sizes (10^6 add, 10^7 add, 10^7 sum), the Methodology section explicitly notes this and recommends investigation before declaring the phase complete.
</acceptance_criteria>
  <done>
    Phase 2 measurement complete. 02-BASELINE-AFTER.md committed alongside the implementation. Phase 2 success criteria #1 (>=2x large) and #2 (<10% small) are evidenced by both JUnit timing tests and JMH benchmark numbers.
</done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Reference Plan 01 — any threat rated high must have disposition = block. STRIDE review enforced at plan-close."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner to JUnit test JVM | Test inputs are random seed-driven; no external input |
| ParallelRegressionTest to ParallelOps | Package-private threshold override (same package) |
| JMH bench to NumJa | Same public API as Phase 1; no instrumentation hooks |
| 02-BASELINE-AFTER.md to .planning/STATE.md | Doc-only — no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | DoS (resource exhaustion) | ParallelRegressionTest timing at n=10^7 | accept | 5 warmup + 9 measure iterations ~= 14 calls per test = ~1.3 seconds total at Phase 2 speeds. Well under any timeout. Test data is allocated once per test and is GC'd at method exit. |
| T-2-02 | DoS (pool exhaustion) | ParallelRegressionTest | mitigate | Singleton FJP reused across tests via ThreadPoolConfig.getInstance().getForkJoinPool(). The resetThresholdForTesting call in @After does NOT touch the pool. |
| T-2-08 | Tampering (test-result forgery) | ParallelRegressionTest ratio assertions | mitigate | Tests use hard ratios (0.5 for >=2x, 1.10 for <10%) — not soft-fail. If CI noise makes a test flaky, raise warmupIters (5 -> 8) before lowering the ratio threshold; do NOT relax the threshold without recording rationale in the SUMMARY. |
| T-2-NUM | Tampering (numerical drift in BASELINE-AFTER) | JMH numbers | accept | JMH numbers are wall-clock measurements, not numerical output. Drift between Phase 1 and Phase 2 is the speedup signal we are MEASURING, not a bug. |
| T-2-SC | Tampering (supply chain) | No new packages | mitigate | No new dependencies; JDK stdlib + existing Maven modules. tldr SC package legitimacy gate satisfied. |

## ASVS L1 Coverage (unchanged)
- V5 Input Validation: BASELINE-AFTER.md reproduces the same machine metadata and JMH flags as Phase 1 BASELINE.md — methodology is auditable.
- V6/V2/V3: n/a.

## Out of Scope
- matmul speedup (Phase 3+ per BASELINE.md note; Phase 2 success criterion #1 is elementwise + reduce only).
- Kahan/tree-reduce summation (Phase 3 ACC-01).
- ComputeBackend interface (Phase 5 HW-01).
- Profiling-driven optimization of LEAF_CUTOFF (RESEARCH §Open Questions #1 — sweep deferred to a follow-up if 16_384 underperforms; not required for Phase 2 success criteria).
</threat_model>

<verification>
- mvn -pl modules/numja -am test -Dtest=ParallelRegressionTest 4 tests pass.
- mvn test exits 0 from repo root (all 8+5 + new tests green).
- java -jar bench/target/benchmarks.jar runs all CoreBench benchmarks including add_elementwise_small.
- .planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md committed with the four required section headers and actual JMH numbers.
- Target ratios (>=2x on large, <10% on small) recorded in the BASELINE-AFTER doc.
</verification>

<success_criteria>
- All Phase 2 requirements (CPU-01, CPU-02) have automated regression tests passing on the dev machine.
- Existing Phase 1 test suite (8+5 tests) remains green.
- JMH measurements recorded in 02-BASELINE-AFTER.md prove the success criteria with real wall-clock numbers.
- Phase 2 is ready for /gsd:verify-work.
</success_criteria>

<output>
Create .planning/phases/02-cpu-parallel-core-threads/02-04-SUMMARY.md when done. The SUMMARY must reference 02-BASELINE-AFTER.md and list the actual measured speedup ratios.
</output>
