---
phase: 02-cpu-parallel-core-threads
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/numja/src/main/java/numja/config/ThreadPoolConfig.java
  - modules/numja/src/test/java/com/numja/config/ThreadPoolConfigTest.java
  - modules/numja/src/main/java/numja/core/ParallelOps.java
  - modules/numja/src/test/java/com/numja/core/ParallelOpsTest.java
autonomous: true
requirements: [CPU-01, CPU-02]
user_setup: []
must_haves:
  truths:
    - "ThreadPoolConfig.getForkJoinPool() returns the same ForkJoinPool instance on repeated calls"
    - "ForkJoinPool is sized to getCurrentThreads() (60% of cores, NOT all of them)"
    - "ParallelOps.elementwiseBinary below THRESHOLD uses the sequential loop, not the FJP"
    - "ParallelOps.elementwiseBinary above THRESHOLD produces output equal to the sequential loop, bit-identical for n=10^6 doubles"
    - "ParallelOps.sum/min/max above THRESHOLD produce results matching the sequential path within relErr 1e-13"
  artifacts:
    - path: "modules/numja/src/main/java/numja/config/ThreadPoolConfig.java"
      provides: "ForkJoinPool singleton accessor"
      contains: "public ForkJoinPool getForkJoinPool"
    - path: "modules/numja/src/test/java/com/numja/config/ThreadPoolConfigTest.java"
      provides: "Singleton-pool regression test (mitigates T-2-02)"
      contains: "getForkJoinPool_isSingleton"
    - path: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      provides: "Threshold-gated ForkJoin elementwise + sum/min/max"
      contains: "class ParallelOps"
    - path: "modules/numja/src/test/java/com/numja/core/ParallelOpsTest.java"
      provides: "Threshold gate + correctness tests"
      contains: "elementwiseBinary_matchesSequential"
  key_links:
    - from: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      to: "modules/numja/src/main/java/numja/config/ThreadPoolConfig.java"
      via: "ThreadPoolConfig.getInstance().getForkJoinPool()"
      pattern: "ThreadPoolConfig\\.getInstance\\(\\)\\.getForkJoinPool"
    - from: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      to: "ForkJoinPool"
      via: "pool.invoke(RecursiveAction root)"
      pattern: "pool\\.invoke\\("
---

<objective>
Build the Wave-0 parallel infrastructure that CPU-01 and CPU-02 will plug into: a thread-pool singleton accessor on `ThreadPoolConfig` (mitigates T-2-02 pool exhaustion), and a `numja.core.ParallelOps` utility that owns the ForkJoin tree for elementwise + reduce paths. Tests assert threshold gating, singleton identity, and numeric equivalence with the existing sequential loops. No `NDArray` method body changes in this plan — wiring happens in plans 02 and 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md
@.planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md
@.planning/STATE.md
@modules/numja/src/main/java/numja/config/ThreadPoolConfig.java
@modules/numja/src/main/java/numja/core/NDArray.java
@modules/numja/src/test/java/com/numja/core/NDArrayTest.java
@modules/numja/pom.xml
</context>

<interfaces>
From `modules/numja/src/main/java/numja/config/ThreadPoolConfig.java` (current state, line 11-176):
- `public static synchronized ThreadPoolConfig getInstance()` — existing lazy singleton at lines 28-33; new accessor MUST follow this pattern.
- `public int getCurrentThreads()` — line 66-68; returns `currentThreads` (capped at 60% of cores via `calculateOptimalThreads()` line 38-40). The new FJP is sized to this value (RESEARCH §Pattern 3).
- `public void setThreads(Integer numThreads)` — line 82-92; mutates `currentThreads` but does NOT recreate the FJP (matches existing behavior). Limitation noted in RESEARCH §Open Questions #4.

From `modules/numja/src/main/java/numja/core/NDArray.java` (current state, line 151-368):
- Elementwise bodies at line 151-323 use either `CommonOps_DDRM.add/subtract/elementMult/scale` (delegating to EJML) or hand-rolled `for` loops over `data.data[i]`.
- Reduce bodies at line 329-368 are hand-rolled `for` loops. `sum` line 329-335, `min` line 347-355, `max` line 360-368. `mean` line 340-342 delegates to `sum()`.
- Shape-mismatch guard `if (!Arrays.equals(shape, other.shape))` at line 152-155 — stays in `NDArray`, NOT moved to `ParallelOps` (RESEARCH §Pitfall 7 + PATTERNS.md "Error handling").
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 02-01-01: Add lazy ForkJoinPool accessor to ThreadPoolConfig + ThreadPoolConfigTest</name>
  <files>
    modules/numja/src/main/java/numja/config/ThreadPoolConfig.java
    modules/numja/src/test/java/com/numja/config/ThreadPoolConfigTest.java
 </files>
  <behavior>
    - Test 1: getForkJoinPool_isSingleton — `cfg.getForkJoinPool() == cfg.getForkJoinPool()` returns true.
    - Test 2: getForkJoinPool_sizedToCurrentThreads — `cfg.getForkJoinPool().getParallelism() == cfg.getCurrentThreads()`.
    - Test 3: getForkJoinPool_survivesSetThreads — `cfg.setThreads(N)` does NOT throw and `getForkJoinPool()` returns the original instance (matches existing setThreads semantics — mutation only, no pool rebuild per RESEARCH §Open Questions #4).
 </behavior>
  <read_first>
    - modules/numja/src/main/java/numja/config/ThreadPoolConfig.java (whole file, especially getInstance() at line 28-33 and getCurrentThreads() at line 66-68)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md §Pattern 3 (lazy singleton FJP) and §Open Questions #4 (setThreads limitation)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md §"`modules/numja/src/main/java/numja/config/ThreadPoolConfig.java` (MODIFY — add `getForkJoinPool()`)"
 </read_first>
  <action>
    Modify `modules/numja/src/main/java/numja/config/ThreadPoolConfig.java`:
    1. Add `import java.util.concurrent.ForkJoinPool;` at the top of the import block (after the existing `java.util.HashMap` and `java.util.Map` imports).
    2. Add a private field `private volatile ForkJoinPool forkJoinPool;` next to the existing `private boolean autoOptimize;` field (around line 16).
    3. Insert a new package-private method `public ForkJoinPool getForkJoinPool()` AFTER the existing `get_threads()` method (around line 75, before `setThreads`). Use double-checked locking over `forkJoinPool`: first null-check on the volatile field, then `synchronized (this)` with a re-check, then `new ForkJoinPool(currentThreads)` and assign. This mirrors the lazy-init shape of the existing `getInstance()` method at line 28-33 — one FJP for the JVM lifetime. Add a one-line comment above the method explaining that `setThreads()` mutates `currentThreads` but does NOT recreate the pool (matches the existing `set_threads` semantics; RESEARCH §Open Questions #4).
    4. Do NOT add a `resetForkJoinPool()` method — out of scope (RESEARCH §Open Questions #4 says "matches existing behavior").
    5. Do NOT change the constructor, `getInstance()`, `calculateOptimalThreads()`, `setThreads()`, or any other existing method.

    Create `modules/numja/src/test/java/com/numja/config/ThreadPoolConfigTest.java`:
    1. Package declaration: `package numja.config;` (note: path is `com/numja/config/` to match existing test layout per PATTERNS.md §Conventions note).
    2. JUnit 4 imports: `org.junit.Test`, `org.junit.Assert.*`.
    3. Three tests with the names from the `<behavior>` block above. Use `ThreadPoolConfig.getInstance()` to get the singleton. Each test must end with `assertTrue`/`assertEquals` only — no soft-fail (this is a regression gate, not a measurement).
    4. The third test must call `setThreads(1)` and confirm the pool is unchanged (instance equality holds). Call `resetToDefaults()` in an `@After` block to restore the thread count for subsequent tests.
 </action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/numja -am test -Dtest=ThreadPoolConfigTest -DfailIfNoTests=false
   </automated>
 </verify>
  <acceptance_criteria>
    - `modules/numja/src/main/java/numja/config/ThreadPoolConfig.java` contains the string `public ForkJoinPool getForkJoinPool()` and the field declaration `private volatile ForkJoinPool forkJoinPool;`
    - `modules/numja/src/main/java/numja/config/ThreadPoolConfig.java` contains the string `import java.util.concurrent.ForkJoinPool;`
    - `modules/numja/src/test/java/com/numja/config/ThreadPoolConfigTest.java` exists, contains the test method names `getForkJoinPool_isSingleton`, `getForkJoinPool_sizedToCurrentThreads`, `getForkJoinPool_survivesSetThreads`, and is on the test classpath under `com/numja/config/` with `package numja.config;`
    - `mvn -pl modules/numja -am test -Dtest=ThreadPoolConfigTest` exits 0 with all three tests passing
    - The previous `ThreadPoolConfig.getInstance()` semantics are unchanged (no new fields exposed, no constructor change)
 </acceptance_criteria>
  <done>
    FJP singleton accessor exists and is unit-tested; T-2-02 (pool exhaustion) mitigation is in place. `ThreadPoolConfig` still compiles with no public API additions.
 </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 02-01-02: Create ParallelOps + ParallelOpsTest (threshold gate, elementwise + reduce ForkJoin</name>
  <files>
    modules/numja/src/main/java/numja/core/ParallelOps.java
    modules/numja/src/test/java/com/numja/core/ParallelOpsTest.java
 </files>
  <behavior>
    - Test 1 (threshold gate): `belowThreshold_usesSequential` — `ParallelOps.elementwiseBinary(a, b, out, Double::sum)` with `a.length = THRESHOLD - 1` populates `out` correctly. The test stubs out `ThreadPoolConfig.getForkJoinPool()` indirectly by asserting the call does NOT increase any pool state — simpler: assert output correctness at this size and rely on the next test to confirm the parallel path is exercised.
    - Test 2 (correctness, large): `elementwiseBinary_matchesSequential` — for `n = 1_000_000`, compute `a + b` via `ParallelOps.elementwiseBinary` and compare to a known-vector hand-computed expected value at indices `[0, mid, last]`. Use delta `1e-9` (matches `NDArrayTest.java:15-22`).
    - Test 3 (sum correctness, large): `sum_matchesSequential` — for `n = 1_000_000` (above THRESHOLD) with seed-driven data, `ParallelOps.sum(data)` returns within relErr `1e-13` of a hand-computed sequential double-loop sum.
    - Test 4 (min/max correctness): `min_matchesSequential` and `max_matchesSequential` — for `n = 500_000`, return exact min/max (deterministic for the given seed, since min/max are exact IEEE ops). Assert delta `0.0`.
    - Test 5 (threshold constant): `THRESHOLD_is100k` — `ParallelOps.THRESHOLD == 100_000` (RESEARCH §User Constraints: threshold dispatch target ~100k).
 </behavior>
  <read_first>
    - modules/numja/src/main/java/numja/core/NDArray.java (line 151-160 for `add` elementwise body, line 329-335 for `sum`, line 347-355 for `min`, line 360-368 for `max` — copy the exact sequential loops for the sub-threshold branches)
    - modules/numja/src/main/java/numja/config/ThreadPoolConfig.java (just edited in task 02-01-01 — confirm `getInstance().getForkJoinPool()` is callable)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md §Pattern 1 (threshold-gated ForkJoin elementwise), §Pattern 2 (tree-partitioned reduce), §Common Pitfalls §Pitfall 1 (boxing), §Pitfall 2 (false sharing), §Pitfall 5 (numerical stability), §Pitfall 7 (no public API drift)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md §"`modules/numja/src/main/java/numja/core/ParallelOps.java` (NEW — utility)"
    - modules/numja/src/test/java/com/numja/core/NDArrayTest.java (JUnit 4 + delta 1e-9 style at line 15-22)
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java line 67-70 for the `relErr` helper pattern
 </read_first>
  <action>
    Create `modules/numja/src/main/java/numja/core/ParallelOps.java`:
    1. Package: `numja.core`. Imports: `java.util.concurrent.ForkJoinPool`, `java.util.concurrent.RecursiveAction`, `java.util.function.DoubleBinaryOperator`. NO `numja.*` imports needed (NDArray/ThreadPoolConfig are referenced by FQN or via `numja.config.ThreadPoolConfig`).
    2. Declare two package-private constants: `public static final int THRESHOLD = 100_000;` and `static final int LEAF_CUTOFF = 16_384;` (LEAF_CUTOFF is package-private — internal tuning knob per RESEARCH §Open Questions #1; expose as `int` so future plans can sweep).
    3. Define a package-private functional interface `interface DoubleBinaryOp { double apply(double a, double b); }` — wait, just use `java.util.function.DoubleBinaryOperator` directly. It already exists in JDK 17. No new interface.
    4. Public method `public static void elementwiseBinary(double[] a, double[] b, double[] out, DoubleBinaryOperator op)`:
       - Compute `final int n = a.length;`.
       - If `n < THRESHOLD`: hand-rolled `for (int i = 0; i < n; i++) out[i] = op.applyAsDouble(a[i], b[i]);` — sub-threshold branch is sequential (RESEARCH §Pattern 1).
       - Else: `ForkJoinPool pool = ThreadPoolConfig.getInstance().getForkJoinPool(); pool.invoke(new ElementwiseTask(a, b, out, op, 0, n));`.
    5. Private static class `ElementwiseTask extends RecursiveAction` with fields `double[] a, b, out; DoubleBinaryOperator op; int lo, hi;`. `compute()`: if `hi - lo <= LEAF_CUTOFF`, sequential leaf loop; else `int mid = (lo + hi) >>> 1; invokeAll(new ElementwiseTask(..., lo, mid), new ElementwiseTask(..., mid, hi));` (RESEARCH §Pattern 1, uses `>>>` to avoid overflow).
    6. Public methods `public static double sum(double[] data)`, `public static double min(double[] data)`, `public static double max(double[] data)` — each follows the same threshold-gated shape but uses a `SumTask` / `MinTask` / `MaxTask` `RecursiveAction` with a single accumulator field. The leaf computes a per-chunk local sum (or local min/max); parent merges left/right at `join()`. The accumulator types: `double` for sum, `double` for min/max (use `Double.MAX_VALUE` / `-Double.MAX_VALUE` as initial). The merge order MUST be deterministic left-to-right (RESEARCH §Pitfall 5).
    7. Add Javadoc one-liner above each public method matching the style of `NDArray.java:148-150`.
    8. Do NOT add any other public surface. `ParallelOps` MUST NOT be referenced by anything outside the `numja.core` and `numja.config` packages (RESEARCH §Pitfall 7).

    Create `modules/numja/src/test/java/com/numja/core/ParallelOpsTest.java`:
    1. Package: `numja.core`. Path: `modules/numja/src/test/java/com/numja/core/` (per PATTERNS.md §Conventions note — existing test root is `com/numja/core/`).
    2. JUnit 4 (`org.junit.Test`, `org.junit.Assert.*`).
    3. Helper method `private static double[] randomVec(int n, long seed)` mirroring `CoreBench.java:32-37`.
    4. Implement the five tests from the `<behavior>` block. The two largest arrays (1_000_000 and 500_000) are below 16 MB and fit in CI heap easily — no `-Xmx` tuning needed.
    5. For the `relErr` helper inside `sum_matchesSequential`, copy the pattern from `GoldenReferenceTest.java:67-70` (`denom = Math.max(Math.abs(expected), 1e-30)`). Use tolerance `1e-13` to match RESEARCH §Pitfall 5 / STATE.md.
 </action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/numja -am test -Dtest=ParallelOpsTest -DfailIfNoTests=false
   </automated>
 </verify>
  <acceptance_criteria>
    - `modules/numja/src/main/java/numja/core/ParallelOps.java` exists, contains `public static final int THRESHOLD = 100_000;`, `static final int LEAF_CUTOFF = 16_384;`, the public method `elementwiseBinary(double[], double[], double[], DoubleBinaryOperator)`, and the three public reduce methods `sum`, `min`, `max`. Also contains private static inner classes `ElementwiseTask`, `SumTask`, `MinTask`, `MaxTask` each extending `RecursiveAction`.
    - `ParallelOps` contains no `import numja.ArrayOps` and no `import numja.NumJa` — package boundary enforced (RESEARCH §Pitfall 7).
    - `mvn -pl modules/numja -am test -Dtest=ParallelOpsTest` exits 0 with all five tests passing.
    - `mvn -pl modules/numja -am test -Dtest=NDArrayTest` STILL exits 0 (no regression to existing elementwise/reduce behavior — parallel code path is not yet wired into `NDArray`).
    - No new public class/method appears in `numja.core.ArrayOps` or `numja.NumJa` (RESEARCH §Pitfall 7 check: `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java modules/numja/src/main/java/numja/NumJa.java` returns the SAME count as before this plan).
 </acceptance_criteria>
  <done>
    `ParallelOps` infrastructure is in place and unit-tested. CPU-01 and CPU-02 wiring can now plug into `ParallelOps.elementwiseBinary`, `ParallelOps.sum`, `ParallelOps.min`, `ParallelOps.max` without touching the FJP mechanics again.
 </done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Any threat rated high severity must have disposition = block. STRIDE review enforced at plan-close (this plan + Plans 02-04)."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → ParallelOps | `NDArray` methods (internal); inputs already shape-checked upstream |
| ParallelOps → ForkJoinPool | `ThreadPoolConfig.getInstance().getForkJoinPool()` — singleton (no per-call allocation) |
| RecursiveAction tree → heap | `double[] a, b, out` — caller-owned, no copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | DoS (resource exhaustion) | `ParallelOps.elementwiseBinary` and `sum`/`min`/`max` | mitigate | Gate: `if (n < THRESHOLD) return sequential;` BEFORE any FJP submission. Sub-threshold calls never enter the pool. THRESHOLD = 100_000 enforces a per-call minimum work. Test `ParallelOpsTest.THRESHOLD_is100k` asserts the constant value. |
| T-2-02 | DoS (pool exhaustion) | ForkJoinPool lifecycle | mitigate | Singleton via `ThreadPoolConfig.getForkJoinPool()` (Task 02-01-01). Double-checked locking on `volatile ForkJoinPool forkJoinPool`. Test `ThreadPoolConfigTest.getForkJoinPool_isSingleton` asserts same instance on 2 calls. `setThreads()` does NOT recreate pool (matches existing semantics; documented limitation). |
| T-2-03 | Tampering | RecursiveAction field access | accept | All task fields are private final; no public setters. Caller cannot mutate mid-flight. |
| T-2-04 | Information disclosure | n/a | accept | Library, no PII, no network — no info-disclosure surface. |
| T-2-05 | Repudiation | n/a | accept | No external transactions. |
| T-2-06 | Elevation of privilege | n/a | accept | No auth surface. |
| T-2-SC | Tampering (supply chain) | JDK 17 stdlib (ForkJoinPool, RecursiveAction) | mitigate | No new dependencies in this plan — only `java.util.concurrent.*` and `java.util.function.DoubleBinaryOperator` (both JDK 17 stdlib). `tldr SC` package legitimacy gate is satisfied by stdlib — no install task, no `[ASSUMED]`/`[SUS]` packages introduced. |
| T-2-NUM | Tampering (numerical) | Sum reduce | mitigate | Per-chunk partial sums computed in left-to-right order; parent merge in deterministic left-to-right tree (RESEARCH §Pitfall 5). Test `ParallelOpsTest.sum_matchesSequential` asserts relErr ≤ 1e-13. |

## ASVS L1 Coverage

- V5 Input Validation: shape/length validation lives in `NDArray` (unchanged). `ParallelOps` trusts upstream.
- V6 Cryptography: n/a.
- V2 Authentication: n/a.
- V3 Session Management: n/a.

## Out of Scope for This Plan

- `NDArray` method body changes (Plans 02 + 03).
- Benchmark harness additions (Plan 02).
- Full regression sweep (Plan 04).
</threat_model>

<verification>
- `mvn -pl modules/numja -am test -Dtest=ThreadPoolConfigTest` — passes (3 tests).
- `mvn -pl modules/numja -am test -Dtest=ParallelOpsTest` — passes (5 tests).
- `mvn -pl modules/numja -am test -Dtest=NDArrayTest` — still passes (no NDArray changes yet).
- `grep -c "public static" modules/numja/src/main/java/numja/NumJa.java` returns same count as before (RESEARCH §Pitfall 7: no public API drift).
</verification>

<success_criteria>
- All Wave-0 infrastructure files exist and pass unit tests (ThreadPoolConfig FJP singleton, ParallelOps + threshold gating, elementwise + reduce correctness).
- T-2-01 + T-2-02 mitigations are in place and test-verified.
- CPU-01 (Plan 02) and CPU-02 (Plan 03) can now consume `ParallelOps.elementwiseBinary/sum/min/max` directly.
- No changes to `numja.NumJa`, `numja.core.ArrayOps`, or any `NDArray` method body.
</success_criteria>

<output>
Create `.planning/phases/02-cpu-parallel-core-threads/02-01-SUMMARY.md` when done.
</output>
