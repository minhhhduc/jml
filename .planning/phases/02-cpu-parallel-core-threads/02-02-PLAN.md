---
phase: 02-cpu-parallel-core-threads
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - modules/numja/src/main/java/numja/core/ParallelOps.java
  - modules/numja/src/main/java/numja/core/NDArray.java
  - modules/numja/src/test/java/com/numja/core/ParallelElementwiseTest.java
  - bench/src/main/java/bench/CoreBench.java
autonomous: true
requirements: [CPU-01]
user_setup: []
must_haves:
  truths:
    - "NDArray.add(NDArray), NDArray.add(double), subtract, multiply, divide, power, abs, sqrt, exp, log, sin, cos, tan all dispatch to ParallelOps above THRESHOLD"
    - "Same methods stay on the sequential EJML/hand-rolled path when n < THRESHOLD"
    - "Elementwise operations on n = 10^6 produce results matching sequential reference within relErr 1e-13"
    - "CoreBench exposes add_elementwise_small benchmark with @Param {10000, 100000}"
  artifacts:
    - path: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      provides: "elementwiseUnary + scalarBinary + test threshold hook"
      contains: "public static void elementwiseUnary"
    - path: "modules/numja/src/main/java/numja/core/NDArray.java"
      provides: "Threshold-gated dispatch for every elementwise op"
      contains: "ParallelOps.elementwiseBinary"
    - path: "modules/numja/src/test/java/com/numja/core/ParallelElementwiseTest.java"
      provides: "Cross-path correctness for elementwise ops above THRESHOLD"
      contains: "add_matchesSequential_aboveThreshold"
    - path: "bench/src/main/java/bench/CoreBench.java"
      provides: "Small-array regression benchmark"
      contains: "add_elementwise_small"
  key_links:
    - from: "modules/numja/src/main/java/numja/core/NDArray.java"
      to: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      via: "ParallelOps.elementwiseBinary / elementwiseUnary / scalarBinary"
      pattern: "ParallelOps.elementwise"
    - from: "modules/numja/src/test/java/com/numja/core/ParallelElementwiseTest.java"
      to: "modules/numja/src/main/java/numja/core/NDArray.java"
      via: "NumJa.add(NDArray, NDArray) public API"
      pattern: "NumJa.add|NumJa.multiply"
---

<objective>
Wire the threshold-gated parallel path into every elementwise op on NDArray so NumJa.add, multiply, exp, etc. exercise ParallelOps.elementwiseBinary/elementwiseUnary/scalarBinary above 100k elements while staying bit-identical to the existing EJML/sequential path below. Add a ParallelElementwiseTest that compares parallel-path results to a sequential reference at n = 10^6 and forces the sequential path at the same sizes via a package-private test hook on ParallelOps. Extend CoreBench with SmallArrayState and add_elementwise_small so Plan 04 can prove the less-than-10-percent regression criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md
@.planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md
@.planning/phases/02-cpu-parallel-core-threads/02-VALIDATION.md
@.planning/STATE.md
@modules/numja/src/main/java/numja/core/NDArray.java
@modules/numja/src/main/java/numja/core/ParallelOps.java
@modules/numja/src/test/java/com/numja/core/NDArrayTest.java
@bench/src/main/java/bench/CoreBench.java
@.planning/phases/01-baseline-benchmark/BASELINE.md
</context>

<interfaces>
From modules/numja/src/main/java/numja/core/ParallelOps.java (created in Plan 01):
- public static final int THRESHOLD = 100_000; gate value.
- public static void elementwiseBinary(double[] a, double[] b, double[] out, DoubleBinaryOperator op) binary parallel op.
- For unary ops this plan extends ParallelOps with elementwiseUnary(double[] in, double[] out, DoubleUnaryOperator op).
- Package-private setThresholdForTesting(int n) and resetThresholdForTesting() added in this plan.

From modules/numja/src/main/java/numja/core/NDArray.java:
- add(NDArray) line 151-160, add(double) line 165-172, subtract(NDArray) line 177-185, multiply(NDArray) line 197-205, multiply(double) line 210-214, divide(NDArray) line 219-229, power(double) line 241-247, abs() line 252-258, sqrt() line 263-269, exp() line 274-280, log() line 285-291, sin() line 296-302, cos() line 307-313, tan() line 318-324.
- Shape-mismatch guard if (!Arrays.equals(shape, other.shape)) at line 152-155 UNCHANGED.

From bench/src/main/java/bench/CoreBench.java:
- ElemState line 64-77 analog for new SmallArrayState (use seeds 47L/48L per PATTERNS.md).
- add_elementwise line 79-82 analog for add_elementwise_small (reuse randomVec helper at line 32-37).
- @Warmup(5,1) @Measurement(5,1) @Fork(1) at class level line 19-21 DO NOT modify.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 02-02-01: Extend ParallelOps with elementwiseUnary/scalarBinary/test hook, then wire NDArray elementwise ops through the threshold gate</name>
  <files>modules/numja/src/main/java/numja/core/ParallelOps.java, modules/numja/src/main/java/numja/core/NDArray.java</files>
  <read_first>
    - modules/numja/src/main/java/numja/core/ParallelOps.java (just created in Plan 01; mirror for unary)
    - modules/numja/src/main/java/numja/core/NDArray.java (line 151-323, every elementwise body to gate)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md NDArray (MODIFY gate elementwise + reduce) wrapper recipe
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pattern 1 (threshold-gated ForkJoin), Pitfall 7 (no public API drift)
 </read_first>
  <action>
    Step A Extend modules/numja/src/main/java/numja/core/ParallelOps.java:
    1. Add import java.util.function.DoubleUnaryOperator.
    2. Add public method public static void elementwiseUnary(double[] in, double[] out, DoubleUnaryOperator op) mirroring elementwiseBinary shape but with one input array. Leaf loop for (int i = lo; i < hi; i++) out[i] = op.applyAsDouble(in[i]);. Task UnaryTask extends RecursiveAction with fields double[] in, out; DoubleUnaryOperator op; int lo, hi;.
    3. Add public method public static void scalarBinary(double[] a, double scalar, double[] out, DoubleBinaryOperator op) for scalar overloads. Same ForkJoin tree shape; scalar is broadcast, leaf loop for (int i = lo; i < hi; i++) out[i] = op.applyAsDouble(a[i], scalar);. Task ScalarBinaryTask extends RecursiveAction.
    4. Add package-private static field static volatile int testThresholdOverride = -1; and methods static void setThresholdForTesting(int n) and static void resetThresholdForTesting(). Update the gate check in EVERY public method (elementwiseBinary, elementwiseUnary, scalarBinary, sum, min, max) to read int gate = (testThresholdOverride >= 0) ? testThresholdOverride : THRESHOLD; then if (n < gate) { sequential; return; }. One-line comment near testThresholdOverride notes it is for tests only.
    5. Do NOT modify the ElementwiseTask, SumTask, MinTask, MaxTask recursive split / leaf loop logic from Plan 01.

    Step B Modify modules/numja/src/main/java/numja/core/NDArray.java:
    1. Add imports at the top: import numja.config.ThreadPoolConfig; and import java.util.function.DoubleBinaryOperator;. Same-package numja.core.ParallelOps does NOT need an explicit import.
    2. add(NDArray) line 151-160: keep shape check at line 152-155 UNCHANGED, replace CommonOps_DDRM.add(data, other.data, result); with threshold-gated dispatch: int n = data.numRows * data.numCols; if (n < ParallelOps.THRESHOLD) { CommonOps_DDRM.add(data, other.data, result); } else { ParallelOps.elementwiseBinary(data.data, other.data, result.data, Double::sum); }. Return statement unchanged.
    3. Same wrapping recipe for subtract(NDArray) (binary op (x,y) -> x - y), multiply(NDArray) (binary op (x,y) -> x * y), divide(NDArray) (binary op (x,y) -> x / y).
    4. add(double scalar) line 165-172: keep result.setTo(data); then gate n. Above threshold: ParallelOps.scalarBinary(data.data, scalar, result.data, (x,s) -> x + s);.
    5. multiply(double scalar) line 210-214: replace CommonOps_DDRM.scale(scalar, data, result) with threshold-gated pattern using scalarBinary + (x,s) -> x * s.
    6. subtract(double scalar) line 190-192 delegates to add(-scalar) no change.
    7. divide(double scalar) line 234-236 delegates to multiply(1.0 / scalar) no change.
    8. For each unary op power(double), abs(), sqrt(), exp(), log(), sin(), cos(), tan() (lines 241-324): gate on n. Above threshold: ParallelOps.elementwiseUnary(data.data, result.data, Math::exp) (or Math::log, Math::sin, Math::cos, Math::tan, Math::abs, Math::sqrt, d -> Math.pow(d, exponent) for power).
    9. Preserve every Javadoc comment above the gated methods. Do NOT touch sum, min, max, mean, prod, negate, flatten, toDoubleArray, toArray, toString, copy, reshape, getTranspose, get, set.
 </action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl modules/numja -am test -Dtest=NDArrayTest -DfailIfNoTests=false</automated>
 </verify>
  <acceptance_criteria>
    - mvn -pl modules/numja -am test -Dtest=NDArrayTest exits 0 (sub-threshold path identical).
    - modules/numja/src/main/java/numja/core/NDArray.java contains ParallelOps.elementwiseBinary, ParallelOps.elementwiseUnary, ParallelOps.scalarBinary, and ParallelOps.THRESHOLD (grep confirms). The 14 elementwise methods listed in the action are gated.
    - mvn -pl modules/numja -am test -Dtest=ParallelOpsTest STILL exits 0.
    - modules/numja/src/main/java/numja/core/ParallelOps.java contains public static void elementwiseUnary, public static void scalarBinary, and static void setThresholdForTesting.
    - grep -c "public static" modules/numja/src/main/java/numja/NumJa.java returns SAME count as before Plan 02 (no public API drift).
 </acceptance_criteria>
  <done>
    Every elementwise op on NDArray now dispatches to ParallelOps above THRESHOLD. Sub-threshold path preserved bit-identically. ParallelOps extended with elementwiseUnary + scalarBinary + the test threshold hook.
 </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 02-02-02: ParallelElementwiseTest, cross-path correctness above threshold</name>
  <files>modules/numja/src/test/java/com/numja/core/ParallelElementwiseTest.java</files>
  <behavior>
    - Test 1: add_matchesSequential_aboveThreshold, n = 10^6, NumJa.add(a, b) (parallel path) matches sequential reference within relErr 1e-13 at indices [0, n/2, n-1].
    - Test 2: multiply_matchesSequential_aboveThreshold, same shape for NumJa.multiply.
    - Test 3: exp_matchesSequential_aboveThreshold, NumJa.exp(a) parallel vs sequential Math.exp reference at n = 10^6 within abs 1e-12.
    - Test 4: add_forcedSequential_matchesParallel, ParallelOps.setThresholdForTesting(Integer.MAX_VALUE) then ParallelOps.elementwiseBinary(a, b, out, Double::sum) directly twice on copies; assert both out arrays identical (relErr 0.0).
    - Test 5: scalarAdd_matchesSequential_aboveThreshold, a.add(0.5) parallel vs sequential for n = 10^6 within abs 0.0 (scalar 0.5 is IEEE exact).
 </behavior>
  <read_first>
    - modules/numja/src/main/java/numja/core/NDArray.java (line 151-205 for add/subtract/multiply; line 274-280 for exp)
    - modules/numja/src/main/java/numja/core/ParallelOps.java (the new setThresholdForTesting, elementwiseBinary, elementwiseUnary, scalarBinary from Task 02-02-01)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pitfall 5 (numerical stability, relErr 1e-13)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md ParallelElementwiseTest (NEW)
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java line 67-70 for relErr helper
    - bench/src/main/java/bench/CoreBench.java line 32-37 for randomVec helper
 </read_first>
  <action>
    Create modules/numja/src/test/java/com/numja/core/ParallelElementwiseTest.java:
    1. Package numja.core. Path modules/numja/src/test/java/com/numja/core/.
    2. Imports: numja.NumJa; numja.core.NDArray; numja.core.ParallelOps; org.junit.After; org.junit.Test; org.junit.Assert.*;.
    3. Helper private static NDArray randomVec(int n, long seed) copying CoreBench.java:32-37. Helper private static double relErr(double actual, double expected) copying GoldenReferenceTest.java:67-70.
    4. Implement the 5 tests from the <behavior> block. Use sizes n = 1_000_000 (above THRESHOLD = 100_000, ~8 MB heap). Seeds 50L/51L.
    5. For add_matchesSequential_aboveThreshold: build reference by calling NumJa.add(a, b) AFTER ParallelOps.setThresholdForTesting(Integer.MAX_VALUE) (forces sequential branch). Sample indices [0, n/2, n-1]. Compare via assertTrue(relErr(actual, expected) <= 1e-13, ...).
    6. For exp_matchesSequential_aboveThreshold: build reference the same way (force sequential). abs 1e-12 is sufficient (Math.exp is exact to 1 ulp).
    7. For add_forcedSequential_matchesParallel: call ParallelOps.elementwiseBinary(a, b, out, Double::sum) twice with different threshold overrides on copies of a, b. Assert both out arrays identical (relErr 0.0).
    8. For scalarAdd_matchesSequential_aboveThreshold: use a.add(0.5). Assert delta 0.0.
    9. Add @After public void reset() { ParallelOps.resetThresholdForTesting(); } so the override never leaks.
 </action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl modules/numja -am test -Dtest=ParallelElementwiseTest -DfailIfNoTests=false</automated>
 </verify>
  <acceptance_criteria>
    - mvn -pl modules/numja -am test -Dtest=ParallelElementwiseTest exits 0 with all 5 tests passing.
    - mvn -pl modules/numja -am test -Dtest=NDArrayTest STILL exits 0.
    - mvn -pl modules/numja -am test -Dtest=ParallelOpsTest STILL exits 0.
    - The @After reset guarantees threshold override does NOT leak to other tests.
 </acceptance_criteria>
  <done>
    Elementwise correctness verified above THRESHOLD. Parallel path and sequential path produce identical floating-point results within IEEE rounding (relErr <= 1e-13).
 </done>
</task>

<task type="auto">
  <name>Task 02-02-03: Extend CoreBench with SmallArrayState + add_elementwise_small benchmark</name>
  <files>bench/src/main/java/bench/CoreBench.java</files>
  <read_first>
    - bench/src/main/java/bench/CoreBench.java (whole file; ElemState at line 64-77, add_elementwise at line 79-82, randomVec helper at line 32-37)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md CoreBench (MODIFY)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pitfall 3 (hybrid P/E-core variance, do NOT modify @Fork(1))
    - .planning/phases/01-baseline-benchmark/BASELINE.md (89.38ms add elementwise 10^7 baseline)
 </read_first>
  <action>
    Modify bench/src/main/java/bench/CoreBench.java:
    1. Add a new state class SmallArrayState AFTER the existing ElemState (after line 77) and BEFORE add_elementwise (line 79). Use @State(Scope.Thread), @Param({"10000", "100000"}), seeds 47L/48L. Mirror ElemState line 64-77 exactly except for param values and seeds.
    2. Add @Benchmark public void add_elementwise_small(SmallArrayState s, Blackhole bh) { bh.consume(NumJa.add(s.a, s.b)); } AFTER the existing multiply_elementwise (line 85-87) and BEFORE the reduce section header at line 89.
    3. Do NOT modify the class-level annotations (@BenchmarkMode, @OutputTimeUnit, @Warmup, @Measurement, @Fork).
    4. Do NOT touch MatState, ElemState, ReduceState, or any existing benchmark method.
 </action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl bench -am package -DskipTests</automated>
 </verify>
  <acceptance_criteria>
    - mvn -pl bench -am package -DskipTests exits 0.
    - bench/src/main/java/bench/CoreBench.java contains SmallArrayState, add_elementwise_small, @Param({"10000", "100000"}), and uses 47L/48L seeds.
    - New benchmark placed between multiply_elementwise and the // ---------------- reduce ---------------- comment.
    - Existing benchmarks unchanged. git diff shows ONLY additions.
 </acceptance_criteria>
  <done>
    CoreBench.add_elementwise_small is runnable at n=10_000 and n=100_000, providing the small-array measurements Plan 04 needs.
 </done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Reference Plan 01 — any threat rated high must have disposition = block. STRIDE review enforced at plan-close."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller to NDArray.add/sub/mul/div/... | External via NumJa.add(NDArray, NDArray); inputs shape-checked in NDArray (unchanged) |
| NDArray.add to ParallelOps.elementwiseBinary | Internal; gate n < THRESHOLD blocks tiny-parallel DoS (T-2-01) |
| ParallelOps to ForkJoinPool | Singleton via ThreadPoolConfig.getInstance().getForkJoinPool() (T-2-02) |
| Test to ParallelOps.setThresholdForTesting | Package-private; only same-package tests can force sequential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | DoS (resource exhaustion) | NDArray elementwise dispatch | mitigate (carried from Plan 01) | Each gated method computes int n = data.numRows * data.numCols; and checks if (n < ParallelOps.THRESHOLD) BEFORE any FJP submit. Test ParallelElementwiseTest.add_matchesSequential_aboveThreshold exercises the above-threshold branch. |
| T-2-02 | DoS (pool exhaustion) | ForkJoinPool lifecycle | mitigate (carried from Plan 01) | Singleton FJP. setThresholdForTesting does NOT touch the pool. |
| T-2-05 | Repudiation (test-leak) | ParallelOps.setThresholdForTesting | mitigate | Package-private access. Every test that uses it MUST call resetThresholdForTesting() in @After. ParallelElementwiseTest @After hook enforces this for the whole class. |
| T-2-07 | Tampering (numerical) | Parallel unary/scalar elementwise | mitigate | Test asserts relErr <= 1e-13 for binary ops and abs <= 1e-12 for unary exp. |
| T-2-SC | Tampering (supply chain) | JDK 17 stdlib (DoubleBinaryOperator, DoubleUnaryOperator) | mitigate | No new dependencies; stdlib only. tldr SC package legitimacy gate satisfied. |

## ASVS L1 Coverage (unchanged)
- V5 Input Validation: shape check at NDArray.add line 152-155 stays put.
- V6/V2/V3: n/a.

## Out of Scope
- Reduce ops (sum/min/max/mean); Plan 03.
- ParallelRegressionTest timing assertions; Plan 04.
- Full JMH 02-BASELINE-AFTER.md run; Plan 04.
</threat_model>

<verification>
- mvn -pl modules/numja -am test -Dtest=NDArrayTest passes.
- mvn -pl modules/numja -am test -Dtest=ParallelOpsTest Plan 01 tests still pass.
- mvn -pl modules/numja -am test -Dtest=ParallelElementwiseTest 5 new tests pass.
- mvn -pl bench -am package -DskipTests bench compiles.
- grep -c "public static" modules/numja/src/main/java/numja/NumJa.java returns SAME count (no public API drift).
</verification>

<success_criteria>
- Every elementwise op on NDArray (binary, scalar, unary) routes to ParallelOps above THRESHOLD.
- Sub-threshold behavior bit-identical (existing NDArrayTest passes).
- Above-threshold correctness matches sequential reference within relErr 1e-13.
- CoreBench.add_elementwise_small built and runnable at n=10k and n=100k.
</success_criteria>

<output>
Create .planning/phases/02-cpu-parallel-core-threads/02-02-SUMMARY.md when done.
</output>
