---
phase: 02-cpu-parallel-core-threads
plan: 03
type: tdd
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - modules/numja/src/main/java/numja/core/NDArray.java
  - modules/numja/src/test/java/com/numja/core/ParallelReduceTest.java
autonomous: true
requirements: [CPU-02]
user_setup: []
must_haves:
  truths:
    - "NDArray.sum(), NDArray.min(), NDArray.max() all dispatch to ParallelOps above THRESHOLD"
    - "NDArray.mean() continues to delegate to sum() (no double-wrapping the parallel path)"
    - "Reduce ops on n = 10^6 (above THRESHOLD) match the sequential result within relErr 1e-13"
    - "min/max produce exact (delta 0.0) results above threshold"
    - "GoldenReferenceTest still passes (no regression to sum/mean tolerance check)"
  artifacts:
    - path: "modules/numja/src/main/java/numja/core/NDArray.java"
      provides: "Threshold-gated dispatch for sum/min/max"
      contains: "ParallelOps.sum"
    - path: "modules/numja/src/test/java/com/numja/core/ParallelReduceTest.java"
      provides: "Cross-path correctness for reduce ops above THRESHOLD"
      contains: "sum_matchesSequential_aboveThreshold"
  key_links:
    - from: "modules/numja/src/main/java/numja/core/NDArray.java"
      to: "modules/numja/src/main/java/numja/core/ParallelOps.java"
      via: "ParallelOps.sum(data.data) / ParallelOps.min(data.data) / ParallelOps.max(data.data)"
      pattern: "ParallelOps\\.(sum|min|max)"
    - from: "modules/numja/src/main/java/numja/core/NDArray.java"
      to: "modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java"
      via: "sum() / mean() called by GoldenReferenceTest.sum_mean_vs_numpy"
      pattern: "NumJa\\.sum|NumJa\\.mean"
---

<objective>
Wire the threshold-gated parallel path into NDArray.sum, NDArray.min, NDArray.max (mean already delegates to sum so no change). Add ParallelReduceTest that compares parallel-path reduce results to a sequential reference at n = 10^6 with deterministic seed data, asserts relErr 1e-13 for sum/mean and exact equality for min/max, and exercises the test threshold hook to force the sequential path for cross-checking. Confirm GoldenReferenceTest sum_mean_vs_numpy (the existing tolerance gate from Phase 1) still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md
@.planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md
@.planning/phases/02-cpu-parallel-core-threads/02-VALIDATION.md
@.planning/STATE.md
@modules/numja/src/main/java/numja/core/NDArray.java
@modules/numja/src/main/java/numja/core/ParallelOps.java
@modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java
@.planning/phases/01-baseline-benchmark/BASELINE.md
</context>

<interfaces>
From modules/numja/src/main/java/numja/core/ParallelOps.java (Plans 01 and 02):
- public static double sum(double[] data), public static double min(double[] data), public static double max(double[] data). Each threshold-gates on n (with the setThresholdForTesting hook from Plan 02 already wired into all three).
- THRESHOLD = 100_000. SumTask / MinTask / MaxTask are private RecursiveAction inner classes with deterministic left-to-right merge (RESEARCH Pitfall 5).

From modules/numja/src/main/java/numja/core/NDArray.java:
- sum() line 329-335, mean() line 340-342, min() line 347-355, max() line 360-368 are hand-rolled sequential loops on data.data[i].
- mean() at line 340-342 is sum() / getSize() — already routed through sum, so wiring sum() automatically covers mean().

From modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java:
- sum_mean_vs_numpy test at line 160-171: uses NumJa.sum(arr) and NumJa.mean(arr) on n = 1_000_000 doubles, soft-fail accumulator. This is the existing tolerance gate that must remain green.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 02-03-01: Wire NDArray.sum, min, max through the ParallelOps threshold gate</name>
  <files>modules/numja/src/main/java/numja/core/NDArray.java</files>
  <read_first>
    - modules/numja/src/main/java/numja/core/NDArray.java (line 329-335 sum, line 340-342 mean, line 347-355 min, line 360-368 max; preserve Javadoc style at line 148-150)
    - modules/numja/src/main/java/numja/core/ParallelOps.java (sum/min/max already implemented in Plan 01 with threshold hook from Plan 02)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md NDArray (MODIFY gate elementwise + reduce) reduce wrappers
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pattern 2 (tree-partitioned reduce) and Pitfall 5 (deterministic left-to-right merge)
</read_first>
  <action>
    Modify modules/numja/src/main/java/numja/core/NDArray.java:
    1. sum() line 329-335: wrap the existing sequential loop with the threshold gate. Sub-threshold: keep the exact existing double-loop (sum += data.data[i];). Above threshold: return ParallelOps.sum(data.data);. Preserve the Javadoc at line 326-328.
    2. mean() line 340-342: NO change. mean() already delegates to sum() which is now parallel-aware. Do not wrap mean() in another threshold gate (would create double-dispatch).
    3. min() line 347-355: wrap with the threshold gate. Sub-threshold: keep the exact existing loop (Double.MAX_VALUE init, if (data.data[i] < min) min = data.data[i];). Above threshold: return ParallelOps.min(data.data);. Preserve Javadoc.
    4. max() line 360-368: wrap with the threshold gate. Sub-threshold: keep the exact existing loop (-Double.MAX_VALUE init, if (data.data[i] > max) max = data.data[i];). Above threshold: return ParallelOps.max(data.data);. Preserve Javadoc.
    5. Do NOT touch prod() (line 373-379) — out of scope for Phase 2 (Phase 3 accuracy hardening handles it).
    6. Verify by reading the file end-to-end after edits to confirm: (a) every reduce method's Javadoc is preserved, (b) sub-threshold loops are byte-identical to the originals, (c) mean() still calls sum() exactly once.
</action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl modules/numja -am test -Dtest=NDArrayTest -DfailIfNoTests=false</automated>
</verify>
  <acceptance_criteria>
    - mvn -pl modules/numja -am test -Dtest=NDArrayTest exits 0 (sub-threshold path identical, mean() still works through sum()).
    - modules/numja/src/main/java/numja/core/NDArray.java contains the strings ParallelOps.sum, ParallelOps.min, ParallelOps.max, and ParallelOps.THRESHOLD (grep confirms) in the reduce section.
    - mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest exits 0 (sum_mean_vs_numpy soft-fail gate still records PASS for sum/mean; nothing in NumJa changed that would break it).
    - mean() body at line 340-342 is UNCHANGED (still `return sum() / getSize();`).
    - prod() at line 373-379 is UNCHANGED.
</acceptance_criteria>
  <done>
    sum(), min(), max() dispatch to ParallelOps above THRESHOLD. Sub-threshold behavior bit-identical. mean() inherits parallel path via sum() delegation.
</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 02-03-02: ParallelReduceTest, cross-path correctness above threshold for sum/min/max/mean</name>
  <files>modules/numja/src/test/java/com/numja/core/ParallelReduceTest.java</files>
  <behavior>
    - Test 1: sum_matchesSequential_aboveThreshold, n = 1_000_000, NumJa.sum(arr) parallel path matches sequential reference within relErr 1e-13.
    - Test 2: min_matchesSequential_aboveThreshold, n = 500_000, NumJa.min(arr) parallel equals sequential with delta 0.0 (min/max are exact IEEE ops).
    - Test 3: max_matchesSequential_aboveThreshold, n = 500_000, NumJa.max(arr) parallel equals sequential with delta 0.0.
    - Test 4: mean_matchesSequential_aboveThreshold, n = 1_000_000, NumJa.mean(arr) parallel matches sequential within relErr 1e-13.
    - Test 5: sum_forcedSequential_matchesParallel, ParallelOps.setThresholdForTesting(Integer.MAX_VALUE) then directly call ParallelOps.sum(data) twice on copies, assert both results equal (delta 0.0). This proves the parallel reduce and sequential reduce produce bit-identical output for the same input.
    - Test 6: goldenReferenceTest_sumMean_stillPasses, smoke check that the existing sum_mean_vs_numpy golden tolerance holds with the new parallel path (run GoldenReferenceTest as part of the test class via @RunWith or just rely on the separate `mvn` invocation in verify).
</behavior>
  <read_first>
    - modules/numja/src/main/java/numja/core/NDArray.java (line 329-368 sum/min/max; confirm wiring from Task 02-03-01)
    - modules/numja/src/main/java/numja/core/ParallelOps.java (sum/min/max + setThresholdForTesting from Plan 02)
    - .planning/phases/02-cpu-parallel-core-threads/02-RESEARCH.md Pitfall 5 (deterministic merge order, relErr 1e-13)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md ParallelRegressionTest (NEW) — analog for harness style
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java line 67-70 (relErr helper), line 160-171 (sum_mean_vs_numpy for the smoke check)
    - bench/src/main/java/bench/CoreBench.java line 32-37 (randomVec helper)
</read_first>
  <action>
    Create modules/numja/src/test/java/com/numja/core/ParallelReduceTest.java:
    1. Package numja.core. Path modules/numja/src/test/java/com/numja/core/.
    2. Imports: numja.NumJa; numja.core.NDArray; numja.core.ParallelOps; org.junit.After; org.junit.Test; org.junit.Assert.*;.
    3. Helper private static NDArray randomVec(int n, long seed) copying CoreBench.java:32-37. Helper private static double relErr(double actual, double expected) copying GoldenReferenceTest.java:67-70.
    4. Implement the 5 in-class tests from the <behavior> block (test 6 is verified separately via the test command below). Use seeds 60L/61L.
    5. For sum_matchesSequential_aboveThreshold: build reference by computing a hand-written sequential sum on the raw double[] (extract via arr.toDoubleArray() or NumJa.toDoubleArray if public). Compare via assertTrue(relErr(actual, expected) <= 1e-13, ...).
    6. For min_matchesSequential_aboveThreshold / max_matchesSequential_aboveThreshold: build reference the same way (hand-written loop). Assert delta 0.0.
    7. For mean_matchesSequential_aboveThreshold: NumJa.mean(arr) parallel vs hand-computed sequential mean (sum / n). relErr 1e-13.
    8. For sum_forcedSequential_matchesParallel: ParallelOps.setThresholdForTesting(Integer.MAX_VALUE), call ParallelOps.sum(data) twice on the same input (the second call uses the override path which falls back to sequential). Assert both results delta 0.0 (deterministic — same operation order). NOTE: this only proves the sequential path matches itself; the real cross-check is test 1. Keep this test as a sanity gate for the threshold hook.
    9. Add @After public void reset() { ParallelOps.resetThresholdForTesting(); }.
    10. Test 6 is verified by the mvn command in `<verify>` below — do NOT add a JUnit @Test method that calls NumJa.sum (keep this file focused on cross-path correctness; GoldenReferenceTest is the existing tolerance gate).
</action>
  <verify>
    <automated>cd D:\unknown\projects\java_ml; $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path; mvn -pl modules/numja -am test -Dtest=ParallelReduceTest -DfailIfNoTests=false && mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest -DfailIfNoTests=false</automated>
</verify>
  <acceptance_criteria>
    - mvn -pl modules/numja -am test -Dtest=ParallelReduceTest exits 0 with all 5 in-class tests passing.
    - mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest exits 0 and the captured report (target/golden-report.txt per STATE.md) contains PASS lines for sum(1e6) and mean(1e6) entries (manual grep `grep PASS modules/sklearn/target/golden-report.txt`).
    - mvn -pl modules/numja -am test -Dtest=NDArrayTest STILL exits 0.
    - mvn -pl modules/numja -am test -Dtest=ParallelOpsTest STILL exits 0.
    - mvn -pl modules/numja -am test -Dtest=ParallelElementwiseTest STILL exits 0.
    - The @After reset guarantees threshold override does NOT leak to other tests.
</acceptance_criteria>
  <done>
    Reduce correctness verified above THRESHOLD. sum/min/max/mean all match sequential reference within IEEE rounding (relErr <= 1e-13, exact for min/max). GoldenReferenceTest continues to pass — the existing tolerance gate holds against the new parallel path.
</done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Reference Plan 01 — any threat rated high must have disposition = block. STRIDE review enforced at plan-close."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller to NDArray.sum/min/max | External via NumJa.sum(NDArray), NumJa.min(NDArray), NumJa.max(NDArray); inputs are NDArray instances already constructed (no shape check needed for scalar reduces) |
| NDArray.sum to ParallelOps.sum | Internal; gate n < THRESHOLD blocks tiny-parallel DoS (T-2-01) |
| ParallelOps to ForkJoinPool | Singleton via ThreadPoolConfig.getInstance().getForkJoinPool() (T-2-02) |
| ParallelOps.sum -> deterministic merge | Per-chunk partials in left-to-right order, parent merge in deterministic left-to-right tree (RESEARCH Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | DoS (resource exhaustion) | NDArray.sum/min/max dispatch | mitigate (carried from Plans 01/02) | Each gated method computes int n = data.numRows * data.numCols; and checks if (n < ParallelOps.THRESHOLD) BEFORE any FJP submit. Test ParallelReduceTest.sum_matchesSequential_aboveThreshold exercises the above-threshold branch. |
| T-2-02 | DoS (pool exhaustion) | ForkJoinPool lifecycle | mitigate (carried from Plan 01) | Singleton FJP. setThresholdForTesting does NOT touch the pool. |
| T-2-05 | Repudiation (test-leak) | ParallelOps.setThresholdForTesting | mitigate | Package-private access. ParallelReduceTest @After hook enforces resetThresholdForTesting() for the whole class. |
| T-2-NUM | Tampering (numerical) | Tree-partitioned sum reduce | mitigate | Deterministic left-to-right merge order. Test asserts relErr <= 1e-13 vs sequential reference at n = 10^6. GoldenReferenceTest.sum_mean_vs_numpy (Phase 1's existing tolerance gate) re-validates against precomputed NumPy references at n = 10^6 with tol 1e-13. |
| T-2-SC | Tampering (supply chain) | No new packages | mitigate | No new dependencies; JDK stdlib only. tldr SC package legitimacy gate satisfied. |

## ASVS L1 Coverage (unchanged)
- V5 Input Validation: scalar reduce ops do not need shape validation; trust NDArray invariants. Library internal use only.
- V6/V2/V3: n/a.

## Out of Scope
- prod() reduce (Phase 3 handles accuracy hardening).
- matmul (Phase 2 success criterion #1 is elementwise + reduce only; matmul is Phase 3+ target per BASELINE.md note).
- ParallelRegressionTest timing assertions; Plan 04.
- Full JMH 02-BASELINE-AFTER.md run; Plan 04.
</threat_model>

<verification>
- mvn -pl modules/numja -am test -Dtest=NDArrayTest passes.
- mvn -pl modules/numja -am test -Dtest=ParallelOpsTest passes (Plan 01).
- mvn -pl modules/numja -am test -Dtest=ParallelElementwiseTest passes (Plan 02).
- mvn -pl modules/numja -am test -Dtest=ParallelReduceTest 5 new tests pass.
- mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest passes; target/golden-report.txt contains PASS lines for sum and mean.
- mvn -pl bench -am package -DskipTests bench still compiles.
</verification>

<success_criteria>
- NDArray.sum/min/max dispatch to ParallelOps above THRESHOLD; mean() inherits via sum() delegation.
- Sub-threshold reduce behavior bit-identical (NDArrayTest passes).
- Above-threshold reduce matches sequential within relErr 1e-13 (sum/mean) and exactly (min/max).
- GoldenReferenceTest sum/mean tolerance gate still passes — the existing Phase 1 accuracy harness re-validates the new parallel path.
- Plan 04 can now run a timing regression test and write 02-BASELINE-AFTER.md.
</success_criteria>

<output>
Create .planning/phases/02-cpu-parallel-core-threads/02-03-SUMMARY.md when done.
</output>
