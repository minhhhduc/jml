---
phase: 04-adaptive-memory-model
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - bench/src/main/java/bench/PandasBench.java
  - scripts/check_regression.ps1
  - .planning/phases/04-adaptive-memory-model/04-baseline.json
  - .planning/phases/04-adaptive-memory-model/04-BASELINE-AFTER.md
  - .planning/phases/04-adaptive-memory-model/04-VERIFICATION.md
autonomous: true
requirements: [BENCH-03]
user_setup: []
must_haves:
  truths:
    - "`bench/src/main/java/bench/PandasBench.java` exposes 2 new `@Benchmark` methods: `read_csv_streaming_california` (consumes the chunk iterator over california_housing.csv) and `streaming_groupby_sum_titanic` (consumes chunked titanic.csv through `RunningGroupAggregator.sum`)"
    - "Running `mvn -pl bench -am package -DskipTests` succeeds (no compilation errors in the new benchmarks)"
    - "`scripts/check_regression.ps1` is updated to point its `$BaselinePath` at `.planning/phases/04-adaptive-memory-model/04-baseline.json`; the diff loop is unchanged (same perf_benchmarks schema)"
    - "`04-baseline.json` EXTENDS `03-baseline.json` — contains ALL Phase 2 perf benchmarks + ALL Phase 3 accuracy checks + 2 new Phase 4 streaming benchmarks (`read_csv_streaming_california`, `streaming_groupby_sum_titanic`), each with `score_ms` and `tolerance_pct: 50.0` (matching Phase 3's 50% tolerance rationale for hybrid P/E noise floor)"
    - "Running `powershell -ExecutionPolicy Bypass -File scripts/check_regression.ps1` from repo root succeeds against the freshly-measured Phase 4 baseline numbers (self-consistency gate, mirrors Phase 3 pattern)"
    - "`04-BASELINE-AFTER.md` documents the JMH-measured streaming benchmark numbers, the Phase 3 gate still-green status, public API surface unchanged (NumJa=61, ArrayOps=34, Pandas=+1 streaming method, GaussianNB=+2 partial_fit/finalize methods)"
    - "`04-VERIFICATION.md` documents at least 4 must-haves per requirement ID (MEM-01/02/03 + USE-02 + BENCH-03) with status, evidence, source file path, and line-number references (mirrors Phase 3 12/12 pattern)"
  artifacts:
    - path: "bench/src/main/java/bench/PandasBench.java"
      provides: "2 new JMH benchmarks for streaming read_csv + streaming groupby"
      contains: "read_csv_streaming_california"
    - path: "scripts/check_regression.ps1"
      provides: "Updated $BaselinePath pointing at 04-baseline.json; diff logic unchanged"
      contains: "04-baseline.json"
    - path: ".planning/phases/04-adaptive-memory-model/04-baseline.json"
      provides: "Comprehensive baseline: Phase 2 perf + Phase 3 accuracy + Phase 4 streaming benchmarks"
      contains: "streaming_groupby_sum_titanic"
    - path: ".planning/phases/04-adaptive-memory-model/04-BASELINE-AFTER.md"
      provides: "JMH-measured Phase 4 streaming benchmark numbers + Phase 3 gate still-green confirmation"
      contains: "read_csv_streaming_california"
    - path: ".planning/phases/04-adaptive-memory-model/04-VERIFICATION.md"
      provides: "Phase 4 must-have coverage proof (>=4 must-haves per requirement ID, mirroring Phase 3 12/12)"
      contains: "MEM-01"
  key_links:
    - from: "scripts/check_regression.ps1"
      to: ".planning/phases/04-adaptive-memory-model/04-baseline.json"
      via: "$BaselinePath variable pointing at 04-baseline.json (was 03-baseline.json in Phase 3)"
      pattern: "04-baseline\\.json"
    - from: ".planning/phases/04-adaptive-memory-model/04-baseline.json"
      to: ".planning/phases/03-numerical-accuracy-hardening/03-baseline.json"
      via: "Carries forward ALL Phase 3 perf_benchmarks + accuracy_checks entries; ADDS 2 Phase 4 streaming benchmarks"
      pattern: "softmax_extreme_logits"
    - from: "bench/src/main/java/bench/PandasBench.java"
      to: "modules/pandas/src/main/java/pandas/Pandas.java"
      via: "Pandas.read_csv_streaming(california.toString(), opts) returns Iterator<DataFrame>"
      pattern: "read_csv_streaming"
---

<objective>
Wave-4 verification + benchmark gate: extend `PandasBench.java` with 2 streaming benchmarks, create `04-baseline.json` as a COMPREHENSIVE additive extension of `03-baseline.json` (all Phase 2/3 entries preserved + 2 new Phase 4 streaming benchmarks), update `scripts/check_regression.ps1` to point at the Phase 4 baseline path (diff logic unchanged), re-run the JMH suite fresh, commit `04-BASELINE-AFTER.md` with measured streaming numbers + Phase 3 gate still-green confirmation, and produce `04-VERIFICATION.md` documenting must-have coverage for MEM-01/02/03 + USE-02 + BENCH-03.

Purpose: ship the Phase 4 BENCH-03 regression gate (the gate now reads from the comprehensive 04-baseline.json) and prove the Phase 4 streaming additions don't regress Phase 2/3 baselines.

Output: 1 MODIFIED bench file + 1 MODIFIED gate script + 3 NEW artifacts (JSON baseline + AFTER.md + VERIFICATION.md). No new Maven dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-adaptive-memory-model/04-01-PLAN.md
@.planning/phases/04-adaptive-memory-model/04-01-SUMMARY.md
@.planning/phases/04-adaptive-memory-model/04-02-PLAN.md
@.planning/phases/04-adaptive-memory-model/04-02-SUMMARY.md
@.planning/phases/04-adaptive-memory-model/04-03-PLAN.md
@.planning/phases/04-adaptive-memory-model/04-03-SUMMARY.md
@.planning/phases/04-adaptive-memory-model/04-PATTERNS.md
@.planning/phases/04-adaptive-memory-model/04-RESEARCH.md
@.planning/phases/04-adaptive-memory-model/04-VALIDATION.md
@.planning/phases/03-numerical-accuracy-hardening/03-baseline.json
@.planning/phases/03-numerical-accuracy-hardening/03-BASELINE-AFTER.md
@.planning/phases/03-numerical-accuracy-hardening/03-VERIFICATION.md
@.planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md
@scripts/check_regression.ps1
@bench/src/main/java/bench/PandasBench.java
@bench/src/main/java/bench/SklearnBench.java
</context>

<interfaces>
From `bench/src/main/java/bench/PandasBench.java`:
- Existing `@Setup(Level.Trial) public void setUp()` lines 31-35 loads `californiaDf` + `titanicDf` via in-memory `Pandas.read_csv`. ADD a setup for streaming: a `ChunkedReadOptions` field with `chunkRows = 10_000` initialized in the same `setUp()`.
- Existing `@Benchmark read_csv_california(Blackhole)` lines 37-40 — the in-memory benchmark; UNCHANGED.
- Existing `@Benchmark groupby_mean_titanic(Blackhole)` lines 42-45 — UNCHANGED.

From `scripts/check_regression.ps1` (Phase 3 version):
- The script reads `$BaselinePath = '.planning/phases/03-numerical-accuracy-hardening/03-baseline.json'` (Phase 3 path). UPDATE the variable to `'.planning/phases/04-adaptive-memory-model/04-baseline.json'`. The diff loop, JMH invocation, and exit logic stay IDENTICAL — only the path changes.

From `.planning/phases/03-numerical-accuracy-hardening/03-baseline.json`:
- Schema: `{_meta, perf_benchmarks: [{benchmark, params, score_ms, tolerance_pct, ...}], accuracy_checks: [{op, max_rel_err, fixture, enforced_by}]}`.
- `tolerance_pct: 50.0` is the Phase 3 standard (keep at 50.0 for Phase 4 streaming benchmarks; Phase 2 perf entries keep their original tolerance per Phase 3 carry-forward).

From `.planning/phases/03-numerical-accuracy-hardening/03-VERIFICATION.md`:
- Must-have coverage table pattern: `| Must-Have ID | Truth | Evidence | Status |` rows, one per requirement ID. Phase 4 must cover MEM-01/02/03 + USE-02 + BENCH-03 (5 requirements; >=4 must-haves each = >=20 total).

From `.planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md`:
- Machine metadata table format (copy verbatim).
- "## Kết quả" section with subsection per benchmark group.
- "## Methodology" + "## Cách reproduce" + "## Nhận xét nhanh" sections.

From `modules/pandas/src/main/java/pandas/Pandas.java` (Wave-2):
- `read_csv_streaming(String, ChunkedReadOptions)` returns `Iterator<DataFrame>` — used by the 2 new benchmarks.

From `modules/pandas/src/main/java/pandas/internal/RunningGroupAggregator.java` (Wave-2):
- Static `sum(Iterator<DataFrame>, String groupColumn, String aggColumn)` — used by `streaming_groupby_sum_titanic`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 04-04-01: Add 2 streaming benchmarks to PandasBench.java
  <files>bench/src/main/java/bench/PandasBench.java
  <read_first>
    - bench/src/main/java/bench/PandasBench.java (the existing JMH setup + 2 benchmarks to extend)
    - bench/src/main/java/bench/SklearnBench.java (the JMH annotation style + state scope pattern to mirror)
    - modules/pandas/src/main/java/pandas/Pandas.java (read_csv_streaming signature from Wave-2)
    - modules/pandas/src/main/java/pandas/internal/RunningGroupAggregator.java (sum signature from Wave-2)
    - .planning/phases/04-adaptive-memory-model/04-PATTERNS.md (Pandas.java additive streaming path contract)
    - .planning/phases/04-adaptive-memory-model/04-RESEARCH.md (Anti-Patterns: don't load whole file into List)
</read_first>
  <action>
    Modify `bench/src/main/java/bench/PandasBench.java`:

    1. Add new imports: `import pandas.internal.ChunkedReadOptions;` and `import java.util.Iterator;`.

    2. Add a new field after `titanicDf`:
       ```java
       private ChunkedReadOptions streamingOpts;
       ```
       Initialize it in `setUp()` AFTER the two existing reads:
       ```java
       streamingOpts = new ChunkedReadOptions().chunkRows(10_000);
       ```

    3. Add new `@Benchmark` method for streaming read_csv:
       ```java
       @Benchmark
       public void read_csv_streaming_california(Blackhole bh) throws Exception {
           Iterator<DataFrame> it = Pandas.read_csv_streaming(CALIFORNIA.toString(), streamingOpts);
           int totalRows = 0;
           while (it.hasNext()) {
               DataFrame chunk = it.next();
               totalRows += chunk.shape()[0];
           }
           bh.consume(totalRows);
       }
       ```
       Add a `# ponytail:` comment: benchmark does NOT close the streaming reader — handle reclaimed by GC; add try-with-resources if this benchmark shows handle leak in process explorer.

    4. Add new `@Benchmark` method for streaming groupby:
       ```java
       @Benchmark
       public void streaming_groupby_sum_titanic(Blackhole bh) throws Exception {
           Iterator<DataFrame> it = Pandas.read_csv_streaming(TITANIC.toString(), streamingOpts);
           DataFrame result = pandas.internal.RunningGroupAggregator.sum(it, "sex", "fare");
           bh.consume(result);
       }
       ```
       titanic.csv has `sex` (male/female) and `fare` (numeric) columns — verify with `dist/datasets/titanic.csv` and adjust if column name differs.

    5. DO NOT modify the existing 2 `@Benchmark` methods.

    6. The class-level JMH annotations are unchanged.

    Verify compilation: `mvn -pl bench -am package -DskipTests` must exit 0.
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl bench -am package -DskipTests
</automated>
</verify>
  <acceptance_criteria>
    - `bench/src/main/java/bench/PandasBench.java` contains the new method `public void read_csv_streaming_california(Blackhole bh) throws Exception` annotated with `@Benchmark`.
    - The file also contains the new method `public void streaming_groupby_sum_titanic(Blackhole bh) throws Exception` annotated with `@Benchmark`.
    - Both new methods use `Pandas.read_csv_streaming(...)` (grep verification returns >= 2 matches).
    - The new `streaming_groupby_sum_titanic` calls `pandas.internal.RunningGroupAggregator.sum(...)` (grep verification returns 1 match).
    - `mvn -pl bench -am package -DskipTests` exits 0.
    - The existing 2 benchmarks are UNCHANGED.
</acceptance_criteria>
  <done>
    2 new JMH streaming benchmarks added and compile. The bench jar exposes 4 benchmarks: 2 in-memory (Phase 2 carry-forward) + 2 streaming (Phase 4 new).
</done>
</task>

<task type="auto">
  <name>Task 04-04-02: Create 04-baseline.json + update gate script path
  <files>
    - .planning/phases/04-adaptive-memory-model/04-baseline.json
    - scripts/check_regression.ps1
  <read_first>
    - .planning/phases/03-numerical-accuracy-hardening/03-baseline.json (source schema + values to carry forward verbatim)
    - .planning/phases/03-numerical-accuracy-hardening/03-BASELINE-AFTER.md (machine metadata + Phase 2/3 numbers)
    - scripts/check_regression.ps1 (the script whose $BaselinePath line is updated)
    - .planning/STATE.md (confirms 50% tolerance rationale from Phase 3; do NOT reintroduce 15%)
    - .planning/phases/04-adaptive-memory-model/04-VALIDATION.md (per-task verification map)
</read_first>
  <action>
    1. Create `.planning/phases/04-adaptive-memory-model/04-baseline.json` — JSON object (2-space indent, valid JSON, no trailing commas). Structure:

    - `_meta`: `phase: 04-adaptive-memory-model`, `created: 2026-08-28`, `machine: Intel Core i7-1255U, 15.7GB RAM, Windows 11, JDK 25.0.3+9, Maven 3.9.15`, `perf_tolerance_pct: 50.0`, `carries_from: 03-baseline.json (Phase 2 perf + Phase 3 accuracy, all preserved verbatim) + adds 2 Phase 4 streaming benchmarks`, `tolerance_rationale: Phase 3 raised default from 15% to 50% due to hybrid P/E noise floor on i7-1255U; Phase 4 inherits the 50% standard`.

    - `perf_benchmarks`: array of 10 entries — 8 Phase 2 carry-forward (verbatim from 03-baseline.json: add_elementwise x2, multiply_elementwise x2, sum_reduce, mean_reduce, add_elementwise_small x2) + 2 NEW Phase 4 entries:
      - `{"benchmark": "read_csv_streaming_california", "params": {"chunkRows": 10000}, "score_ms": <MEASURED>, "tolerance_pct": 50.0, "phase4_change": "NEW — chunked read_csv; expected ~chunk allocation overhead vs in-memory read_csv_california=71.572ms"}`
      - `{"benchmark": "streaming_groupby_sum_titanic", "params": {"chunkRows": 10000}, "score_ms": <MEASURED>, "tolerance_pct": 50.0, "phase4_change": "NEW — streaming groupby via RunningGroupAggregator.sum; expected sum_reduce + per-chunk merge overhead"}`

    - `accuracy_checks`: array of 3 entries (Phase 3 verbatim carry-forward): softmax_extreme_logits, logsumexp_simple, sum_pathological_cancellation — each with `max_rel_err` + `enforced_by`.

    Replace `<MEASURED>` placeholders with the FRESH JMH numbers from Task 04-04-03 (run JMH FIRST, capture numbers, THEN write the JSON with real values).

    2. Update `scripts/check_regression.ps1` — change ONLY the `$BaselinePath` variable (one line change):
    - Find the line `$BaselinePath = '.planning/phases/03-numerical-accuracy-hardening/03-baseline.json'`
    - Replace with: `$BaselinePath = '.planning/phases/04-adaptive-memory-model/04-baseline.json'`
    - Update the auto-generated header comment from `Auto-generated by Phase 3` to `Auto-generated by Phase 4 (extends Phase 3 logic, points at 04-baseline.json)`.
    - All other logic (build phase, JMH invocation, diff loop, exit-on-regression) stays byte-identical to Phase 3.

    3. Verify JSON validity after writing:
    ```powershell
    Get-Content .planning/phases/04-adaptive-memory-model/04-baseline.json | ConvertFrom-Json
    ```
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      powershell -Command "Get-Content .planning/phases/04-adaptive-memory-model/04-baseline.json | ConvertFrom-Json | Select-Object -ExpandProperty perf_benchmarks | Measure-Object | Select-Object -ExpandProperty Count"
      grep "BaselinePath" scripts/check_regression.ps1
</automated>
</verify>
  <acceptance_criteria>
    - `.planning/phases/04-adaptive-memory-model/04-baseline.json` exists, is valid JSON.
    - The file contains `_meta`, `perf_benchmarks`, and `accuracy_checks` top-level keys.
    - `perf_benchmarks` has 10 entries: 8 Phase 2 carry-forward + 2 new Phase 4 streaming.
    - The 2 Phase 4 entries have `"benchmark"` matching the new `@Benchmark` method names.
    - Each Phase 4 entry has non-zero `score_ms` (freshly measured), `tolerance_pct: 50.0`, `params: {"chunkRows": 10000}`.
    - `accuracy_checks` has 3 entries (Phase 3 verbatim carry-forward).
    - `scripts/check_regression.ps1` line containing `BaselinePath` is updated to `.planning/phases/04-adaptive-memory-model/04-baseline.json`.
    - `git diff scripts/check_regression.ps1` shows ONLY the `$BaselinePath` value + header comment change; diff loop unchanged.
</acceptance_criteria>
  <done>
    04-baseline.json committed with Phase 2/3 entries carried forward verbatim + 2 new Phase 4 streaming benchmarks with freshly-measured numbers. Gate script points at 04-baseline.json. JSON schema identical to 03-baseline.json so diff logic works unchanged.
</done>
</task>

<task type="auto">
  <name>Task 04-04-03: Run JMH + commit 04-BASELINE-AFTER.md + gate self-consistency check
  <files>
    - .planning/phases/04-adaptive-memory-model/04-BASELINE-AFTER.md
  <read_first>
    - .planning/phases/02-cpu-parallel-core-threads/02-BASELINE-AFTER.md (template — copy structure + machine metadata)
    - .planning/phases/03-numerical-accuracy-hardening/03-BASELINE-AFTER.md (Phase 3 update — column style + intentional-Phase-change flagging)
    - scripts/check_regression.ps1 (gate script for self-consistency verification)
    - .planning/STATE.md (confirms 50% tolerance, hybrid P/E noise floor, public API surface notes)
</read_first>
  <action>
    1. Run JMH benchmarks FRESH (the executor MUST actually run these — do NOT copy numbers from prior phases):

    ```powershell
    cd D:\unknown\projects\java_ml
    $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
    mvn -pl bench -am package -DskipTests
    java -jar bench/target/benchmarks.jar -f 1 -wi 3 -i 3 -w 1s -r 1s
    ```

    Capture stdout. The 2 new benchmarks appear in the output:
    - `PandasBench.read_csv_streaming_california` — expected ~10-30ms/op (california_housing.csv ~20k rows; 2 chunks at 10k rows; mostly BufferedReader overhead + Series allocation per chunk)
    - `PandasBench.streaming_groupby_sum_titanic` — expected ~5-15ms/op (titanic.csv ~891 rows; single chunk; aggregator overhead)

    2. Run the gate script self-consistency check (per Phase 3 pattern — gate MUST pass against freshly-measured baseline on first run):

    ```powershell
    powershell -ExecutionPolicy Bypass -File scripts/check_regression.ps1
    ```

    Expected: exit code 0, output PASS message. If non-zero, debug (typical cause: measured number exceeds baseline + 50% tolerance).

    3. Commit `.planning/phases/04-adaptive-memory-model/04-BASELINE-AFTER.md` with FRESH measured numbers. Structure (copy from 02-BASELINE-AFTER.md + 03-BASELINE-AFTER.md):

    - "## Machine metadata" — verbatim from Phase 2 (same machine).
    - "## Kết quả" with three subsections:
      - "Streaming benchmarks (Phase 4 NEW)" — table: `Benchmark | chunkRows | Score (ms/op) | vs in-memory | Status`. Rows for the 2 new streaming benchmarks.
      - "In-memory benchmarks (Phase 2/3 carry-forward — gate must stay green)" — table summarizing Phase 2/3 numbers WITH the Phase 4 re-measurement. For each: `Benchmark | Phase 2 Score | Phase 3 Score | Phase 4 Score | Status (PASS/FLAG)`. Proves Phase 4 didn't regress Phase 2/3 paths.
      - "Small-array regression (threshold-gate structural property)" — copy from Phase 2/3 verbatim.
    - "## Methodology" — same as Phase 3: hybrid P/E variance, single-fork, JMH 99.9% CI.
    - "## Cách reproduce" — `pwsh -ExecutionPolicy Bypass -File scripts/check_regression.ps1` from repo root, plus the manual JMH command.
    - "## Nhận xét nhanh (input cho Phase 5+)" — observations:
      - Streaming benchmarks added; both within expected noise envelope (or flagged with explanation).
      - Phase 3 BENCH-03 gate STILL GREEN against 04-baseline.json — JSON-extends-03 pattern preserves all Phase 2/3 thresholds.
      - Public API surface: NumJa.java = 61 (frozen), ArrayOps.java = 34 (frozen), Pandas.java = +1 method (read_csv_streaming, Wave-2), GaussianNB.java = +2 methods (partial_fit, finalize, Wave-3). All additive; no existing signature touched.
      - Test count after Phase 4: 55 (pre-Phase-4) + 13 (Wave-1) + 13 (Wave-2) + 9 (Wave-3) = ~90 tests, 0 failures, 1 pre-existing @Ignore.

    4. Re-run gate one more time to confirm idempotent exit 0:
    ```powershell
    powershell -ExecutionPolicy Bypass -File scripts/check_regression.ps1
    ```
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      powershell -ExecutionPolicy Bypass -File scripts/check_regression.ps1
</automated>
</verify>
  <acceptance_criteria>
    - `.planning/phases/04-adaptive-memory-model/04-BASELINE-AFTER.md` exists with required sections.
    - The "Streaming benchmarks" subsection reports FRESH JMH-measured scores for both new benchmarks.
    - The "In-memory benchmarks" subsection re-measures Phase 2/3 benchmarks and confirms each is within 50% of Phase 3 baseline.
    - The "## Nhận xét nhanh" section states: "Public API surface: NumJa.java = 61 (frozen), ArrayOps.java = 34 (frozen), Pandas.java = +1 method (read_csv_streaming), GaussianNB.java = +2 methods (partial_fit, finalize)".
    - Running `pwsh scripts/check_regression.ps1` from repo root exits 0.
    - Same script run a second time also exits 0 (idempotent).
</acceptance_criteria>
  <done>
    04-BASELINE-AFTER.md committed with FRESH JMH numbers for the 2 streaming benchmarks and Phase 2/3 carry-forward (all within 50% tolerance — gate green). Gate script self-consistency verified (exit 0, idempotent).
</done>
</task>

<task type="auto">
  <name>Task 04-04-04: Create 04-VERIFICATION.md with must-have coverage proof
  <files>.planning/phases/04-adaptive-memory-model/04-VERIFICATION.md
  <read_first>
    - .planning/phases/03-numerical-accuracy-hardening/03-VERIFICATION.md (template — table format + must-have coverage structure)
    - .planning/phases/02-cpu-parallel-core-threads/02-VERIFICATION.md (carry-forward pattern: 22/22 must-haves verified)
    - .planning/phases/04-adaptive-memory-model/04-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map)
    - .planning/phases/04-adaptive-memory-model/04-VALIDATION.md (per-task verification map for source file paths)
    - .planning/STATE.md (public API surface counters to cite)
</read_first>
  <action>
    Create `.planning/phases/04-adaptive-memory-model/04-VERIFICATION.md` with this structure:

    YAML frontmatter:
    - phase: 04-adaptive-memory-model
    - status: verified-GO
    - verified_at: 2026-08-28
    - verifier: claude-sonnet-4.5
    - total_must_haves: 22
    - verified_must_haves: 22
    - percent: 100
    - overrides: []

    Body sections:

    1. "## Summary" — Phase 4 ships stdlib-only chunked CSV reader + running-aggregator groupby + GaussianNB.partial_fit + PandasPipeline builder. All 22 must-haves verified; full suite green; BENCH-03 regression gate still green against 04-baseline.json.

    2. "## Must-Have Coverage" — table with columns `ID | Truth | Evidence | Source | Status`. At least 22 rows covering:
       - MEM-01 (3): read_csv signature byte-identical; small file single chunk; chunked shape matches in-memory
       - MEM-02 (4): read_csv_streaming path normalization; chunkRows validation; try-with-resources release; -Xmx128m large file
       - MEM-03 (5): aggregator cross-path equivalence for 6 aggs + multi-chunk merge + NaN-skip + partial_fit 2-calls == fit + idempotent
       - USE-02 (4): 4-line caller works; missing load IAE; missing estimator IAE; predict after run
       - BENCH-03 (4): gate script path; exits non-zero on regression; baseline.json extends 03; 2 new JMH benchmarks
       - API surface (3): Pandas +1 method, NumJa = 61, ArrayOps = 34

       Every row Status = PASS.

    3. "## Test Suite Status" — table with columns `Module | Pre-Phase-4 tests | Phase 4 added | Total | Pass | Fail | @Ignore`. Use actual numbers from `mvn test` run (NOT placeholder zeros). Pre-Phase-4 was 55 tests across all modules (per STATE.md). Phase 4 added: 6 ChunkedReadOptions + 7 CsvChunkReader + 8 RunningGroupAggregator + 5 PandasStreaming + 5 GaussianNBPartialFit + 4 PandasPipeline = 35 new tests.

    4. "## Public API Surface" — table with columns `File | Phase 3 count | Phase 4 count | Delta`. NumJa.java = 61 unchanged; ArrayOps.java = 34 unchanged; Pandas.java pre-Phase-4 + 1; GaussianNB.java pre-Phase-4 + 2; Pipeline.java unchanged.

    5. "## STRIDE Threats Addressed" — table with columns `ID | Component | Mitigation | Test`. List T401 (path normalize), T402 (initialized flag), T403 (AutoCloseable), T405 (chunkRows cap), T406 (CSV formula NaN-sub, documented but deferred).

    6. "## Conclusion" — state "Phase 4: GO." + summary: 22/22 must-haves, 35 new tests, gate still green, additive-only API surface, stdlib-only.
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn test
</automated>
</verify>
  <acceptance_criteria>
    - `.planning/phases/04-adaptive-memory-model/04-VERIFICATION.md` exists with YAML frontmatter.
    - The file contains the Must-Have Coverage table with >= 22 rows, all Status = PASS.
    - The "Test Suite Status" table reports actual counts from the `mvn test` run.
    - The "Public API Surface" table shows NumJa.java = 61, ArrayOps.java = 34, Pandas.java +1, GaussianNB.java +2.
    - The "STRIDE Threats Addressed" table lists at least T401, T402, T403, T405.
    - The Conclusion section states "Phase 4: GO" (or equivalent GO verdict).
</acceptance_criteria>
  <done>
    Phase 4 verification document committed: 22/22 must-haves verified, test suite green, public API surface change documented, STRIDE threats addressed. Phase 4 is officially GO for ship.
</done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → benchmark input data | `dist/datasets/*.csv` files are read by the JMH benchmarks. Trust boundary same as Wave-1/2/3 — path normalization + readable check + AutoCloseable. |
| Bench output JSON → gate script | The `04-baseline.json` is a static file read by the gate script. Tampering with the JSON could mask regressions, but the file is committed to git (auditable history) and the gate script is also committed (auditable logic). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-401 | Tampering (baseline JSON drift) | `04-baseline.json` | mitigate | File committed to git (auditable history); `scripts/check_regression.ps1` `$BaselinePath` line is grep-verifiable; any silent change to `score_ms` shows up in `git log -p`. |
| T-04-04-402 | Denial of Service (gate script infinite loop) | `scripts/check_regression.ps1` | mitigate | Same as Phase 3 — `mvn package` + `java -jar` invocations have explicit timeout via JMH `-r 1s` + `-wi 3 -i 3` config. Script has `$ErrorActionPreference = 'Stop'`. |
| T-04-04-403 | Information Disclosure (BENCH output leaks data) | JMH CSV output | accept | `target/jmh-phase3.csv` is a build artifact, not committed. Numbers (score_ms) are aggregate performance metrics, not sensitive data. |
| T-04-04-SC | Tampering | slopcheck | mitigate | Zero new Maven deps; pom.xml files unchanged; verified by `git diff --stat` against Wave-3 baseline. The gate script + bench + baseline JSON + AFTER.md + VERIFICATION.md are exactly the 5 files in `files_modified`. |
| T-04-04-404 | Repudiation (benchmark not actually run) | `04-BASELINE-AFTER.md` claimed numbers | mitigate | The `<automated>` verify command for Task 04-04-03 is `pwsh scripts/check_regression.ps1` which actually invokes `mvn package` + `java -jar benchmarks.jar` — if the script exits 0, the benchmarks DID run. |

**disposition_policy:** block on high — any unmitigated HIGH severity threat aborts the plan. Currently all threats are mitigated or accepted; no blockers.
</threat_model>

<verification>
- All 4 tasks complete and verified
- `mvn test` from repo root reports 0 failures
- `mvn -pl bench -am package -DskipTests` succeeds
- `pwsh scripts/check_regression.ps1` exits 0
- `grep -c "public static" Pandas.java` returns pre-Plan-04-02 count + 1
- `grep -c "public static" GaussianNB.java` returns pre-Plan-04-03 count + 2
- `grep -c "public static" NumJa.java` = 61 (frozen)
- `grep -c "public static" ArrayOps.java` = 34 (frozen)
- `git diff --stat` shows exactly 5 files modified
- 0 NEW Maven dependencies (pom.xml files unchanged)
</verification>

<success_criteria>
1. `04-baseline.json` committed with 10 perf_benchmarks (8 Phase 2 + 2 Phase 4) + 3 accuracy_checks (Phase 3 verbatim), all with real measured `score_ms`.
2. `scripts/check_regression.ps1` updated to point at `04-baseline.json`; diff logic unchanged; script exits 0.
3. `04-BASELINE-AFTER.md` committed with FRESH JMH numbers for streaming benchmarks + Phase 2/3 carry-forward confirmation.
4. `04-VERIFICATION.md` documents 22/22 must-haves verified with PASS status.
5. `mvn test` from repo root reports 0 failures across all modules.
6. Public API surface: NumJa.java = 61, ArrayOps.java = 34, Pandas.java = pre-Phase-4 + 1, GaussianNB.java = pre-Phase-4 + 2.
7. Zero new Maven dependencies added.
8. STRIDE threats T401/T402/T403/T405/T406 all addressed with mitigation in code + test verification.
9. Phase 3 BENCH-03 regression gate STILL GREEN against Phase 4 baseline.
</success_criteria>

<output>
Create `.planning/phases/04-adaptive-memory-model/04-04-SUMMARY.md` when done
</output>
