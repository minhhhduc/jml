---
phase: 04-adaptive-memory-model
plan: 03
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java
  - modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java
  - modules/sklearn/src/test/java/sklearn/naive_bayes/GaussianNBPartialFitTest.java
  - modules/sklearn/src/test/java/sklearn/pipeline/PandasPipelineTest.java
autonomous: true
requirements: [MEM-03, USE-02]
user_setup: []
must_haves:
  truths:
    - "GaussianNB.partial_fit(X, y) is callable on a fresh instance (first-call == fit semantics) AND on a previously-partial_fit'd instance (continue from existing accumulators) — verified by `twoPartialFits_equalOneFit` within `1e-9` per-class tolerance"
    - "GaussianNB.finalize() divides the running accumulators into means/variances and sets `fitted=true`; calling predict() before finalize throws `IllegalStateException(\"Not fitted.\")` (existing `check()` behavior preserved)"
    - "Existing GaussianNB.fit(NDArray X, int[] y) signature is byte-identical: `git diff` of the existing fit method body returns empty (T-04-03 API-2)"
    - "PandasPipeline.run() consumes the chunked CSV iterator from `Pandas.read_csv_streaming`, calls `partial_fit` on each chunk, calls `finalize()` once at the end, and returns the fitted estimator — verified by `PandasPipelineTest.tenLineCallerPattern` exercising a 4-line caller block (USE-02 success criterion)"
    - "PandasPipeline.run() throws `IllegalArgumentException` with descriptive message when `load()` was not called OR when `partialFit()` was not called"
    - "PandasPipeline.run() closes the CsvChunkReader via try-with-resources — file handle released even on partial_fit failure"
  artifacts:
    - path: "modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java"
      provides: "+2 public methods `partial_fit(NDArray, int[])` + `finalize()` + private `init(X, y)` refactor; existing `fit(NDArray, int[])` refactored to call init+accumulate+divide"
      contains: "public GaussianNB partial_fit"
    - path: "modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java"
      provides: "Fluent builder: load(Path).chunk(int).partialFit(GaussianNB).run() — uses try-with-resources on CsvChunkReader"
      contains: "public final class PandasPipeline"
    - path: "modules/sklearn/src/test/java/sklearn/naive_bayes/GaussianNBPartialFitTest.java"
      provides: "Cross-path equivalence tests: twoPartialFits_equalOneFit, singlePartialFit_equalsFit, init_resetsState, init_idempotentAfterFirstCall"
      contains: "twoPartialFits_equalOneFit"
    - path: "modules/sklearn/src/test/java/sklearn/pipeline/PandasPipelineTest.java"
      provides: "Integration tests: tenLineCallerPattern, missingPath_throwsIAE, modelPredictsAfterRun"
      contains: "tenLineCallerPattern"
  key_links:
    - from: "modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java"
      to: "modules/pandas/src/main/java/pandas/internal/CsvChunkReader.java"
      via: "new CsvChunkReader(path, opts) inside try-with-resources in run()"
      pattern: "new CsvChunkReader"
    - from: "modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java"
      to: "modules/pandas/src/main/java/pandas/Pandas.java"
      via: "Could use Pandas.read_csv_streaming OR construct CsvChunkReader directly; the plan uses direct construction to keep the dependency one-directional"
      pattern: "ChunkedReadOptions"
    - from: "modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java"
      to: "modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java"
      via: "partial_fit calls init(X,y) on first invocation, accumulate(X,y) thereafter; finalize() divides and sets fitted=true"
      pattern: "initialized"
---

<objective>
Wave-3 end-to-end pipeline: refactor `GaussianNB.fit` to extract `init/accumulate/divide` private methods, then ADD `partial_fit(NDArray, int[])` and `finalize()` as +2 public methods (existing `fit` byte-identical at signature level — only the body calls the extracted helpers, but the per-cell FP math is identical). Build `sklearn.pipeline.PandasPipeline` as a fluent builder that consumes `Pandas.read_csv_streaming` (Wave-2) and feeds each chunk to `partial_fit`, then `finalize()`. Closes MEM-03 (model with partial_fit semantics) and USE-02 (4-line caller pattern).

TDD core: `partial_fit` — write `twoPartialFits_equalOneFit` cross-path equivalence test FIRST (RED), implement `partial_fit` (GREEN), confirm `finalize()` correctly flips `fitted=true` and divides (REFACTOR). `PandasPipeline` is glue wired AFTER partial_fit tests are green.

Purpose: this plan completes the Phase 4 loop — caller can now write a 4-line `load → chunk → partialFit → run` builder that streams a large CSV through GaussianNB without loading the full file in memory.

Output: 1 MODIFIED source file (`GaussianNB.java`, additive on signature), 1 NEW source file (`PandasPipeline.java`), 2 NEW test classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-adaptive-memory-model/04-01-PLAN.md
@.planning/phases/04-adaptive-memory-model/04-01-SUMMARY.md
@.planning/phases/04-adaptive-memory-model/04-02-PLAN.md
@.planning/phases/04-adaptive-memory-model/04-02-SUMMARY.md
@.planning/phases/04-adaptive-memory-model/04-PATTERNS.md
@.planning/phases/04-adaptive-memory-model/04-RESEARCH.md
@modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java
@modules/sklearn/src/main/java/sklearn/pipeline/Pipeline.java
@modules/pandas/src/main/java/pandas/Pandas.java
@modules/pandas/src/main/java/pandas/internal/CsvChunkReader.java (from Wave 1)
@modules/pandas/src/main/java/pandas/internal/ChunkedReadOptions.java (from Wave 1)
@modules/sklearn/src/test/java/sklearn/TestSklearnJUnit.java
</context>

<interfaces>
From `modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java`:
- Existing `public GaussianNB fit(NDArray X, int[] y)` lines 18-47 — body uses `means`, `variances`, `classPriors`, `counts` arrays; `n_classes` derived from max label + 1; `fitted` boolean at line 14 set true at end. The refactor extracts: `private void init(NDArray X, int[] y)` (allocates means/variances/classPriors/counts sized to y cardinality, sets classPriors), `private void accumulate(NDArray X, int[] y)` (adds data[i][j] to means[y[i]][j] AND diff*diff to variances[y[i]][j] where diff is computed against the CURRENT means — same as fit lines 32-41), `private void divide()` (means /= counts, variances = variances/counts + 1e-9, sets fitted=true). The refactored `fit` becomes `init(X, y); accumulate(X, y); divide(); return this;`.
- `predict(NDArray X)` line 49-54 calls `check()` which throws `IllegalStateException("Not fitted.")` if `!fitted` — this behavior MUST be preserved. After `partial_fit` (which does NOT call `divide()`), `fitted` stays false; `predict` throws ISE until `finalize()` is called.
- `extract2D(NDArray arr)` line 91-95 — private helper used by both fit and predict.

From `modules/pandas/src/main/java/pandas/Pandas.java`:
- `read_csv_streaming(String, ChunkedReadOptions)` (Wave-2) returns `Iterator<DataFrame>` after path.normalize() + Files.isReadable() check. `PandasPipeline.run()` uses this directly.

From `modules/pandas/src/main/java/pandas/internal/CsvChunkReader.java` (Wave 1):
- `implements Iterator<DataFrame>, AutoCloseable` — `PandasPipeline.run()` MUST wrap in try-with-resources per STRIDE-T403 (file handle leak on early break).

From `modules/sklearn/src/main/java/sklearn/pipeline/Pipeline.java`:
- Package `sklearn.pipeline` and reflective style — `PandasPipeline` is in the same package but uses a typed fluent builder, not reflection. `Pipeline.java` is unchanged.

From `modules/sklearn/src/test/java/sklearn/TestSklearnJUnit.java`:
- JUnit 4 style template for `GaussianNBPartialFitTest` + `PandasPipelineTest`. Uses `org.junit.Test`, `org.junit.Before`, `static org.junit.Assert.*`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-03-01: GaussianNB.partial_fit + finalize with cross-path equivalence tests (TDD</name>
  <files>
    - modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java
    - modules/sklearn/src/test/java/sklearn/naive_bayes/GaussianNBPartialFitTest.java
  <read_first>
    - modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java (the existing fit body to extract init/accumulate/divide from)
    - .planning/phases/04-adaptive-memory-model/04-PATTERNS.md §GaussianNB additive partial_fit (the cross-path equivalence invariant + WR-05 defensive init)
    - .planning/phases/04-adaptive-memory-model/04-RESEARCH.md §Pitfall 3 (partial_fit first-call == fit semantics mismatch — the exact bug the TDD cycle must catch)
    - modules/pandas/src/test/java/sklearn/TestSklearnJUnit.java (JUnit 4 style template)
</read_first>
  <behavior>
    - Test 1 (`twoPartialFits_equalOneFit`): build X_full (n=1000, d=4) with 3 classes; build X1 = rows 0-499, y1 = first 500 labels; X2 = rows 500-999, y2 = remaining 500 labels. Fit `GaussianNB a` via two `partial_fit` calls + `finalize()`. Fit `GaussianNB b` via single `fit(X_full, y_full)`. Assert `Arrays.equals(a.predict(X_full), b.predict(X_full))` (categorical predictions bit-identical). Also assert `a.score(X_full, y_full) == b.score(X_full, y_full)` within 1e-9.
    - Test 2 (`singlePartialFit_equalsFit`): same data; `a.partial_fit(X_full, y_full).finalize()` vs `b.fit(X_full, y_full)`; categorical predictions identical. Proves first-call partial_fit == fit semantics.
    - Test 3 (`init_resetsState`): create `a`, partial_fit(X1, y1), partial_fit(X2, y2); create `b` (fresh), b.partial_fit(X1, y1), b.partial_fit(X2, y2); assert a and b predict identically (proves internal accumulators are NOT corrupted across calls — running-form invariant).
    - Test 4 (`init_idempotentAfterFirstCall`): call `partial_fit(X, y)` 3 times in a row on the same instance with the SAME data; assert the 2nd and 3rd calls don't reset state (i.e., a.predict after call 2 == a.predict after call 3). Equivalent: after `a.partial_fit(X,y).partial_fit(X,y).partial_fit(X,y).finalize()`, the means array is 3x the per-class sum divided by 3x count — equivalent to single fit (the assertion is via comparing to a fresh `b.fit(X,y)` whose predict outputs match).
    - Test 5 (`predictBeforeFinalize_throws`): `a.partial_fit(X, y)` (no finalize); assert `a.predict(X)` throws `IllegalStateException` with message containing "Not fitted" (the existing `check()` behavior preserved).
</behavior>
  <action>
    **RED first (write failing tests):**

    Create `modules/sklearn/src/test/java/sklearn/naive_bayes/GaussianNBPartialFitTest.java` with 5 `@Test` methods per the behavior block. Use a fixed-seed `Random` to build X_full deterministically (so the test is reproducible). Use NDArray via `new NDArray(double[][], int[] shape)` — check the actual constructor in `numja.core.NDArray.java` first (the test must compile). If NDArray ctor signature differs, adapt: the test class is the place to fix the API mismatch, NOT the production class. Run `mvn -pl modules/sklearn -am test -Dtest=GaussianNBPartialFitTest` — confirm RED (compilation error since `partial_fit` doesn't exist).

    **GREEN (refactor + implement):**

    Modify `modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java`:

    1. Add ONE field at the top of the class (after `fitted`):
       ```java
       private boolean initialized = false;  // WR-05 defensive init — false on construction
       ```

    2. Refactor existing `fit(NDArray, int[])` body (lines 18-47) by extracting three PRIVATE methods (do NOT change `fit`'s signature or its observable behavior):
       - `private void init(NDArray X, int[] y)` — body = existing lines 19-30 (extract data, compute n_classes, allocate means/variances/classPriors/counts, compute classPriors from counts); set `initialized = true;` at end.
       - `private void accumulate(NDArray X, int[] y)` — body = existing lines 32-41 (the per-class running sum for means and per-class running sumSq for variances — note: in the existing fit body, `means[c][j] /= counts[c]` happens BEFORE the variance loop at line 35, so `accumulate` MUST be called AFTER `init` (which sets classPriors from counts) but the variance loop in `accumulate` uses the ALREADY-divided means — extract the existing two-pass ordering exactly).
       - `private void divide()` — body = existing lines 42-43 (means /= counts, variances = variances/counts + 1e-9 — note: in the existing fit, means are already divided by line 35 BEFORE the variance loop, so `divide()` only divides variances — re-read the existing fit body and replicate the exact two-pass ordering; if the refactor breaks the math, restore from git); set `fitted = true;` at end.
       - Refactored `fit` becomes: `init(X, y); accumulate(X, y); divide(); return this;` (3 lines + braces — lazy).

    3. Add TWO new public methods:
       ```java
       public GaussianNB partial_fit(NDArray X, int[] y) {
           if (!initialized) init(X, y);
           else accumulate(X, y);
           return this;  // fitted stays false until finalize()
       }

       public GaussianNB finalize() {
           divide();
           return this;
       }
       ```

    4. CONSTRAINT: the existing `fit(NDArray X, int[] y)` signature MUST be byte-identical (public method, signature `GaussianNB fit(NDArray, int[])`, throws nothing extra). `git diff modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java` shows ONLY the new `initialized` field + the refactored 3-line `fit` body + 2 new methods. The per-cell FP math is unchanged — verified by `singlePartialFit_equalsFit` (which compares `partial_fit + finalize` to `fit`).

    5. ASVS L1 + STRIDE javadoc on `partial_fit` and `finalize`: trust boundary = caller-controlled NDArray (no I/O); STRIDE-T402 (stale state across calls) mitigated by `initialized` flag.

    Run `mvn -pl modules/sklearn -am test -Dtest=GaussianNBPartialFitTest` — confirm GREEN (5 tests pass).

    **REFACTOR (if needed):**

    If `divide()`'s split between `init/accumulate/divide` causes a 1-cell FP rounding difference vs the existing fit (very unlikely since the math is unchanged, but test `singlePartialFit_equalsFit` will catch it), revert `divide()` to do the variance division ONLY and let `accumulate()` keep using already-divided means (matching existing fit lines 32-41 ordering).

    After 5/5 green, run the full sklearn suite: `mvn -pl modules/sklearn -am test` — must report 0 failures.
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/sklearn -am test "-Dtest=GaussianNBPartialFitTest" "-Dsurefire.failIfNoSpecifiedTests=false"
 </automated>
</verify>
  <acceptance_criteria>
    - `modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java` contains the new method `public GaussianNB partial_fit(NDArray X, int[] y)` and `public GaussianNB finalize()`.
    - The file contains a new field `private boolean initialized = false;` (grep verification: `grep -c "private boolean initialized" modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java` returns 1).
    - Existing `fit(NDArray X, int[] y)` signature is byte-identical (public, returns GaussianNB, throws nothing extra) — `git diff` of just that method's signature line shows no change.
    - `modules/sklearn/src/test/java/sklearn/naive_bayes/GaussianNBPartialFitTest.java` exists with 5 `@Test` methods (twoPartialFits_equalOneFit, singlePartialFit_equalsFit, init_resetsState, init_idempotentAfterFirstCall, predictBeforeFinalize_throws).
    - Running `mvn -pl modules/sklearn -am test -Dtest=GaussianNBPartialFitTest` reports 5 tests passed, 0 failed.
    - Running `mvn -pl modules/sklearn -am test` (full sklearn suite) reports 0 failures (pre-existing tests still green; partial_fit is additive).
    - `twoPartialFits_equalOneFit` asserts `Arrays.equals(a.predict(X_full), b.predict(X_full))` is true (categorical predictions bit-identical between partial-fit-then-finalize and one-shot fit).
    - `predictBeforeFinalize_throws` asserts the exception message contains `"Not fitted"` (preserves existing `check()` behavior).
</acceptance_criteria>
  <done>
    TDD core complete: `GaussianNB.partial_fit` proven equivalent to one-shot `fit` on the same data (within 1 ULP categorical prediction match); `finalize()` correctly flips `fitted=true` and divides running accumulators; `predict` before `finalize` throws ISE per existing check(). 5 tests pass; full sklearn suite still green; existing `fit` signature byte-identical.
</done>
</task>

<task type="auto">
  <name>Task 04-03-02: PandasPipeline builder + integration tests (USE-02</name>
  <files>
    - modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java
    - modules/sklearn/src/test/java/sklearn/pipeline/PandasPipelineTest.java
  <read_first>
    - modules/sklearn/src/main/java/sklearn/pipeline/Pipeline.java (same package conventions + javadoc preamble style to mirror)
    - modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java (the partial_fit signature Wave-3 just added — the builder calls it)
    - modules/pandas/src/main/java/pandas/internal/CsvChunkReader.java (the AutoCloseable iterator the builder wraps in try-with-resources)
    - modules/pandas/src/main/java/pandas/internal/ChunkedReadOptions.java (the options POJO the builder wires)
    - modules/pandas/src/main/java/pandas/Pandas.java (read_csv_streaming alternative — the plan uses direct CsvChunkReader construction to keep dependency one-directional)
    - .planning/phases/04-adaptive-memory-model/04-PATTERNS.md §PandasPipeline (the 4-line caller pattern + try-with-resources + Pipeline.java style differences)
    - .planning/phases/04-adaptive-memory-model/04-RESEARCH.md §Pitfall 2 (file handle leak on early break — mitigated by try-with-resources in run())
    - modules/sklearn/src/test/java/sklearn/TestSklearnJUnit.java (JUnit 4 integration test style)
</read_first>
  <action>
    **1. Create `modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java`** — `public final class` in package `sklearn.pipeline` with fluent builder + one-shot `run()`:

    - Private fields: `private Path path;`, `private ChunkedReadOptions opts = new ChunkedReadOptions();` (default chunkRows applied in `chunk(int)` setter), `private GaussianNB estimator;` (single typed estimator, NOT a `List<Object>` like reflective `Pipeline`).

    - Fluent setters returning `this`:
      - `public PandasPipeline load(Path p) { this.path = p; return this; }`
      - `public PandasPipeline chunk(int rows) { this.opts.chunkRows = rows; return this; }` — overwrites the default `0` in `ChunkedReadOptions` (validation happens in CsvChunkReader ctor).
      - `public PandasPipeline partialFit(GaussianNB e) { this.estimator = e; return this; }` — typed to `GaussianNB` for now (the only model with `partial_fit`); can be generalized in v2 if other models add `partial_fit`.

    - `run()` method:
      - Validate `path != null` else `throw new IllegalArgumentException("load(path) required.")`
      - Validate `estimator != null` else `throw new IllegalArgumentException("partialFit(estimator) required.")`
      - Determine column names: for the integration test, the CSV has a label column as the LAST column (after feature columns). Use a hardcoded convention `String labelColumn = guessLabelColumn(path)` — actually simpler: the test passes the label column name explicitly via a new setter `labelColumn(String)`. Add: `private String labelColumn = null;`, `public PandasPipeline labelColumn(String name) { this.labelColumn = name; return this; }`.
      - `try (CsvChunkReader reader = new CsvChunkReader(path, opts))`: while `reader.hasNext()`: get chunk DataFrame, extract feature columns (all except `labelColumn`), extract labels from `labelColumn` Series as `int[] y`, build NDArray X from feature columns, call `estimator.partial_fit(X, y)`. Wrap `IOException` from reader.next() into a RuntimeException with file path + chunk number.
      - After loop: `estimator.finalize();`
      - Return `estimator` (the typed GaussianNB).

    - `# ponytail: hardcoded GaussianNB typing — generalize to `PartialFitCapable` interface when a 2nd model gains partial_fit`
    - `# ponytail: chunkRows default 0 = validate-at-open — lazy, no separate default constant to maintain`
    - Javadoc preamble per ASVS L1: trust boundary = filesystem (caller path) → estimator (in-process GaussianNB); STRIDE-T401 (path) mitigated by reader canonicalization, T403 (handle leak) mitigated by try-with-resources.

    **2. Create `modules/sklearn/src/test/java/sklearn/pipeline/PandasPipelineTest.java`** — 3+ integration tests:

    - `tenLineCallerPattern` — write a 30-row temp CSV with header `f1,f2,label` and 30 rows of `Random.nextGaussian()` data + binary label; then run the 4-line caller:
      ```java
      GaussianNB model = new PandasPipeline()
          .load(Paths.get(tempPath.toString()))
          .chunk(10)
          .partialFit(new GaussianNB())
          .labelColumn("label")
          .run();
      ```
      Verify: `model != null`, calling `model.predict(X_test)` does NOT throw `IllegalStateException("Not fitted.")` (proves `finalize()` was called), accuracy > 0.5 on the same data (sanity).

    - `missingPath_throwsIAE` — `new PandasPipeline().partialFit(new GaussianNB()).labelColumn("label").run()` (no `.load(...)`) — assert `IllegalArgumentException` with message containing `"load(path) required."`.

    - `missingEstimator_throwsIAE` — `new PandasPipeline().load(tempPath).labelColumn("label").run()` (no `.partialFit(...)`) — assert `IllegalArgumentException` with message containing `"partialFit(estimator) required."`.

    - `modelPredictsAfterRun` — run `tenLineCallerPattern` with a 100-row dataset, 2 features, 2 classes; assert `model.score(X_train, y_train) > 0.6` (sanity check the streaming fit produces a non-trivial model).

    Use `@Before`/`@After` for temp file lifecycle (create `tempPath = Files.createTempFile("phase4_pipe_", ".csv")` in setup, delete in teardown).

    Run `mvn -pl modules/sklearn -am test -Dtest=PandasPipelineTest` — confirm 4+ tests pass. Then run full sklearn suite `mvn -pl modules/sklearn -am test` — must report 0 failures.
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/sklearn -am test "-Dtest=PandasPipelineTest" "-Dsurefire.failIfNoSpecifiedTests=false"
 </automated>
</verify>
  <acceptance_criteria>
    - `modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java` exists, contains `public final class PandasPipeline`, fluent setters `load(Path)`, `chunk(int)`, `partialFit(GaussianNB)`, `labelColumn(String)`, and `public GaussianNB run() throws IOException`.
    - The `run()` body wraps `new CsvChunkReader(path, opts)` in try-with-resources (grep verification: `grep -c "try (CsvChunkReader" modules/sklearn/src/main/java/sklearn/pipeline/PandasPipeline.java` returns 1).
    - The `run()` body calls `estimator.partial_fit(X, y)` inside the loop AND `estimator.finalize()` after the loop (grep verification: 2 lines matching `partial_fit\|finalize` excluding comments).
    - The `run()` body throws `IllegalArgumentException` for missing `path` AND for missing `estimator` (2 distinct `throw new IllegalArgumentException` lines).
    - `modules/sklearn/src/test/java/sklearn/pipeline/PandasPipelineTest.java` exists with at least 4 `@Test` methods (tenLineCallerPattern, missingPath_throwsIAE, missingEstimator_throwsIAE, modelPredictsAfterRun).
    - Running `mvn -pl modules/sklearn -am test -Dtest=PandasPipelineTest` reports 4+ tests passed, 0 failed.
    - Running `mvn -pl modules/sklearn -am test` (full sklearn suite) reports 0 failures (GaussianNBPartialFitTest 5 + PandasPipelineTest 4 + pre-existing tests).
    - The `tenLineCallerPattern` test executes a 4-line `new PandasPipeline().load(...).chunk(...).partialFit(...).labelColumn(...).run()` chain (count the method calls — must be ≤ 6 to satisfy USE-02 "<10 lines" criterion).
</acceptance_criteria>
  <done>
    Wave-3 complete: GaussianNB.partial_fit + finalize proven bit-equivalent to one-shot fit (TDD green); PandasPipeline builder implements the 4-line USE-02 caller pattern with try-with-resources for file handle hygiene; integration tests confirm end-to-end streaming fit + predict + accuracy sanity. Public API surface of `GaussianNB.java` grows by exactly +2 methods (partial_fit + finalize) + +1 private field (initialized); `Pipeline.java` UNCHANGED.
</done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → estimator (GaussianNB) | Caller-controlled `Path` is consumed by `PandasPipeline.run()`; chunks converted to NDArray + int[] labels, fed to `estimator.partial_fit`. |
| Caller code → GaussianNB partial_fit state | Caller may invoke `partial_fit` multiple times; the `initialized` flag prevents stale-state corruption (STRIDE-T402). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-402 | Tampering (stale state across partial_fit calls) | `GaussianNB.partial_fit` | mitigate | `private boolean initialized = false;` field; first call invokes `init(X, y)` (allocates accumulators), subsequent calls invoke `accumulate(X, y)`. Verified by `init_idempotentAfterFirstCall` test (same data 3x produces same predictions as single fit). |
| T-04-03-401 | Elevation of Privilege | `PandasPipeline.load(Path)` | transfer | Path normalization happens in `Pandas.read_csv_streaming` (Wave-2) or directly in `CsvChunkReader` ctor's `Files.newBufferedReader` (the reader itself does not normalize — caller-side responsibility); documented in `PandasPipeline` javadoc that the builder passes path through to `CsvChunkReader` unchanged (canonicalization done by `read_csv_streaming` if caller uses that path; the builder uses direct construction to keep dependency one-directional per the plan). |
| T-04-03-403 | Denial of Service (file handle leak) | `PandasPipeline.run()` | mitigate | `try-with-resources` on `CsvChunkReader`; verified by integration test (the 30-row CSV is re-openable in subsequent test runs — handle released). |
| T-04-03-406 | Tampering (cross-path drift) | `partial_fit + finalize` vs `fit` | mitigate | Cross-path equivalence tests pin 2-partial-fit + finalize to 1-shot fit (`twoPartialFits_equalOneFit`, `singlePartialFit_equalsFit`). Categorical predictions bit-identical (Arrays.equals). |
| T-04-03-SC | Tampering | slopcheck | mitigate | Zero new Maven deps; pom.xml files unchanged; verified by `git diff --stat` against Wave-2 baseline. |
| T-04-03-407 | Information Disclosure | `IllegalStateException` on unfitted predict | accept | Preserves existing `check()` behavior; no PII, low-value target; standard Java exception contract. |

**disposition_policy:** block on high — any unmitigated HIGH severity threat aborts the plan. Currently all threats are mitigated, transferred, or accepted; no blockers.
</threat_model>

<verification>
- All 5 `GaussianNBPartialFitTest` tests pass via `mvn -pl modules/sklearn -am test -Dtest=GaussianNBPartialFitTest`
- All 4 `PandasPipelineTest` tests pass via `mvn -pl modules/sklearn -am test -Dtest=PandasPipelineTest`
- Full sklearn module suite still green: `mvn -pl modules/sklearn -am test`
- `git diff modules/sklearn/src/main/java/sklearn/naive_bayes/GaussianNB.java` shows ONLY the new `initialized` field + refactored 3-line `fit` body + 2 new methods
- Existing `Pipeline.java` UNCHANGED (`git diff modules/sklearn/src/main/java/sklearn/pipeline/Pipeline.java` returns empty)
- Public API surface (`NumJa.java` = 61, `ArrayOps.java` = 34, `Pandas.java` = +1 method from Wave-2) unchanged in this plan
</verification>

<success_criteria>
1. `GaussianNB.partial_fit(NDArray, int[])` is callable on fresh and pre-fit instances; categorical predictions match one-shot `fit` on the same data (within 0 mismatches for n=1000).
2. `GaussianNB.finalize()` divides running accumulators into means/variances and sets `fitted=true`; `predict` after `finalize` does NOT throw ISE.
3. `GaussianNB.fit(NDArray, int[])` signature is byte-identical to pre-Plan-04-03 state.
4. `PandasPipeline.run()` works end-to-end: loads CSV → chunks → calls `partial_fit` per chunk → calls `finalize` → returns fitted GaussianNB that can `predict` without ISE.
5. `PandasPipeline.run()` throws `IllegalArgumentException` with descriptive message when `load()` or `partialFit()` is missing.
6. The full USE-02 caller chain `new PandasPipeline().load(...).chunk(...).partialFit(...).labelColumn(...).run()` compiles and runs in ≤ 6 method calls (satisfies "<10 lines").
7. Public API surface: `NumJa.java` (61) and `ArrayOps.java` (34) untouched; `Pipeline.java` untouched; `Pandas.java` +1 method (Wave-2); `GaussianNB.java` +2 methods (partial_fit + finalize).
8. No new Maven dependencies added.
9. ASVS L1 + STRIDE block present in javadoc of `PandasPipeline` + `partial_fit` + `finalize`.
</success_criteria>

<output>
Create `.planning/phases/04-adaptive-memory-model/04-03-SUMMARY.md` when done
</output>
