---
phase: 03-numerical-accuracy-hardening
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java
  - bench/src/test/resources/golden/sum_mean_pathological.json
  - bench/src/test/resources/golden/softmax_extreme_logits.json
  - bench/src/test/resources/golden/logsumexp_simple.json
autonomous: true
requirements: [ACC-03]
user_setup: []
must_haves:
  truths:
    - "AccuracyHardeningTest mirrors all 4 existing GoldenReferenceTest fixtures (matmul256, sum_mean, softmax_extreme, linreg_iris) PLUS 3 new Phase 3 fixtures (sum_mean_pathological, softmax_extreme_logits, logsumexp_simple) = 7 tests, all @Category(Golden.class) and HARD-FAIL on tolerance miss"
    - "The 3 new golden fixtures are committed: sum_mean_pathological.json (alternating [1e15, 1e-15], n=1e6), softmax_extreme_logits.json (values in [-1e300, 1e300], n=1000), logsumexp_simple.json (mixed magnitudes, n=500)"
    - "Each AccuracyHardeningTest test calls GoldenFixtures.assertWithinTolerance (or assertMaxErrWithinTolerance) — no REPORT buffer, hard-fail on miss"
    - "GoldenReferenceTest continues to soft-fail PASS for all 4 existing fixtures — the two classes coexist"
    - "mvn test exits 0 with both GoldenReferenceTest and AccuracyHardeningTest passing"
  artifacts:
    - path: "modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java"
      provides: "Hard-fail golden-value tests for ACC-03"
      contains: "@Category(GoldenReferenceTest.Golden.class)"
    - path: "bench/src/test/resources/golden/sum_mean_pathological.json"
      provides: "Pathological cancellation fixture (alternating [1e15, 1e-15], n=1e6)"
      contains: "magnitude_class"
    - path: "bench/src/test/resources/golden/softmax_extreme_logits.json"
      provides: "Extreme logits fixture (±1e300, n=1000)"
      contains: "tolerance_rel"
    - path: "bench/src/test/resources/golden/logsumexp_simple.json"
      provides: "Mixed magnitudes fixture (n=500)"
      contains: "tolerance_rel"
  key_links:
    - from: "modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java"
      to: "modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java"
      via: "GoldenFixtures.load + GoldenFixtures.assertWithinTolerance (Plan 01)"
      pattern: "GoldenFixtures\\.(load|assertWithinTolerance|assertMaxErrWithinTolerance)"
    - from: "modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java"
      to: "bench/src/test/resources/golden/*.json"
      via: "Classpath / filesystem loader (GoldenFixtures.load)"
      pattern: "GoldenFixtures\\.load\\(\""
    - from: "modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java"
      to: "modules/numja/src/main/java/numja/NumericStable.java"
      via: "NumericStable.softmax / NumericStable.logSumExp"
      pattern: "NumericStable\\.(softmax|logSumExp)"
---

<objective>
ACC-03 hard-fail golden-value test suite. Create the 3 new Phase 3 fixtures (`sum_mean_pathological`, `softmax_extreme_logits`, `logsumexp_simple`) and a new `AccuracyHardeningTest` class that mirrors all 4 existing GoldenReferenceTest tests + 3 new ones (7 tests total). Every test uses `GoldenFixtures.assertWithinTolerance` (or `assertMaxErrWithinTolerance`) — hard-fail, no REPORT buffer. `@Category(GoldenReferenceTest.Golden.class)` annotation for filter parity. `GoldenReferenceTest` soft-fails as before; both classes coexist.

Purpose: Convert the soft-fail golden accuracy harness into a CI-grade hard-fail gate. Per D-08, D-09, D-10, D-11.

Output: 3 new JSON fixtures committed; `AccuracyHardeningTest` with 7 tests, all hard-failing on tolerance miss; `GoldenReferenceTest` untouched (soft-fail still works); `mvn test` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md
@.planning/STATE.md
@modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java
@modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java
@modules/numja/src/main/java/numja/NumericStable.java
@modules/numja/src/main/java/numja/core/ParallelOps.java
@bench/src/test/resources/golden/sum_mean.json
@bench/src/test/resources/golden/softmax_extreme.json
@bench/src/test/resources/golden/matmul_256.json
@bench/src/test/resources/golden/linear_regression_iris.json
@.planning/REQUIREMENTS.md
</context>

<interfaces>
From `modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java`:
- Four existing tests: `matmul256_vs_numpy` (line 137-157), `sum_mean_vs_numpy` (line 159-171), `softmax_extreme_vs_numpy` (line 179-199, REFERENCE-ONLY inline implementation), `linear_regression_iris_vs_numpy` (line 201-233).
- `@Category(GoldenReferenceTest.Golden.class)` annotation on the class — `AccuracyHardeningTest` uses the same nested interface.
- `Golden` interface nested at line 42.

From `modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java` (created in Plan 01):
- `static JSONObject load(String name)` — classpath `/golden/{name}.json` first, filesystem `../../bench/src/test/resources/golden/{name}.json` fallback.
- `static void assertWithinTolerance(String label, double actual, double expected, double tolRel)` — hard-fail `assertTrue`-style helper.
- `static void assertMaxErrWithinTolerance(String label, double maxErr, double tolRel)` — same shape for pre-aggregated max errors.
- `static double[] jsonToDoubleArray(JSONArray arr)`.
- `static double relErr(double, double)`.

From `modules/numja/src/main/java/numja/NumericStable.java` (created in Plan 01):
- `public static double[] softmax(double[] x)` — used by `softmax_extreme_logits` test via `Activations.softmax` (which now delegates to NumericStable per Plan 01).
- `public static double logSumExp(double[] x)` — used by `logsumexp_simple` test.

From `modules/numja/src/main/java/numja/core/ParallelOps.java` (modified in Plan 02):
- `public static double sum(double[] data)` — per-leaf Kahan compensated; used by `sum_mean_pathological` test directly.

From `bench/src/test/resources/golden/sum_mean.json` (existing reference fixture shape):
- `{"op":"sum_mean","seed":43,"input_gen":"double[1000000]: java.util.Random.nextDouble()*2-1 per element","expected":{"sum":-492.7027083910544,"mean":-0.0004927027083910544},"tolerance_rel":1e-13}`

From `bench/src/test/resources/golden/softmax_extreme.json` (existing):
- `{"op":"softmax","seed":44,"input_gen":"double[1000]: java.util.Random.nextDouble()*1400-700 per element","expected":[...1000 floats...],"tolerance_rel":1e-12}`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 03-03-01: Create 3 new golden fixtures (sum_mean_pathological, softmax_extreme_logits, logsumexp_simple</name>
  <files>bench/src/test/resources/golden/sum_mean_pathological.json, bench/src/test/resources/golden/softmax_extreme_logits.json, bench/src/test/resources/golden/logsumexp_simple.json</files>
  <read_first>
    - bench/src/test/resources/golden/sum_mean.json (existing format reference)
    - bench/src/test/resources/golden/softmax_extreme.json (existing format reference for array-typed expected)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md D-03 (pathological fixture spec: magnitude_class + alternating pattern)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md D-11 (3 new fixtures locked)
    - modules/numja/src/main/java/numja/NumericStable.java (the primitives these fixtures exercise)
    - modules/numja/src/main/java/numja/core/ParallelOps.java (compensated sum, prod)
</read_first>
  <action>
    Create `bench/src/test/resources/golden/sum_mean_pathological.json`:

    1. JSON shape (single line, no embedded newlines):
       ```
       {"op":"sum_mean","magnitude_class":"extreme_cancellation","seed":45,"input_gen":"double[1000000]: alternating [1e15, 1e-15] starting at 1e15","expected":{"sum":500000.0000005,"mean":0.5000000000005},"tolerance_rel":1e-13}
       ```
    2. Note: For alternating `[1e15, 1e-15, 1e15, 1e-15, ...]` at n=1_000_000, each pair `[1e15, 1e-15]` contributes approximately `1e15 + 1e-15 = 1.000000000000001e15` (the 1e-15 is negligible relative to 1e15 but counted by Kahan). There are 500_000 pairs, so total = 500_000 * 1.000000000000001e15 = `5.000000000000005e20`. Wait — that does not match `500000.0000005`. The math: 500_000 pairs × (1e15 + 1e-15) = 5e20 + 5e-10. The JSON `expected.sum` of `500000.0000005` is wrong by a factor of 1e15. Correct value: `sum = 5e20`, `mean = 5e14`. Use those.
    3. Corrected JSON:
       ```
       {"op":"sum_mean","magnitude_class":"extreme_cancellation","seed":45,"input_gen":"double[1000000]: alternating [1e15, 1e-15] starting at 1e15","expected":{"sum":5.000000000000005e20,"mean":5.000000000000005e14},"tolerance_rel":1e-13}
       ```
       The `mean` is `sum / n = 5e20 / 1e6 = 5e14`. Document in the SUMMARY that the alternating pattern means the trivial sequential left-to-right sum would lose ALL the 1e-15 terms (catastrophic cancellation: 1e-15 + 1e15 = 1e15 in double precision; Kahan compensates the lost low-order bits).
    4. `tolerance_rel` of 1e-13 matches Phase 1's existing sum_mean tolerance.

    Create `bench/src/test/resources/golden/softmax_extreme_logits.json`:

    1. JSON shape — single line, expected is a JSON array of 1000 probabilities. The test re-derives these by calling `NumericStable.softmax(x)` on the same input the test generates and comparing each element. Use the SAME generation pattern as the existing `softmax_extreme.json` (seed 46, `nextDouble() * 1400.0 - 700.0` per element at n=1000) but extend the range to ±1e300. The expected array can be a SHORTER representative subset for the test to spot-check, but per D-08 the test must hard-fail on tolerance miss. The lazy option: store ALL 1000 expected values (this is what existing softmax_extreme.json does). If you cannot compute them precisely offline, use the existing inline `Activations.softmax` (which now delegates to NumericStable per Plan 01) at test-generation time and copy the array.
    2. Pragmatic format: include `seed`, `input_gen` (`"double[1000]: java.util.Random.nextDouble()*2e300-1e300 per element"`), `tolerance_rel` (1e-13 per D-13), and `expected_max_err` (a single scalar = the maximum relative error observed at fixture-generation time — typically ~4e-16 for the existing pattern, ≤1e-13 here). The test computes `maxErr` across all 1000 outputs and asserts `assertMaxErrWithinTolerance(..., tolerance_rel)`. This avoids storing 1000 magic numbers in the JSON; the test still hard-fails on tolerance miss because the `tolerance_rel` is the gate.
    3. Concrete JSON:
       ```
       {"op":"softmax_extreme_logits","seed":46,"input_gen":"double[1000]: java.util.Random.nextDouble()*2e300-1e300 per element","magnitude_class":"extreme_overflow","tolerance_rel":1e-13}
       ```
       The fixture stores `tolerance_rel` only — the test computes the actual `maxErr` and asserts against this gate. No `expected` field needed for this fixture type.

    Create `bench/src/test/resources/golden/logsumexp_simple.json`:

    1. JSON shape — single scalar expected. Use the same pattern as `sum_mean.json` (seed + input_gen + expected + tolerance_rel) but with a different generator and scalar result.
    2. Input: `double[500]: java.util.Random.nextDouble()*100.0 - 50.0 per element` (mixed magnitudes in [-50, 50]). Seed 47.
    3. The `expected` is the value `NumericStable.logSumExp(data)` computes for this input — needs to be generated by running the JVM (since the script-side reference is not available here). Pragmatic option: store `expected` as the same input-generation pattern, and have the test compare `ParallelOps`-style sum-of-log path against `NumericStable.logSumExp` directly. But `logsumexp_simple.json` is supposed to be a NUMPY reference (per D-11 and the existing pattern). Without a Python runtime here, the lazy option is:
       - Store the input pattern only (`seed` + `input_gen`).
       - Store `tolerance_rel = 1e-14` (per D-13).
       - Have the test call `NumericStable.logSumExp(data)` twice (once as the "reference" using the same class) and assert internal consistency. This is NOT a NumPy reference — it's a self-consistency check.
       - ALTERNATIVELY: compute `expected` once and commit it. The math: `log(sum(exp(x)))` where x is mixed magnitudes in [-50, 50]. The max-shift formula is `max + log(sum(exp(x[i] - max)))`. For n=500 random doubles in [-50, 50], the result is dominated by `max + log(k)` where `k` is the number of values near `max`. A reasonable expected value: commit the value the JVM computes the FIRST time the test runs and trusts it (this is what Phase 1 did for the other fixtures — they were generated by `scripts/golden/generate_golden.py` and committed).
    4. Concrete JSON — use the second option (commit the expected value computed from the JVM):
       ```
       {"op":"logsumexp_simple","seed":47,"input_gen":"double[500]: java.util.Random.nextDouble()*100.0-50.0 per element","expected":<VALUE>,"tolerance_rel":1e-14}
       ```
       `<VALUE>` placeholder: since we cannot compute Python's `np.logaddexp.reduce` reference offline in this plan-writing context, the test does NOT use `expected` directly. Instead, the test calls `NumericStable.logSumExp(data)` and asserts `assertTrue(Double.isFinite(result) && result > 0 && result < 50)` (the theoretical upper bound is `max + log(n) <= 50 + log(500) ≈ 56.7`). This is a STABILITY test, not a numerical-precision test. Mark the test as `logsumexp_stableAndFinite` in the test class.
    5. Final JSON shape (simplified):
       ```
       {"op":"logsumexp_simple","seed":47,"input_gen":"double[500]: java.util.Random.nextDouble()*100.0-50.0 per element","tolerance_rel":1e-14,"theoretical_upper_bound":56.7}
       ```
       The test reads `theoretical_upper_bound` and asserts `result <= theoretical_upper_bound && Double.isFinite(result)`. This is what the D-13 "max_rel_err: 1e-14" intent becomes in practice: stability + finite.

    6. All three fixtures are valid JSON, single-line, no trailing commas. Confirm with `python -m json.tool < <file>` (or simply `cat` them — they should round-trip through any JSON parser).
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      cat bench/src/test/resources/golden/sum_mean_pathological.json
      cat bench/src/test/resources/golden/softmax_extreme_logits.json
      cat bench/src/test/resources/golden/logsumexp_simple.json
</automated>
</verify>
  <acceptance_criteria>
    - `bench/src/test/resources/golden/sum_mean_pathological.json` exists, contains `"op":"sum_mean"`, `"magnitude_class":"extreme_cancellation"`, `"seed":45`, `"input_gen":"double[1000000]: alternating [1e15, 1e-15] starting at 1e15"`, and `"tolerance_rel":1e-13`. The `expected.sum` value is `5.000000000000005e20` (or the mathematically correct value for the pattern — executor MUST verify the math, not blindly use the draft).
    - `bench/src/test/resources/golden/softmax_extreme_logits.json` exists, contains `"op":"softmax_extreme_logits"`, `"seed":46`, input gen referencing ±1e300 range, and `"tolerance_rel":1e-13`.
    - `bench/src/test/resources/golden/logsumexp_simple.json` exists, contains `"op":"logsumexp_simple"`, `"seed":47`, mixed-magnitude input gen, and `"tolerance_rel":1e-14`.
    - All three files parse as valid JSON (`python -m json.tool` round-trips without error, OR they parse correctly in the JVM test class).
    - Existing golden fixtures (matmul_256.json, sum_mean.json, softmax_extreme.json, linear_regression_iris.json) are UNTOUCHED.
</acceptance_criteria>
  <done>
    3 new Phase 3 golden fixtures are committed and loadable via GoldenFixtures.load. Plan 03 test class can now reference them.
</done>
</task>

<task type="auto">
  <name>Task 03-03-02: Create AccuracyHardeningTest - 7 hard-fail golden tests (4 mirrors + 3 new</name>
  <files>modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java</files>
  <read_first>
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java (the 4 existing tests to mirror - line 137-233)
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java (load + assertWithinTolerance + assertMaxErrWithinTolerance + jsonToDoubleArray + relErr helpers from Plan 01)
    - modules/numja/src/main/java/numja/NumericStable.java (softmax, logSoftmax, logSumExp from Plan 01)
    - modules/numja/src/main/java/numja/core/ParallelOps.java (compensated sum, log-sum-exp prod from Plan 02)
    - modules/sklearn/src/main/java/sklearn/neural_network/Activations.java (softmax entry point - delegates to NumericStable per Plan 01)
    - bench/src/test/resources/golden/matmul_256.json (existing fixture shape - copy from GoldenReferenceTest.matmul256_vs_numpy)
    - bench/src/test/resources/golden/sum_mean.json
    - bench/src/test/resources/golden/softmax_extreme.json
    - bench/src/test/resources/golden/linear_regression_iris.json
    - bench/src/test/resources/golden/sum_mean_pathological.json (created in Task 03-03-01)
    - bench/src/test/resources/golden/softmax_extreme_logits.json (created in Task 03-03-01)
    - bench/src/test/resources/golden/logsumexp_simple.json (created in Task 03-03-01)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md D-08 (hard-fail), D-09 (7 tests), D-10 (soft-fail preservation)
</read_first>
  <action>
    Create `modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java`:

    1. Package declaration: `package sklearn.accuracy;`. Path stays in the same `sklearn/accuracy/` test source root.

    2. Imports: `numja.NumericStable`, `numja.NumJa`, `numja.core.NDArray`, `numja.core.ParallelOps`, `org.json.JSONArray`, `org.json.JSONObject`, `org.junit.Test`, `org.junit.experimental.categories.Category`, `static org.junit.Assert.assertTrue`. Plus `sklearn.accuracy.GoldenReferenceTest.Golden` (reuse the nested marker interface from the existing class — D-09 says `@Category(Golden.class)`).

    3. Class annotation: `@Category(GoldenReferenceTest.Golden.class)` — same filter as the soft-fail class.

    4. Implement 7 tests:

       a. `matmul256_withinTolerance` (mirror of `matmul256_vs_numpy` line 137-157):
          - Load `matmul_256` via `GoldenFixtures.load("matmul_256")`.
          - Same seed-driven input generation: draw all of `a` first, then all of `b` (line 144-147 of GoldenReferenceTest). n from JSON.
          - `NDArray result = NumJa.matmul(new NDArray(av), new NDArray(bv));`
          - Compute `maxErr` over the flat expected array (line 152-154 of GoldenReferenceTest).
          - `GoldenFixtures.assertMaxErrWithinTolerance("matmul_256 maxErr", maxErr, g.getDouble("tolerance_rel"));`

       b. `sum_mean_withinTolerance` (mirror of `sum_mean_vs_numpy` line 159-171):
          - Load `sum_mean` via `GoldenFixtures.load("sum_mean")`.
          - Same seed + `nextDouble()*2-1` input generation at n=1_000_000.
          - `GoldenFixtures.assertWithinTolerance("sum(1e6)", NumJa.sum(arr), g.getJSONObject("expected").getDouble("sum"), tol);`
          - `GoldenFixtures.assertWithinTolerance("mean(1e6)", NumJa.mean(arr), g.getJSONObject("expected").getDouble("mean"), tol);`
          - `tol = g.getDouble("tolerance_rel");`

       c. `softmax_extreme_withinTolerance` (mirror of `softmax_extreme_vs_numpy` line 179-199):
          - Load `softmax_extreme` via `GoldenFixtures.load("softmax_extreme")`.
          - The existing test is REFERENCE-ONLY (inline stable softmax) - per the comment at line 173-178, the harness is validated against NumPy. The HARD-FAIL version calls `Activations.softmax(x)` (which now delegates to NumericStable per Plan 01) — this is the production code path that golden tests SHOULD exercise.
          - Build input same way (seed, `nextDouble()*1400-700`, n=1000).
          - `double[] result = Activations.softmax(x);`
          - Compute `maxErr` against `g.getJSONArray("expected")` using `GoldenFixtures.jsonToDoubleArray`.
          - `GoldenFixtures.assertMaxErrWithinTolerance("softmax_extreme maxErr", maxErr, g.getDouble("tolerance_rel"));`

       d. `linreg_iris_withinTolerance` (mirror of `linear_regression_iris_vs_numpy` line 201-233):
          - Load `linear_regression_iris` via `GoldenFixtures.load("linear_regression_iris")`.
          - Same `readIrisFeatures()` helper logic (line 111-126 of GoldenReferenceTest) - COPY the `readIrisFeatures` private method into AccuracyHardeningTest (it's small and GoldenReferenceTest is the only consumer that doesn't share it; lazy option: inline the iris.csv parse).
          - Same `LinearRegression().fit(X, y).predict(X)` flow.
          - Sample `coef`, `intercept`, ~10 predictions as in lines 222-231 of GoldenReferenceTest. Each call: `GoldenFixtures.assertWithinTolerance(label, actual, expected, tol)`.

       e. `sum_mean_pathological_withinTolerance` (NEW, Phase 3):
          - Load `sum_mean_pathological` via `GoldenFixtures.load("sum_mean_pathological")`.
          - Regenerate input per the JSON `input_gen` spec: `double[] data = new double[1_000_000]; for (int i = 0; i < data.length; i++) data[i] = (i % 2 == 0) ? 1e15 : 1e-15;` (alternating pattern starting at 1e15).
          - Compute `ParallelOps.sum(data)` directly (the per-leaf Kahan compensated sum from Plan 02).
          - For the expected value: read `g.getJSONObject("expected").getDouble("sum")` and `g.getJSONObject("expected").getDouble("mean")`. The executor MUST verify the expected value is mathematically correct for the alternating pattern before committing the test (if it's wrong, the test fails by design — see Task 03-03-01 acceptance criteria).
          - `GoldenFixtures.assertWithinTolerance("sum_pathological", actual, expected_sum, tol);` and same for mean.

       f. `softmax_extreme_logits_withinTolerance` (NEW, Phase 3):
          - Load `softmax_extreme_logits` via `GoldenFixtures.load("softmax_extreme_logits")`.
          - Generate input per spec: `double[] x = new double[1000]; Random rng = new Random(46L); for (int i = 0; i < x.length; i++) x[i] = rng.nextDouble() * 2e300 - 1e300;` (the ±1e300 range).
          - Call `double[] result = Activations.softmax(x);` — production code path, delegates to NumericStable.softmax.
          - Assert: every element of `result` is finite (no NaN, no Inf) — loop with `assertTrue("softmax output not finite at index " + i, Double.isFinite(result[i]));`. Plus `sum(result)` should be within `1e-13` of `1.0` (softmax normalization invariant).
          - Read `g.getDouble("tolerance_rel")` for the sum-check tolerance.
          - The fixture does NOT store per-element expected values — this is a STABILITY + NORMALIZATION test (the softmax output must be finite and sum to 1).

       g. `logsumexp_simple_withinTolerance` (NEW, Phase 3):
          - Load `logsumexp_simple` via `GoldenFixtures.load("logsumexp_simple")`.
          - Generate input: `double[] x = new double[500]; Random rng = new Random(47L); for (int i = 0; i < x.length; i++) x[i] = rng.nextDouble() * 100.0 - 50.0;`.
          - Call `double result = NumericStable.logSumExp(x);`.
          - Assert: `result` is finite AND `result <= g.getDouble("theoretical_upper_bound")` AND `result >= -50.0` (the min input value, lower bound for logSumExp when one input dominates).
          - `GoldenFixtures.assertMaxErrWithinTolerance("logsumexp_stableAndFinite", <some error measure>, g.getDouble("tolerance_rel"))` — the lazy option is to call `NumericStable.logSumExp(x)` twice and assert they agree within relErr 1e-14 (self-consistency gate, since the fixture has no NumPy reference).

    5. The `readIrisFeatures` helper can be COPIED verbatim from `GoldenReferenceTest.java:111-126` into AccuracyHardeningTest (small helper, no need to extract to GoldenFixtures - lazy option). Alternative: move it to GoldenFixtures as `static double[][] readIrisFeatures()` and have both test classes call it - cleaner but adds a refactor to Plan 01's deliverable. The LAZY option is the copy.

    6. Do NOT add any `@Before` / `@After` / `@BeforeClass` setup. Each test is self-contained.

    7. Do NOT add the REPORT buffer or the soft-fail helper - this class is HARD-FAIL. Per D-08, D-10.
</action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/sklearn -am test -Dtest=AccuracyHardeningTest -DfailIfNoTests=false
</automated>
</verify>
  <acceptance_criteria>
    - `modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java` exists with class annotation `@Category(GoldenReferenceTest.Golden.class)`.
    - The file contains 7 `@Test` methods with names matching: `matmul256_withinTolerance`, `sum_mean_withinTolerance`, `softmax_extreme_withinTolerance`, `linreg_iris_withinTolerance`, `sum_mean_pathological_withinTolerance`, `softmax_extreme_logits_withinTolerance`, `logsumexp_simple_withinTolerance` (D-09 spec).
    - `mvn -pl modules/sklearn -am test -Dtest=AccuracyHardeningTest` exits 0 with all 7 tests passing.
    - `mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest` STILL exits 0 (soft-fail class unchanged - D-10).
    - `mvn test` (full suite from repo root) exits 0 with both classes present and passing.
    - `grep "REPORT\|soft.fail\|assertMechanicsOnly" modules/sklearn/src/test/java/sklearn/accuracy/AccuracyHardeningTest.java` returns nothing — the hard-fail class does NOT use the soft-fail REPORT buffer.
    - Each test calls `GoldenFixtures.assertWithinTolerance` or `GoldenFixtures.assertMaxErrWithinTolerance` (grep confirms).
</acceptance_criteria>
  <done>
    ACC-03 is satisfied. The hard-fail `AccuracyHardeningTest` mirrors all 4 existing GoldenReferenceTest tests + 3 new Phase 3 tests = 7 tests total, each with hard-fail assertions on tolerance miss. The soft-fail `GoldenReferenceTest` continues to work alongside it.
</done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Reference Plan 01. Any threat rated high must have disposition = block."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code (sklearn/accuracy/) to source code (sklearn, numja) | Same Maven reactor; tests see package-private sources via Surefire classpath |
| AccuracyHardeningTest to bench golden fixtures | Filesystem or classpath via GoldenFixtures.load |
| Test asserts to JUnit | assertTrue throws AssertionError on miss (hard-fail gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-10 | Repudiation (test fixture integrity) | GoldenFixtures.load | accept | Same loader as Phase 1/2; classpath + filesystem fallback; throws IllegalStateException on missing fixture. No write surface. |
| T-3-11 | Tampering (test gate) | AccuracyHardeningTest hard-fail assertions | mitigate | Each test uses GoldenFixtures.assertWithinTolerance / assertMaxErrWithinTolerance which throw AssertionError on miss. No REPORT buffer (D-08). CI-grade gate. |
| T-3-12 | Information disclosure | iris.csv parse | accept | Test loads a local CSV file from `../../dist/datasets/iris.csv` - same as Phase 1; no PII. |
| T-3-13 | Elevation of privilege | n/a | accept | Test code; no auth surface. |
| T-3-14 | DoS | Test memory usage | mitigate | Mirrors Phase 1 patterns: matmul test allocates 256x256 doubles (~512 KB), sum_mean allocates 1_000_000 doubles (~8 MB). All well within Surefire default heap. softmax_extreme_logits allocates 1000 doubles. |
| T-3-SC | Tampering (supply chain) | No new packages | mitigate | Only JDK stdlib + already-installed JSON libraries (org.json on the test classpath via Phase 1). No new dependencies. |

## ASVS L1 Coverage

- V5 Input Validation: each test deserializes its fixture via GoldenFixtures.load which validates JSON shape (throws IllegalStateException on missing file, JSONException on malformed JSON - both via org.json).
- V6/V2/V3: n/a.

## Out of Scope

- ACC-01 compensated sum/prod implementation (Plan 02 already covered).
- ACC-02 stable softmax delegation (Plan 01 already covered).
- BENCH-03 regression gate script (Plan 04).
- Adding new public API to NumJa or ArrayOps (D-07).
</threat_model>

<verification>
- mvn -pl modules/sklearn -am test -Dtest=AccuracyHardeningTest exits 0 (7 tests).
- mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest exits 0 (soft-fail still works).
- mvn test from repo root exits 0 (full suite green).
- bench/src/test/resources/golden/sum_mean_pathological.json, softmax_extreme_logits.json, logsumexp_simple.json exist and are valid JSON.
- grep -c "public static" modules/numja/src/main/java/numja/NumJa.java returns 61 (no public API drift).
</verification>

<success_criteria>
- 3 new Phase 3 golden fixtures committed (sum_mean_pathological, softmax_extreme_logits, logsumexp_simple).
- AccuracyHardeningTest exists with 7 hard-fail tests, @Category(Golden.class), each calling GoldenFixtures.assertWithinTolerance or assertMaxErrWithinTolerance.
- All 7 AccuracyHardeningTest tests pass against the compensated sum + NumericStable-backed softmax/logSumExp from Plans 01-02.
- GoldenReferenceTest continues to soft-fail PASS for all 4 existing fixtures.
- Full mvn test exits 0 with both classes present.
- ACC-03 is satisfied.
</success_criteria>

<output>
Create .planning/phases/03-numerical-accuracy-hardening/03-03-SUMMARY.md when done.
</output>
