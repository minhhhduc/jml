---
phase: 03-numerical-accuracy-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/numja/src/main/java/numja/NumericStable.java
  - modules/sklearn/src/main/java/sklearn/neural_network/Activations.java
  - modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java
  - modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java
autonomous: true
requirements: [ACC-02]
user_setup: []
must_haves:
  truths:
    - "NumericStable.softmax(double[]) returns the same values as the inline stable softmax currently in Activations.softmax on in-range inputs (no behavior change for already-stable callers)"
    - "Activations.softmax and LogisticRegression.computeProbabilities both delegate to NumericStable.softmax, eliminating duplicate inline implementations"
    - "NumericStable.softmax/logSoftmax/logSumExp handle extreme logits (±1e300) without producing NaN or Inf in the output"
    - "GoldenFixtures.load(name) returns the same JSONObject as GoldenReferenceTest.load(name) for every existing golden fixture"
    - "GoldenFixtures.check(label, actual, expected, tolRel) records PASS/FAIL to a per-call buffer the same way GoldenReferenceTest.check does"
  artifacts:
    - path: "modules/numja/src/main/java/numja/NumericStable.java"
      provides: "Max-shift stable softmax/logSoftmax/logSumExp primitives"
      contains: "public final class NumericStable"
    - path: "modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java"
      provides: "Package-private load/check/relErr helpers shared by GoldenReferenceTest + AccuracyHardeningTest"
      contains: "static JSONObject load"
  key_links:
    - from: "modules/sklearn/src/main/java/sklearn/neural_network/Activations.java"
      to: "modules/numja/src/main/java/numja/NumericStable.java"
      via: "Direct class import: NumericStable.softmax(x)"
      pattern: "NumericStable\\.softmax"
    - from: "modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java"
      to: "modules/numja/src/main/java/numja/NumericStable.java"
      via: "Direct class import: NumericStable.softmax(logits)"
      pattern: "NumericStable\\.softmax"
    - from: "modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java"
      to: "modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java"
      via: "GoldenFixtures.load / GoldenFixtures.check calls"
      pattern: "GoldenFixtures\\.(load|check)"
---

<objective>
Build the Wave-1 infrastructure for ACC-02: a single-source `numja.NumericStable` primitive class hosting stable softmax + logSoftmax + logSumExp, refactor the two existing sklearn consumers (`Activations.softmax`, `LogisticRegression.softmax`) to delegate to it, and create a package-private `GoldenFixtures` utility that extracts the `load()` + `check()` helpers out of `GoldenReferenceTest` so Plan 03 can build `AccuracyHardeningTest` against the same shared loader. No behavior change for in-range inputs (existing stable code is lifted verbatim).

Purpose: Establish single source of truth for stable softmax/log primitives so future groupby/aggregation work can reuse `logSumExp`; enable Plan 03's hard-fail `AccuracyHardeningTest` to share fixture-loading code with `GoldenReferenceTest` without duplication (per D-11).

Output: `NumericStable.java` (new public class, NOT added to NumJa facade per D-07); `Activations.java` + `LogisticRegression.java` delegate to it; `GoldenFixtures.java` (new package-private util).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md
@.planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md
@.planning/STATE.md
@modules/sklearn/src/main/java/sklearn/neural_network/Activations.java
@modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java
@modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java
@bench/src/test/resources/golden/sum_mean.json
@bench/src/test/resources/golden/softmax_extreme.json
@.planning/REQUIREMENTS.md
</context>

<interfaces>
From `modules/sklearn/src/main/java/sklearn/neural_network/Activations.java` line 89-107 (current `softmax`):
- Returns `double[]` of length `x.length`.
- Algorithm: scan max → `exp[i] = Math.exp(x[i] - max)` → sum → divide. Max-shift stable for finite inputs.
- Public static; called by `MLPClassifier` at line 88 and 202.

From `modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java` line 170-184 (current private `softmax`):
- Same algorithm as `Activations.softmax` (max-shift + exp + normalize).
- Private (package-scoped helper); called from `computeProbabilities` line 167.
- Allocates one new `double[]` and mutates it in place — same allocation pattern as `Activations`.

From `modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java`:
- `load(String name)` line 55-65: tries classpath `/golden/{name}.json` first, then filesystem fallback to `../../bench/src/test/resources/golden/{name}.json`. Returns `JSONObject`.
- `relErr(double, double)` line 67-70: `|actual - expected| / max(|expected|, 1e-30)`.
- `check(String, double, double, double)` line 73-82: appends formatted PASS/FAIL line to `REPORT` list, also prints to stdout.
- `checkMaxErr(String, double, double)` line 85-90: same shape for pre-aggregated max errors.
- `flushReport()` line 92-98: writes REPORT list to `target/golden-report.txt`.
- `assertMechanicsOnly()` line 100-103: always passes (soft-fail).
- `Golden` marker interface at line 42 (nested in `GoldenReferenceTest`).

From `modules/numja/src/main/java/numja/NumJa.java`:
- Public static count = 61 (frozen per D-07). `NumericStable` is NOT added here.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 03-01-01: Create numja.NumericStable with stable softmax + logSoftmax + logSumExp</name>
  <files>modules/numja/src/main/java/numja/NumericStable.java</files>
  <read_first>
    - modules/sklearn/src/main/java/sklearn/neural_network/Activations.java (line 89-107: existing stable softmax to lift verbatim)
    - modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java (line 170-184: same algorithm in the second consumer)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md §D-04..D-07 (package, class shape, public-API-delta constraint)
    - .planning/research/SUMMARY.md §Stack (max-shift softmax rationale)
    - .planning/REQUIREMENTS.md §ACC-02 (formal text)
 </read_first>
  <action>
    Create `modules/numja/src/main/java/numja/NumericStable.java`:

    1. Package declaration: `package numja;` (sits at the `numja` root alongside `NumJa.java`, NOT in `numja.core` — per D-04 the class is reachable by direct import, not added to the facade).

    2. Class signature: `public final class NumericStable { private NumericStable() {} ... }` — final + private constructor matches the NumJa facade style and prevents instantiation/subclassing.

    3. Method 1 — softmax:
       - Signature: `public static double[] softmax(double[] x)`.
       - Javadoc: one-liner "Max-shift stable softmax: subtract max before exp so result is finite for any finite input."
       - Body — lift the EXACT algorithm from `Activations.java:89-107` verbatim: scan max in a loop, then `exp[i] = Math.exp(x[i] - max)` and accumulate sum in a second loop, then divide in a third loop. Do NOT change order of operations or the `if (val > max)` comparison style — bit-identical output for in-range inputs is the goal (D-05).
       - Allocate one new `double[]` of length `x.length` and return it (matches existing allocation pattern).

    4. Method 2 — logSoftmax:
       - Signature: `public static double[] logSoftmax(double[] x)`.
       - Javadoc: one-liner "Max-shift stable log-softmax: returns log probabilities without the underflow of softmax(x) -> log()."
       - Body: scan max in loop, then `result[i] = (x[i] - max) - log(sum(exp(x[i] - max)))`. Use `logSumExp(x)` from below to compute the denominator (do NOT inline `logSumExp` again). Allocate one new `double[]` of length `x.length` and return it.

    5. Method 3 — logSumExp:
       - Signature: `public static double logSumExp(double[] x)`.
       - Javadoc: one-liner "Max-shift stable log(sum(exp(x))): returns -Inf if x is empty; otherwise max + log(sum(exp(x - max)))."
       - Body: if `x.length == 0` return `Double.NEGATIVE_INFINITY`. Otherwise scan max in loop, then loop accumulating `sum += Math.exp(x[i] - max)`, then return `max + Math.log(sum)`. Single scalar return (no array).

    6. No other public surface. No imports beyond `java.lang.Math`. Do NOT add any logger, no exception types beyond `IllegalArgumentException` (not used here), no JUnit dependencies.

    7. Do NOT add `softmax` / `logSoftmax` / `logSumExp` to `numja.NumJa` facade (D-07 public-API-delta: 0 — NumJa.java stays at 61 public static). Verify by `grep -c "public static" modules/numja/src/main/java/numja/NumJa.java` returning 61 after this task.
 </action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/numja -am compile
  </automated>
 </verify>
  <acceptance_criteria>
    - `modules/numja/src/main/java/numja/NumericStable.java` exists, contains `public final class NumericStable`, `private NumericStable()`, and the three method signatures `public static double[] softmax(double[])`, `public static double[] logSoftmax(double[])`, `public static double logSumExp(double[])`.
    - `mvn -pl modules/numja -am compile` exits 0 (numja module compiles with the new class).
    - `grep -c "public static" modules/numja/src/main/java/numja/NumJa.java` returns 61 (no public-API drift on the facade, per D-07).
    - `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java` returns 34 (no public-API drift on ArrayOps, per D-07).
    - The three methods each contain a Javadoc one-liner (the second word after `*` must be "Max-shift" or similar — not absent).
 </acceptance_criteria>
  <done>
    `NumericStable` is in place with three max-shift stable primitives. No public API drift on the facade. Ready to be referenced by sklearn consumers (next task) and by ParallelOps.prod (Plan 02).
 </done>
</task>

<task type="auto">
  <name>Task 03-01-02: Refactor sklearn softmax consumers to delegate to NumericStable.softmax</name>
  <files>modules/sklearn/src/main/java/sklearn/neural_network/Activations.java, modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java</files>
  <read_first>
    - modules/sklearn/src/main/java/sklearn/neural_network/Activations.java (whole file, especially line 89-107 — the existing stable softmax body to be replaced by a one-line delegation)
    - modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java (whole file, especially line 159-184 — `computeProbabilities` + the private `softmax` helper to be removed)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md §D-05 (extraction target: both consumers delegate to NumericStable.softmax; zero behavior change for in-range inputs)
 </read_first>
  <action>
    Modify `modules/sklearn/src/main/java/sklearn/neural_network/Activations.java`:

    1. Add import at the top of the file (after the existing `package sklearn.neural_network;` line): `import numja.NumericStable;`.

    2. Replace the entire body of `public static double[] softmax(double[] x)` (line 89-107) with a one-line delegation: `return NumericStable.softmax(x);`. Preserve the Javadoc lines 84-88 verbatim.

    3. Do NOT touch any other method in `Activations.java` — sigmoid, relu, tanh, linear, derivatives all stay verbatim.

    Modify `modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java`:

    1. Add import at the top of the file (after the existing imports for `NDArray` and `NumJa`): `import numja.NumericStable;`.

    2. Replace the body of the private `softmax(double[] x)` method (line 170-184) with a one-line delegation: `return NumericStable.softmax(x);`. Keep the Javadoc one-liner above it.

    3. The private `softmax` method on `LogisticRegression` becomes a thin one-liner — keep it (the call site at line 167 still routes through it). Alternatively, delete the private method entirely and have `computeProbabilities` (line 167) call `NumericStable.softmax(logits)` directly. EITHER choice is acceptable; the lazy option is to keep the private method as a one-line delegator so the diff is minimal. Document the choice in the SUMMARY.

    4. Do NOT touch any other method in `LogisticRegression.java` — `fit`, `predict`, `predictProba`, `score`, `computeProbabilities` body stays verbatim.

    5. Verify by `grep -n "NumericStable"` on both files — each must have exactly one new reference.
 </action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/sklearn -am compile
  </automated>
 </verify>
  <acceptance_criteria>
    - `mvn -pl modules/sklearn -am compile` exits 0.
    - `modules/sklearn/src/main/java/sklearn/neural_network/Activations.java` contains `import numja.NumericStable;` AND `return NumericStable.softmax(x);` in the `softmax` method body.
    - `modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java` contains `import numja.NumericStable;` AND a delegation call to `NumericStable.softmax` (either via a one-line private `softmax` method or directly in `computeProbabilities`).
    - `grep -c "NumericStable" modules/sklearn/src/main/java/sklearn/neural_network/Activations.java modules/sklearn/src/main/java/sklearn/linear_model/LogisticRegression.java` returns 1 per file (one import + one use; no double-imports).
    - `mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest -DfailIfNoTests=false` exits 0 — the existing `softmax_extreme_vs_numpy` reference-only check still PASSes (its inline implementation is unchanged; only the consumers `Activations.softmax` + `LogisticRegression.softmax` now delegate, and the reference-only check is independent).
 </acceptance_criteria>
  <done>
    Both sklearn softmax consumers delegate to `NumericStable.softmax`. The pre-existing inline implementations are removed in favor of a single source of truth. No behavior change for in-range inputs (D-05 satisfied); ACC-02 softmax/log stability now lives in one place.
 </done>
</task>

<task type="auto">
  <name>Task 03-01-03: Extract GoldenFixtures package-private utility from GoldenReferenceTest</name>
  <files>modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java, modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java</files>
  <read_first>
    - modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java (lines 55-103: every helper to extract; lines 47-51: REPORT list + resetReport)
    - .planning/phases/03-numerical-accuracy-hardening/03-CONTEXT.md §D-11 (extract load/check helpers to package-private utility)
    - .planning/phases/02-cpu-parallel-core-threads/02-PATTERNS.md §JUnit 4 conventions (test classpath style)
 </read_first>
  <action>
    Create `modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java`:

    1. Package declaration: `package sklearn.accuracy;`. Path stays in the existing `sklearn/accuracy/` test source root.

    2. JUnit 4 imports: `org.json.JSONArray`, `org.json.JSONObject`, `org.json.JSONTokener`. Plus `java.io.File`, `java.io.FileInputStream`, `java.io.InputStream`, `java.io.PrintWriter`, `java.util.ArrayList`, `java.util.List`.

    3. Static fields:
       - `private static final String GOLDEN_DIR = "../../bench/src/test/resources/golden";` (same path as the existing one in `GoldenReferenceTest`).
       - `private static final List<String> REPORT = new ArrayList<>();` (shared soft-fail buffer — `GoldenReferenceTest` keeps appending to it; `AccuracyHardeningTest` will NOT use the buffer per D-08/D-10).

    4. Public static helpers (package-private — no `public` modifier):
       - `static JSONObject load(String name) throws Exception` — copy the EXACT body of `GoldenReferenceTest.load` line 55-65. Tries classpath `/golden/{name}.json` first, falls back to `GOLDEN_DIR + "/" + name + ".json"` on filesystem, throws `IllegalStateException` if neither exists.
       - `static double relErr(double actual, double expected)` — copy the EXACT body of `GoldenReferenceTest.relErr` line 67-70: `denom = Math.max(Math.abs(expected), 1e-30); return Math.abs(actual - expected) / denom;`.
       - `static void check(String label, double actual, double expected, double tolRel)` — copy `GoldenReferenceTest.check` line 73-82 verbatim (formats PASS/FAIL line, appends to `REPORT`, prints to stdout).
       - `static void checkMaxErr(String label, double maxErr, double tolRel)` — copy `GoldenReferenceTest.checkMaxErr` line 85-90 verbatim.
       - `static void flushReport()` — copy `GoldenReferenceTest.flushReport` line 92-98 verbatim (writes REPORT to `target/golden-report.txt`).
       - `static void resetReport()` — copy the body of `GoldenReferenceTest.resetReport` line 47-51 (clears `REPORT` and adds the header line).
       - `static void assertMechanicsOnly()` — copy `GoldenReferenceTest.assertMechanicsOnly` line 100-103 (flushes report then `assertTrue(true)`).

    5. Utility for AccuracyHardeningTest:
       - `static void assertWithinTolerance(String label, double actual, double expected, double tolRel)` — the HARD-FAIL counterpart of `check`: `double err = relErr(actual, expected); assertTrue(label + " err=" + err + " tol=" + tolRel, err <= tolRel);`. Throws `AssertionError` on miss (no REPORT buffer involvement). Used by Plan 03's `AccuracyHardeningTest`.
       - `static void assertMaxErrWithinTolerance(String label, double maxErr, double tolRel)` — same shape for pre-aggregated max errors.

    6. `static double[] jsonToDoubleArray(JSONArray arr)` — copy `GoldenReferenceTest.jsonToDoubleArray` line 128-132 verbatim (used by `softmax_extreme_logits` test in Plan 03).

    Modify `modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java`:

    1. Remove the inline helpers that are now in `GoldenFixtures`: delete `load` (line 55-65), `relErr` (line 67-70), `check` (line 73-82), `checkMaxErr` (line 85-90), `flushReport` (line 92-98), `assertMechanicsOnly` (line 100-103), `jsonToDoubleArray` (line 128-132). Delete the static field `REPORT` (line 45) and `GOLDEN_DIR` (line 44) — both moved to `GoldenFixtures`.

    2. Update the call sites to use `GoldenFixtures.load(...)`, `GoldenFixtures.check(...)`, etc. — preserve the same args. For `resetReport` (currently called in `@BeforeClass` at line 47-51), call `GoldenFixtures.resetReport()` instead.

    3. The `@Category(GoldenReferenceTest.Golden.class)` annotation on the class (line 38) STAYS — `Golden` is still the nested interface at line 42 used by the category filter.

    4. The `@Ignore` on `printReport` (line 239) STAYS — soft-fail harness behaviour is preserved.

    5. Run `mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest -DfailIfNoTests=false` after edits to confirm the soft-fail harness still records PASS lines to `target/golden-report.txt`.
 </action>
  <verify>
    <automated>
      cd D:\unknown\projects\java_ml
      $env:Path = 'C:\Users\Admin\.maven\maven-3.9.15\bin;' + $env:Path
      mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest -DfailIfNoTests=false
  </automated>
 </verify>
  <acceptance_criteria>
    - `modules/sklearn/src/test/java/sklearn/accuracy/GoldenFixtures.java` exists with all the helpers listed in action step 4-6. The file contains `static JSONObject load`, `static double relErr`, `static void check`, `static void assertWithinTolerance`, and `static void assertMaxErrWithinTolerance` (grep confirms each).
    - `mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest` exits 0 and `modules/sklearn/target/golden-report.txt` contains `PASS` lines for the 4 existing fixtures (matmul_256, sum_mean, softmax_extreme, linear_regression_iris).
    - `modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java` no longer contains `private static JSONObject load` (the inline definition is gone — `grep "private static JSONObject load" GoldenReferenceTest.java` returns nothing).
    - `modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java` contains `GoldenFixtures.load(` and `GoldenFixtures.check(` (call sites rewired).
    - The `@Category(GoldenReferenceTest.Golden.class)` annotation is preserved on `GoldenReferenceTest`.
 </acceptance_criteria>
  <done>
    `GoldenFixtures` utility is the single source for fixture loading and soft-fail recording. `GoldenReferenceTest` is a thin caller. Plan 03 can now build `AccuracyHardeningTest` against `GoldenFixtures.assertWithinTolerance` without re-implementing the loader.
 </done>
</task>

</tasks>

<threat_model>
security_enforcement: true
asvs_level: 1
disposition_policy: "Any threat rated high must have disposition = block. STRIDE review at plan-close."

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sklearn caller → Activations.softmax / LogisticRegression.computeProbabilities | External library API; unchanged signatures |
| sklearn → numja.NumericStable | Internal cross-module class import; `NumericStable` is `public final` (deliberately callable) |
| Test code → GoldenFixtures | Same-package test-only utility; package-private to `sklearn.accuracy` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (numerical) | NumericStable.softmax/logSoftmax/logSumExp | mitigate | Max-shift pattern lifted verbatim from the existing `Activations.softmax` (a stable implementation that's already been validated by `GoldenReferenceTest.softmax_extreme_vs_numpy` at relErr ~4.3e-16). Same algorithm = same numerical behaviour for in-range inputs. No new failure surface. |
| T-3-02 | Spoofing (test fixture integrity) | GoldenFixtures.load | accept | Same loader as the existing `GoldenReferenceTest.load` — classpath first, filesystem fallback. Throws `IllegalStateException` on missing file. No write surface (read-only). |
| T-3-03 | Repudiation | GoldenFixtures.check / REPORT buffer | accept | Soft-fail only; `target/golden-report.txt` is the audit trail. `AccuracyHardeningTest` (Plan 03) does NOT use the buffer — hard-fail goes straight to JUnit. |
| T-3-04 | Information disclosure | n/a | accept | Library; no PII or network. |
| T-3-05 | Elevation of privilege | n/a | accept | No auth surface. |
| T-3-SC | Tampering (supply chain) | No new packages | mitigate | No new dependencies. Only JDK stdlib (`java.lang.Math`) and JDK JSON classes (already on the test classpath via Phase 1 setup). No `[ASSUMED]`/`[SUS]` packages introduced. |

## ASVS L1 Coverage

- V5 Input Validation: `NumericStable.softmax` and friends trust the caller to pass a non-null `double[]`. Same contract as the existing inline implementations. Documented via Javadoc.
- V6/V2/V3: n/a.

## Out of Scope

- ACC-01 compensated sum/prod (Plan 02).
- ACC-03 hard-fail AccuracyHardeningTest (Plan 03).
- BENCH-03 regression gate script (Plan 04).
</threat_model>

<verification>
- `mvn -pl modules/numja -am compile` exits 0 (NumericStable compiles standalone).
- `mvn -pl modules/sklearn -am compile` exits 0 (delegation compiles).
- `mvn -pl modules/sklearn -am test -Dtest=GoldenReferenceTest` exits 0 and `modules/sklearn/target/golden-report.txt` contains PASS lines for the 4 existing fixtures.
- `grep -c "public static" modules/numja/src/main/java/numja/NumJa.java` returns 61.
- `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java` returns 34.
</verification>

<success_criteria>
- `NumericStable` exists with stable softmax/logSoftmax/logSumExp; no public API drift on the `numja.NumJa` or `numja.core.ArrayOps` facade.
- `Activations.softmax` and `LogisticRegression.computeProbabilities` both delegate to `NumericStable.softmax` — single source of truth.
- `GoldenFixtures` utility owns the loader, soft-fail recorder, and the hard-fail `assertWithinTolerance` helper for Plan 03.
- `GoldenReferenceTest` continues to soft-fail PASS for all 4 existing fixtures (matmul_256, sum_mean, softmax_extreme, linear_regression_iris).
</success_criteria>

<output>
Create `.planning/phases/03-numerical-accuracy-hardening/03-01-SUMMARY.md` when done.
</output>
