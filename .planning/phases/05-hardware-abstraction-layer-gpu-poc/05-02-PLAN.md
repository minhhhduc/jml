---
phase: 05-hardware-abstraction-layer-gpu-poc
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - modules/numja/src/main/java/numja/core/ArrayOps.java
  - modules/numja/src/main/java/numja/core/NDArray.java
  - modules/numja/src/test/java/com/numja/core/CpuThreadBackendGoldenTest.java
autonomous: true
requirements: [HW-01, HW-02]
security_enforcement: true
asvs_level: 1
disposition_policy: block on high

must_haves:
  truths:
    - "ArrayOps.dot (the gemm POC target) routes through BackendSelector.get().matmul(...) and produces results bit-identical to the pre-refactor inline EJML path"
    - "At least 5 hot paths total wired through BackendSelector: dot, sum, min, max, prod (and optionally NDArray elementwiseBinary/Unary/ScalarBinary)"
    - "CpuThreadBackend stays numerically equivalent — ParallelRegressionTest 4/4 green and ratios within tolerance of Phase 2 baseline"
    - "HW-02 size ceiling enforced before dispatch: any n > MAX_DISPATCH_N throws IllegalArgumentException at the dispatch boundary with a clear message"
    - "NumJa.java=61 and ArrayOps.java=34 public static counts unchanged"
  artifacts:
    - path: modules/numja/src/main/java/numja/core/ArrayOps.java
      provides: "dot/sum/min/max/prod routed through BackendSelector.get()"
      contains: "BackendSelector.get()"
    - path: modules/numja/src/main/java/numja/core/NDArray.java
      provides: "sum/min/max/prod instance methods + optional elementwise trio routed through BackendSelector"
      contains: "BackendSelector.get()"
    - path: modules/numja/src/test/java/com/numja/core/CpuThreadBackendGoldenTest.java
      provides: "Pre-refactor vs post-refactor numeric equivalence + size-ceiling guard tests"
      exports: ["goldenDotMatchesPreRefactor", "dispatchCeilingThrows"]
  key_links:
    - from: "ArrayOps.dot"
      to: "BackendSelector.get().matmul"
      via: "static dispatch through ComputeBackend interface"
      pattern: "BackendSelector\\.get\\(\\)\\.matmul"
    - from: "NDArray.sum/min/max/prod"
      to: "BackendSelector.get().sum/min/max/prod"
      via: "replaces inline ParallelOps.* calls in instance methods"
      pattern: "BackendSelector\\.get\\(\\)\\.(sum|min|max|prod)"
    - from: "CpuThreadBackendGoldenTest"
      to: "ArrayOps.dot pre-refactor output"
      via: "fixed-seed double[] + deterministic input"
      pattern: "new Random\\(42L\\)"
---

<objective>
Wire 5+ hot paths through `BackendSelector.get().<op>(...)` so the HAL abstraction introduced in 05-01 becomes load-bearing: future GPU backends plug in without touching `ArrayOps` / `NDArray`. `ArrayOps.dot` (the gemm POC target) is wired first with a golden-value test that locks the pre-refactor numeric path. Then sum/min/max/prod are wired in `NDArray` instance methods. A HW-02 size ceiling guards the dispatch boundary against hostile inputs.

Purpose: Make the backend selection real (not just an unused selector). Lock current numerics so a future GPU backend can be swapped in and diff-tested against the golden values.
Output: `ArrayOps.java` + `NDArray.java` modified to route through `BackendSelector.get()`; `CpuThreadBackendGoldenTest` proving parity. Zero public API change. Zero new Maven deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardware-abstraction-layer-gpu-poc/05-RESEARCH.md
@.planning/phases/05-hardware-abstraction-layer-gpu-poc/05-01-PLAN.md

<interfaces>
Key contracts the executor must match exactly (extracted from codebase — no re-exploration needed).

From modules/numja/src/main/java/numja/core/ArrayOps.java (pre-refactor, lines 213-217 — the dot body to preserve bit-identical):
    DMatrixRMaj result = new DMatrixRMaj(a.getData().numRows, b.getData().numCols);
    CommonOps_DDRM.mult(a.getData(), b.getData(), result);
    return new NDArray(result, new int[]{a.getData().numRows, b.getData().numCols});

From modules/numja/src/main/java/numja/core/ComputeBackend.java (created in 05-01):
- void matmul(double[] a, int aRows, int aCols, double[] b, int bRows, int bCols, double[] out)
- double sum(double[] data)
- double prod(double[] data)
- double min(double[] data)
- double max(double[] data)
- void elementwiseBinary(double[] a, double[] b, double[] out, DoubleBinaryOperator op)
- void elementwiseUnary(double[] in, double[] out, DoubleUnaryOperator op)
- void scalarBinary(double[] a, double scalar, double[] out, DoubleBinaryOperator op)

From modules/numja/src/main/java/numja/core/CpuThreadBackend.java (created in 05-01):
- public static CpuThreadBackend getInstance()
- All 8 methods delegate 1:1 to ParallelOps / EJML. matmul is EJML-equivalent to ArrayOps.dot on raw arrays (DMatrixRMaj wrapping + CommonOps_DDRM.mult + System.arraycopy into out).

From modules/numja/src/main/java/numja/core/BackendSelector.java (created in 05-01):
- public static ComputeBackend get()
- public static final int THRESHOLD_GPU = 4_096_000  (research constant; HW-02 DoS guard uses a smaller value)
- HW-02 ceiling: align with pandas.internal.ChunkedReadOptions.MAX_CHUNK_ROWS = 100_000 — for dispatch DoS guard use a defensible per-op value. Recommended: MAX_DISPATCH_N = 1_000_000_000 (10^9 doubles = 8GB at n=10^9 — far beyond any single JVM op but small enough to allow legitimate matmul of e.g. 31622^2 = ~10^9 cells). Document the constant in BackendSelector.java with the chunk-reader alignment rationale.

From modules/numja/src/main/java/numja/core/NDArray.java (current dispatch sites to rewire):
- sum()/min()/max()/prod() at lines 402, 424, 441, 459 — currently call ParallelOps.sum/min/max/prod(data.data) above THRESHOLD, sequential below
- Optionally NDArray.add/sub/mul/div at lines 152, 189, 214, 246 — currently call ParallelOps.elementwiseBinary above THRESHOLD

From modules/pandas/src/main/java/pandas/internal/ChunkedReadOptions.java:
- public static final int MAX_CHUNK_ROWS = 100_000  (reference ceiling — HW-02 DoS guard aligns in spirit, not byte-value, with this)

From modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java:
- 4 timing tests using median-of-51 + FJP-vs-raw ratio gates. Ratios at n=10^7: elementwise ~0.95-1.10, reduce ~0.55-0.75. These are the regression gate that dispatch wiring must NOT break.
- @After reset() calls ParallelOps.resetThresholdForTesting() — this pattern is already in place; do NOT add a second hygiene block in new tests.

Test source directory: modules/numja/src/test/java/com/numja/core/ (test package is com.numja.core, NOT numja.core). Main source dir: modules/numja/src/main/java/numja/core/ (package numja.core).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ArrayOps.dot through BackendSelector + golden test</name>
  <files>modules/numja/src/main/java/numja/core/ArrayOps.java, modules/numja/src/test/java/com/numja/core/CpuThreadBackendGoldenTest.java</files>
  <read_first>
    - modules/numja/src/main/java/numja/core/ArrayOps.java  (current dot body lines 213-217 to preserve)
    - modules/numja/src/main/java/numja/core/CpuThreadBackend.java  (the matmul signature to call)
    - modules/numja/src/main/java/numja/core/ComputeBackend.java  (interface contract)
    - modules/numja/src/main/java/numja/core/BackendSelector.java  (get() entry point)
    - modules/numja/src/test/java/com/numja/core/ParallelRegressionTest.java  (median-of-51 timing pattern to follow)
 </read_first>
  <acceptance_criteria>
    - Test 1 (goldenDotMatchesPreRefactor): fixed-seed 64x64 matmul via ArrayOps.dot — capture expected matrix ONCE before any refactor (commit the expected file or inline literal), then assert post-refactor ArrayOps.dot on the same input matches the captured expected value element-by-element with exact == on doubles (because CpuThreadBackend.matmul is byte-equivalent to inline EJML).
    - Test 2 (dotMatmulViaBackendSelector): call BackendSelector.get().matmul(rawA, 64, 64, rawB, 64, 64, outRaw) directly and verify outRaw matches the captured golden expected. Proves the interface path itself is correct, not just ArrayOps.dot's wrapper.
    - Test 3 (dotDispatchCeilingThrows): when n = MAX_DISPATCH_N + 1 is passed into BackendSelector.get().matmul (e.g. aCols * bRows > MAX_DISPATCH_N), BackendSelector throws IllegalArgumentException with a message containing "exceeds dispatch ceiling" and the offending n.
 </behavior>
  <action>
    (1) Capture the golden expected BEFORE refactoring. In CpuThreadBackendGoldenTest.java, the first test computes `expected` inline from the inline EJML path — BUT we need a stable reference that survives the refactor. Solution: in the SAME test method, run both paths against the same input:
      - Path A = inline CommonOps_DDRM.mult (what ArrayOps.dot does today)
      - Path B = BackendSelector.get().matmul(...) (post-refactor path)
      Assert path B output equals path A output, element-by-element, with `assertEquals(expected, BackendSelector.get().matmul(...))` and `expected` derived from path A.
    This single-test layout avoids the "golden file drifts" pitfall — the reference is recomputed each run from the unchanged inline code path.

    (1b) Declare the HW-02 ceiling constant now so all Task 1 tests compile. In `BackendSelector.java` (created in 05-01), add:
        public static final long MAX_DISPATCH_N = 1_000_000_000L;
    with javadoc: 10^9 doubles = 8GB; aligns in spirit with `pandas.internal.ChunkedReadOptions.MAX_CHUNK_ROWS=100_000` row ceiling but is per-op, not per-chunk. Do NOT add the checkSize enforcement yet — that ships in Task 2. Task 1 only needs the constant so its ceiling test compiles.

    Write CpuThreadBackendGoldenTest.java in package com.numja.core with these tests:
      - `goldenDotMatchesPreRefactor(64x64)`: fixed seed new Random(42L), generate 64x64 raw double[] a, b. Compute expected via inline `new DMatrixRMaj(...)` + `CommonOps_DDRM.mult(...)`. Compute actual via `BackendSelector.get().matmul(a, 64, 64, b, 64, 64, out)`. assertArrayEquals on flattened `expected.data` vs `out`. JUnit 4: `assertArrayEquals(double[], double[], double eps)` — but since both paths use identical EJML the values are bit-identical, so use `assertEquals(double[], double[])` (JUnit 4 has overloads that use exact ==).
      - `dotDispatchCeilingThrows()`: assert that calling `BackendSelector.get().matmul(new double[(MAX_DISPATCH_N + 1) * 2], MAX_DISPATCH_N + 1, 2, ...)` throws IllegalArgumentException. Note: 05-02 ships the ceiling constant — the test validates it is in place and enforced at the dispatch boundary. (The ceiling constant itself lives in BackendSelector as a NEW constant `MAX_DISPATCH_N = 1_000_000_000` declared in 05-02 Task 2 below. To avoid forward-dependency, this test reads the constant via `BackendSelector.MAX_DISPATCH_N`. If MAX_DISPATCH_N is not yet declared, this test fails compilation — which is acceptable because Task 2 declares it before this test runs in the same plan. Order: do the ArrayOps.dot refactor + write this test in Task 1; declare MAX_DISPATCH_N + size-ceiling enforcement in Task 2; the ceiling test goes green in Task 2's run.)

    (2) Refactor ArrayOps.dot (lines 213-217). New body:
        DMatrixRMaj ma = a.getData();
        DMatrixRMaj mb = b.getData();
        int aRows = ma.numRows, aCols = ma.numCols, bRows = mb.numRows, bCols = mb.numCols;
        DMatrixRMaj result = new DMatrixRMaj(aRows, bCols);
        BackendSelector.get().matmul(ma.data, aRows, aCols, mb.data, bRows, bCols, result.data);
        return new NDArray(result, new int[]{aRows, bCols});
    Preserve shape/dim handling exactly. Add `import numja.core.BackendSelector;` if not already imported.

    (3) Do NOT add the HW-02 size ceiling at the ArrayOps.dot call site — it lives in BackendSelector (Task 2). ArrayOps.dot just calls `BackendSelector.get().matmul(...)`; if BackendSelector throws IllegalArgumentException on oversized n, ArrayOps.dot propagates.

    Run full numja test suite after refactor. ParallelRegressionTest must stay 4/4 green (refactor only swaps inline EJML for BackendSelector → CpuThreadBackend → same EJML body, bit-identical).
 </action>
  <verify>
    <automated>cd D:/unknown/projects/java_ml && "$HOME/.maven/maven-3.9.15/bin/mvn" -q -pl modules/numja -am test -Dtest='CpuThreadBackendGoldenTest,ParallelRegressionTest</automated>
 </verify>
  <done>ArrayOps.dot routes through BackendSelector.get().matmul; CpuThreadBackendGoldenTest passes (3/3 once Task 2 declares MAX_DISPATCH_N); ParallelRegressionTest stays 4/4 green; full numja test suite green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sum/min/max/prod + add HW-02 size ceiling in BackendSelector</name>
  <files>modules/numja/src/main/java/numja/core/ArrayOps.java, modules/numja/src/main/java/numja/core/NDArray.java, modules/numja/src/main/java/numja/core/BackendSelector.java, modules/numja/src/test/java/com/numja/core/CpuThreadBackendGoldenTest.java</files>
  <read_first>
    - modules/numja/src/main/java/numja/core/ArrayOps.java  (sum/min/max/prod bodies to refactor)
    - modules/numja/src/main/java/numja/core/NDArray.java  (instance method dispatch sites at lines 402, 424, 441, 459)
    - modules/numja/src/main/java/numja/core/BackendSelector.java  (add MAX_DISPATCH_N constant + ceiling guard in get() wrapper methods)
    - modules/numja/src/main/java/numja/core/CpuThreadBackend.java  (delegation methods being routed through)
    - modules/numja/src/main/java/numja/core/ParallelOps.java  (current direct callers — must keep working for ParallelRegressionTest which still calls ParallelOps directly)
 </read_first>
  <acceptance_criteria>
    - Test 4 (reduceOpsRoutedThroughBackend): ArrayOps.sum/min/max/prod on a 10000-element array returns values bit-identical to a direct call via BackendSelector.get().<op>(...). JUnit 4 assertEquals on double primitives (exact ==).
    - Test 5 (ndarrayInstanceOpsRoutedThroughBackend): NDArray.sum/min/max/prod instance methods on a 10000-element array return values bit-identical to BackendSelector.get().<op>(...) on the underlying data.
    - Test 6 (dispatchCeilingEnforcedForReductions): sum/min/max/prod via BackendSelector.get().<op>(new double[MAX_DISPATCH_N + 1]) each throw IllegalArgumentException with the offending n in the message.
 </behavior>
  <action>
    (1) Add to BackendSelector.java (file created in 05-01):
      - New constant: `public static final long MAX_DISPATCH_N = 1_000_000_000L;` with javadoc explaining the constant (10^9 doubles = 8GB; aligns in spirit with pandas.internal.ChunkedReadOptions.MAX_CHUNK_ROWS=100_000 row ceiling but is per-op, not per-chunk).
      - Wrap each of the 8 ComputeBackend methods in a small static guard helper. Pattern:
          public static void elementwiseBinary(double[] a, double[] b, double[] out, DoubleBinaryOperator op) {
              checkSize(a.length);
              get().elementwiseBinary(a, b, out, op);
          }
          public static double sum(double[] data) {
              checkSize(data.length);
              return get().sum(data);
          }
        (and similarly for prod/min/max/elementwiseUnary/scalarBinary/matmul — note matmul uses `aRows * bCols` for output, so checkSize(aRows * bCols).)
      - Private helper `private static void checkSize(long n) { if (n > MAX_DISPATCH_N) throw new IllegalArgumentException("dispatch n=" + n + " exceeds MAX_DISPATCH_N=" + MAX_DISPATCH_N); }`. Long to avoid overflow on aCols * bCols for giant inputs.
      - Keep `public static ComputeBackend get()` as the raw interface accessor (still used by BackendSelectorTest test 1 and by CpuThreadBackendGoldenTest test 2). The wrapping methods above are the gated dispatchers. Name them after the ComputeBackend methods (sum/prod/min/max/...) so ArrayOps/NDArray can call them by the same name they used on ComputeBackend.

    (2) Refactor ArrayOps.sum/min/max/prod (lines 222, 260, 267, 310). Each body currently delegates to NDArray instance method. Change to call `BackendSelector.sum(a.getData().data)` (and similar for min/max/prod) when `n >= ParallelOps.THRESHOLD`, fall through to NDArray instance method (which uses sequential loop) when `n < ParallelOps.THRESHOLD`. Simpler: keep ArrayOps.sum as `return a.sum()` (delegates to NDArray), and only rewire the NDArray instance methods. This avoids adding new logic to ArrayOps and keeps the public-API static counts unchanged. Choose this simpler path.

    (3) Refactor NDArray instance methods at lines 402 (sum), 424 (min), 441 (max), 459 (prod). Replace the existing branch:
        if (n < ParallelOps.THRESHOLD) { /* sequential */ } else { return ParallelOps.sum(data.data); }
    with:
        if (n < ParallelOps.THRESHOLD) { /* sequential */ } else { return BackendSelector.sum(data.data); }
    (and similarly for min/max/prod). Elementwise trio (add/sub/mul/div/elementwiseBinary) — IF the executor judges the change is a 1-line swap with no behaviour drift, rewire too; otherwise leave for a follow-up plan. Per must_haves: "at least 5 hot paths total wired" — dot (Task 1) + 4 reduce = 5. The elementwise trio is optional and may be deferred if any subclass dispatch or DMatrixRMaj wrapping complicates the swap.

    (4) Update CpuThreadBackendGoldenTest to add tests 4, 5, 6 (as listed in behavior). Test 4: ArrayOps.sum on seed-42 array of 10000 doubles equals BackendSelector.sum on same array. Test 5: NDArray.sum (instance) on same array equals BackendSelector.sum. Test 6: BackendSelector.sum(new double[MAX_DISPATCH_N + 1]) throws IllegalArgumentException with message containing "exceeds MAX_DISPATCH_N".

    (5) Run full numja test suite. ParallelRegressionTest still passes 4/4 (it calls ParallelOps directly, never BackendSelector — the test file is untouched by this refactor and remains green). CpuThreadBackendGoldenTest passes 6/6.

    (6) Confirm public API counts: `grep -c "public static" modules/numja/src/main/java/numja/core/NumJa.java` = 61, `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java` = 34. No new dependencies added to modules/numja/pom.xml.
 </action>
  <verify>
    <automated>cd D:/unknown/projects/java_ml && "$HOME/.maven/maven-3.9.15/bin/mvn" -q -pl modules/numja -am test</automated>
 </verify>
  <done>5+ hot paths routed through BackendSelector; HW-02 MAX_DISPATCH_N ceiling enforced at the dispatch boundary; CpuThreadBackendGoldenTest passes 6/6; ParallelRegressionTest still 4/4 green; full numja suite green; public API frozen; no new deps</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied array size -> BackendSelector dispatch | Untrusted n crosses here (DoS via OOM or GPU device lock) |
| Existing trust boundary from 05-01 | JVM system properties -> BackendSelector (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-HW-02 | Denial of Service | BackendSelector dispatcher wrappers (sum/min/max/prod/elementwise/matmul) | mitigate | New `MAX_DISPATCH_N = 1_000_000_000` constant in BackendSelector. Each dispatcher wrapper calls private `checkSize(long n)` before delegating; oversized n throws `IllegalArgumentException("dispatch n=X exceeds MAX_DISPATCH_N=Y")`. Aligned in spirit with `pandas.internal.ChunkedReadOptions.MAX_CHUNK_ROWS = 100_000` row ceiling (per-op vs per-chunk). Documented in javadoc on the constant. |
| T-05-HW-01 | Elevation of Privilege | BackendSelector.setBackend | mitigate (carries from 05-01) | Name-based allowlist {"cpu-thread"} + opt-in flag `numja.backend.allow=true`. Unchanged. |
| T-05-R3 | Tampering (numeric drift) | CpuThreadBackend.matmul vs inline ArrayOps.dot | mitigate | CpuThreadBackendGoldenTest goldenDotMatchesPreRefactor runs both paths against the same seed-fixed input every test run; asserts exact == on doubles (no algorithm change, no tolerance). Same fixture used in dotMatmulViaBackendSelector for direct interface check. |
| T-05-SC | Tampering | Maven dependency resolution | mitigate | 05-02 adds NO new dependencies to modules/numja/pom.xml. |

ASVS applicability: V5 Input Validation (HW-02 ceiling guard newly added in 05-02; HW-01 unchanged). V4 Access Control (HW-01 unchanged). Disposition policy: block on high.
</threat_model>

<verification>
- `mvn -q -pl modules/numja -am test` full numja suite green (90 pre-existing + new CpuThreadBackendGoldenTest 6/6 = 96 total, 0 failures, 1 pre-existing @Ignore).
- `grep -c "public static" modules/numja/src/main/java/numja/core/NumJa.java` = 61 (frozen).
- `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java` = 34 (frozen).
- `grep -c "<dependency>" modules/numja/pom.xml` unchanged.
- ParallelRegressionTest.java untouched, 4/4 green.
- `grep -c "BackendSelector.get" modules/numja/src/main/java/numja/core/ArrayOps.java` >= 1 (dot wired).
- `grep -c "BackendSelector.get" modules/numja/src/main/java/numja/core/NDArray.java` >= 4 (sum/min/max/prod wired).
- `grep -c "MAX_DISPATCH_N" modules/numja/src/main/java/numja/core/BackendSelector.java` >= 3 (declaration + checks).
</verification>

<success_criteria>
- 5+ hot paths (dot, sum, min, max, prod) routed through `BackendSelector.get().<op>(...)` — dot via ArrayOps, sum/min/max/prod via NDArray instance methods.
- CpuThreadBackendGoldenTest 6/6 green; demonstrates parity via two-path check on same seed-fixed input.
- ParallelRegressionTest 4/4 green; ratios within 30% gate of Phase 2 baseline (no observable drift from dispatch indirection).
- HW-02 MAX_DISPATCH_N ceiling enforced at the BackendSelector dispatcher boundary; oversized n throws IllegalArgumentException before any backend call.
- Public API counts unchanged (NumJa=61, ArrayOps=34); zero new Maven deps; stdlib-only preserved.
</success_criteria>

<output>
Create .planning/phases/05-hardware-abstraction-layer-gpu-poc/05-02-SUMMARY.md when done
</output>
