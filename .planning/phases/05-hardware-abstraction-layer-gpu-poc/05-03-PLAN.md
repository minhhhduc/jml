---
phase: 05-hardware-abstraction-layer-gpu-poc
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - pom.xml
  - bench/tornado-poc/pom.xml
  - bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java
  - docs/05-GPU-POC-RESULTS.md
  - docs/COLAB-RECIPE.md
autonomous: false
requirements: [HW-03]
security_enforcement: true
asvs_level: 1
disposition_policy: block on high

must_haves:
  truths:
    - "bench/tornado-poc/ Maven module exists with its own pom.xml (TornadoVM 5.2.0-jdk21 dep, maven.compiler.release=21)"
    - "Root pom.xml lists <module>bench/tornado-poc</module>; modules/numja/pom.xml has zero new dependencies"
    - "GemmBench.java runs 4096^2 GEMM on both CPU (FJP) and GPU (TornadoVM with explicit -Dtornado.device), reports speedup ratio + transfer % + device + JDK + hardware + environment (local|colab)"
    - "docs/05-GPU-POC-RESULTS.md decision doc exists, explicitly states which environment produced the numbers and a go/no-go recommendation for a future GPU backend milestone"
    - "docs/COLAB-RECIPE.md documents: install TornadoVM 5.2.0 on Colab Ubuntu runtime, upload jar, run, capture output, transfer results back"
  artifacts:
    - path: bench/tornado-poc/pom.xml
      provides: "TornadoVM 5.2.0-jdk21 dep, maven.compiler.release=21, parent = root pom"
      contains: "tornado"
    - path: bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java
      provides: "CPU + GPU 4096^2 GEMM bench with explicit device override"
      contains: "TaskGraph"
    - path: pom.xml
      provides: "Root pom adds <module>bench/tornado-poc</module>"
      contains: "bench/tornado-poc"
    - path: docs/05-GPU-POC-RESULTS.md
      provides: "Decision doc with go/no-go recommendation"
      contains: "go/no-go"
    - path: docs/COLAB-RECIPE.md
      provides: "Colab NVIDIA runtime execution recipe"
      contains: "TornadoVM"
  key_links:
    - from: "GemmBench CPU path"
      to: "numja.core.ArrayOps.dot"
      via: "static dispatch through HAL backend"
      pattern: "ArrayOps\\.dot|BackendSelector\\.get\\(\\)\\.matmul"
    - from: "GemmBench GPU path"
      to: "TornadoVM TaskGraph"
      via: "@Parallel kernels over flattened double[]"
      pattern: "@Parallel"
    - from: "GemmBench device override"
      to: "-Dtornado.device system property"
      via: "explicit opt-in, no auto-detect"
      pattern: "tornado\\.device"
---

<objective>
Stand up the TornadoVM GPU POC as a separate Maven module (`bench/tornado-poc/`) and produce a decision document with a go/no-go recommendation for a future GPU backend milestone. The POC runs 4096^2 GEMM on both CPU (FJP via the HAL) and GPU (TornadoVM), with explicit device override and labeled output. The same built jar must run on either the local dev box (CPU-only, GPU_ABSENT labeled) or Google Colab (NVIDIA CUDA runtime) — the choice of where to run is delegated to the user via a checkpoint.

Purpose: Answer HW-03 ("POC document + benchmark: it nhat 1 op chung minh GPU kha thi") with real numbers. Default-path modules/numja stays stdlib-only (zero new Maven deps). The POC module is isolated; nothing in production code paths references it.
Output: `bench/tornado-poc/` module with pom + GemmBench; root pom updated; `docs/05-GPU-POC-RESULTS.md` + `docs/COLAB-RECIPE.md`. Zero changes to `modules/numja/pom.xml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardware-abstraction-layer-gpu-poc/05-RESEARCH.md
@.planning/phases/05-hardware-abstraction-layer-gpu-poc/05-01-PLAN.md
@.planning/phases/05-hardware-abstraction-layer-gpu-poc/05-02-PLAN.md
@.planning/phases/01-baseline-benchmark/VERSIONS.md

<interfaces>
Key contracts the executor must match exactly (extracted from codebase — no re-exploration needed).

From .planning/phases/01-baseline-benchmark/VERSIONS.md (verified live 2026-08-26):
- TornadoVM version: v5.2.0-jdk21 (build for OpenJDK 21); dual-track also has v5.2.0-jdk25
- GPU backends: NVIDIA CUDA (Linux + Windows amd64), AMD OpenCL, Apple Metal, Intel Level Zero
- Phase 5 picks `-jdk21` to match current compile target
- Confidence: High

From pom.xml (root):
- groupId: com.numja, artifactId: numja, version: 0.1.0, packaging: pom
- Maven compiler source/target: 17
- <modules> section currently lists: modules/numja, modules/matplotlib, modules/seaborn, modules/pandas, modules/sklearn, bench

From bench/pom.xml (sibling module pattern):
- parent: com.numja:numja:0.1.0, relativePath: ../pom.xml
- artifactId: bench, packaging: jar
- maven-compiler-plugin source/target: 17 (POC overrides to 21)

From modules/numja/src/main/java/numja/core/ArrayOps.java (CPU matmul entry point used in GemmBench):
- public static NDArray dot(NDArray a, NDArray b) — routes through BackendSelector.get().matmul (post 05-02)
- POC CPU baseline: add numja-core as dependency and call ArrayOps.dot via the HAL (it routes through CpuThreadBackend.matmul which is EJML). This proves the production CPU path is the baseline, not a parallel impl.

From modules/numja/src/main/java/numja/core/NDArray.java:
- DMatrixRMaj-backed 2D array. POC can construct via `new NDArray(new DMatrixRMaj(N, N))` or via `ArrayOps.zeros(N, N)`.

Hard constraints carried from prior phases:
- Maven 3.9.15 not on PATH — use $HOME/.maven/maven-3.9.15/bin/mvn
- modules/numja/pom.xml stdlib-only (NO new deps; 05-03 only adds deps to bench/tornado-poc/pom.xml)
- JUnit 4, --release 17 for the main module; --release 21 ONLY for bench/tornado-poc
- Public API frozen (NumJa=61, ArrayOps=34 — unaffected by 05-03)

User-decision point (resolved in orchestrator's user_decisions): POC target is NVIDIA/CUDA. Local box (i7-1255U) has NO NVIDIA GPU. POC runs either (a) locally as CPU-only/GPU_ABSENT negative-result or (b) on Google Colab NVIDIA runtime. The SAME built artifact must run on either target.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bench/tornado-poc/ module with pom + GemmBench</name>
  <files>bench/tornado-poc/pom.xml, bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java, pom.xml, docs/COLAB-RECIPE.md</files>
  <read_first>
    - pom.xml  (root pom — add <module>bench/tornado-poc</module>)
    - bench/pom.xml  (sibling module pattern: parent, relativePath, plugin config)
    - .planning/phases/01-baseline-benchmark/VERSIONS.md  (TornadoVM 5.2.0-jdk21)
    - modules/numja/src/main/java/numja/core/ArrayOps.java  (CPU matmul entry)
    - modules/numja/src/main/java/numja/core/CpuThreadBackend.java  (matmul body — EJML)
    - modules/numja/pom.xml  (CONFIRM untouched after this task — re-grep <dependency> count)
 </read_first>
  <action>
    (1) Create `bench/tornado-poc/pom.xml` (new file). Contents:
      - parent: com.numja:numja:0.1.0, relativePath: ../../pom.xml (note TWO levels up — root is at repo root, this file is at bench/tornado-poc/pom.xml)
      - artifactId: tornado-poc, packaging: jar
      - properties: maven.compiler.release=21 (TornadoVM 5.2.0-jdk21 target)
      - dependencies:
        - com.numja:numja-core:0.1.0 (CPU baseline via ArrayOps.dot; matches the bench module's pattern)
        - uk.ac.manchester.tornado:tornado-api:5.2.0-jdk21 (TornadoVM core API for TaskGraph + @Parallel kernels)
        - uk.ac.manchester.tornado:tornado-runtime:5.2.0-jdk21 (runtime; needed at compile + bench)
        - uk.ac.manchester.tornado:tornado-drivers:5.2.0-jdk21 (driver layer; needed for actual GPU dispatch on Colab)
      - NO JUnit, NO JMH — this is a standalone runner, not a benchmark framework. POC is invoked via `java -cp ... bench.tornadopoc.GemmBench` after `mvn package`.
      - maven-compiler-plugin release=21 (NOT source/target=17 — must match TornadoVM bytecode)
      - Build a shaded jar with maven-shade-plugin matching bench/pom.xml pattern; mainClass = bench.tornadopoc.GemmBench.
      - Add `finalName>tornado-poc-jar</finalName>` so the produced jar is predictable: `bench/tornado-poc/target/tornado-poc-jar.jar`.

    (2) Create `bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java`. Contents:
      - Package: bench.tornadopoc
      - public class GemmBench with `public static void main(String[] args)`.
      - Read 3 system properties at the top (no auto-detect; HW-03):
        - `-Dtornado.device=<backend:device:idx>` (e.g. `nvidia:0:0`) — REQUIRED for GPU run; absent = CPU-only path
        - `-Dbench.env=<local|colab>` — labels the output environment
        - `-Dbench.size=4096` (default 4096; allow override for smoke testing)
      - Two code paths:
        - **CPU baseline**: use `ArrayOps.dot(a, b)` via numja-core dep — this routes through BackendSelector → CpuThreadBackend.matmul (EJML), proving the production CPU path is the baseline. Warm up with 1 iteration, then time 3 iterations; take median.
        - **GPU path** (if tornado.device is set): allocate flattened double[] of size N*N for A, B, out. Build a TornadoVM `TaskGraph` with @Parallel kernels for `out[i*N + j] = sum_k(a[i*N + k] * b[k*N + j])`. Warm up + time + measure transfer overhead (warm-up out transfer is amortised; cold transfer is `transfer_ms = System.nanoTime() before/after copyToDevice + copyToHost`). Wrap in try/catch for `UnsatisfiedLinkError` / `TornadoRuntimeException` — if GPU init fails, label output as `GPU_ABSENT` and exit 0 (not error) so the local CPU-only run still produces a labeled results doc.
      - **Output schema** (printed to stdout in a stable key=value format that the results doc parses):
        - env=<local|colab>
        - device=<cpu-thread|nvidia:0:0|GPU_ABSENT>
        - jdk=<java.version>
        - hardware=<os.name>+<os.arch>+Runtime.getRuntime().availableProcessors()+"cores"
        - tornado.device=<value of -Dtornado.device, or "unset">
        - size=4096
        - cpu_baseline_ms=<median of CPU iterations>
        - gpu_ms=<median of GPU iterations, or NA>
        - transfer_ms=<GPU transfer overhead, or NA>
        - speedup_ratio=<cpu_ms / gpu_ms, or NA>
        - transfer_pct=<transfer_ms / gpu_ms * 100, or NA>
        - result=GPU_ABSENT|OK|GPU_INIT_FAILED
        - verdict=GO|NO-GO|INSUFFICIENT_DATA  (computed: GO iff speedup_ratio>=2.0 && transfer_pct<50.0; NO-GO iff result=GPU_ABSENT or speedup_ratio<2.0; INSUFFICIENT_DATA otherwise)
      - HW-03 mitigation assertion: before timing GPU, if -Dtornado.device is set, query the runtime for the actual selected device and assert it matches the requested prefix; abort with stderr if mismatch. (TornadoVM API: `TornadoRuntime.getTornadoRuntime().getDriver(0)` or `TornadoRuntimeProvider.getTornadoRuntime().getDefaultDevice()`; refer to TornadoVM 5.2.0-jdk21 docs at https://github.com/beehive-lab/TornadoVM. The exact call may differ — if so, grep the installed TornadoVM jar at `$HOME/.m2/repository/uk/ac/manchester/tornado/...` after first mvn install to find the correct method.)

    (3) Update `pom.xml` (root): add `<module>bench/tornado-poc</module>` to the `<modules>` block. Insert it after `<module>bench</module>`. Do NOT touch any other section.

    (4) Create `docs/COLAB-RECIPE.md` (new file). The recipe documents:
      - Runtime: Google Colab notebook, Runtime > Change runtime type > Hardware accelerator = T4 GPU, OS = Ubuntu 22.04.
      - JDK install: `apt-get install -y openjdk-21-jdk` (TornadoVM 5.2.0-jdk21 needs JDK 21).
      - CUDA toolkit: install matching CUDA 12.x per TornadoVM 5.2.0 release notes.
      - TornadoVM install: download `tornadovm-5.2.0-jdk21-cuda-linux-amd64.tar.gz` from https://github.com/beehive-lab/TornadoVM/releases; extract; set `TORNADO_SDK` env var; source `setenv.sh`.
      - Upload jar: from local box, run the POC mvn package, then `scp bench/tornado-poc/target/tornado-poc-jar.jar <colab>:/content/` (or use Colab's file upload UI).
      - Run command: `java -cp /content/tornado-poc-jar.jar -Dtornado.device=nvidia:0:0 -Dbench.env=colab bench.tornadopoc.GemmBench`
      - Capture: copy stdout, paste into docs/05-GPU-POC-RESULTS.md under `## Colab (NVIDIA T4)`.

    (5) DO NOT modify `modules/numja/pom.xml`. After all edits, verify `grep -c "<dependency>" modules/numja/pom.xml` is unchanged (still 4 — ejml-core, ejml-ddense, commons-math3, junit-test).

    (6) Compile-only smoke test (skip tests since GemmBench has no tests; just package):
      `cd D:/unknown/projects/java_ml && "$HOME/.maven/maven-3.9.15/bin/mvn" -q -pl bench/tornado-poc -am package -DskipTests`
      Expect: BUILD SUCCESS and `bench/tornado-poc/target/tornado-poc-jar.jar` exists. If TornadoVM 5.2.0-jdk21 is not yet in the local Maven repo, the first build will fail with `Could not resolve dependency` — that's expected, and Task 2 (the user checkpoint) handles the actual run.
 </action>
  <verify>
    <automated>cd D:/unknown/projects/java_ml && "$HOME/.maven/maven-3.9.15/bin/mvn" -q -pl bench/tornado-poc -am package -DskipTests</automated>
 </verify>
  <done>bench/tornado-poc/ module compiles; pom.xml adds <module>bench/tornado-poc</module>; modules/numja/pom.xml unchanged; shaded jar produced at bench/tornado-poc/target/tornado-poc-jar.jar; docs/COLAB-RECIPE.md exists with the documented recipe</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run POC locally OR on Colab, write decision doc</name>
  <what-built>bench/tornado-poc module with GemmBench producing labeled stdout in key=value format. Local dev box has i7-1255U (Intel, no NVIDIA GPU), so the local run will label GPU_ABSENT and produce a CPU baseline number. Colab has NVIDIA T4 — running there with -Dtornado.device produces a real GPU vs CPU comparison</what-built>
  <how-to-verify>
    CHOOSE ONE PATH (or both, if time allows):

    PATH A — LOCAL CPU-ONLY RUN (default, fast, no GPU required):
    1. From repo root: `"$HOME/.maven/maven-3.9.15/bin/mvn" -q -pl bench/tornado-poc -am package -DskipTests`
    2. Run: `java -cp bench/tornado-poc/target/tornado-poc-jar.jar -Dbench.env=local bench.tornadopoc.GemmBench`
    3. Expect stdout: `result=GPU_ABSENT`, `verdict=NO-GO`, plus `cpu_baseline_ms=<number>`, `device=cpu-thread`.
    4. Copy stdout into `docs/05-GPU-POC-RESULTS.md` under a `## Local (i7-1255U, GPU_ABSENT)` section.

    PATH B — COLAB NVIDIA RUN (real GPU comparison):
    1. Follow `docs/COLAB-RECIPE.md`.
    2. After running on Colab, paste the labeled stdout into `docs/05-GPU-POC-RESULTS.md` under `## Colab (NVIDIA T4)` section.
    3. If the run produces `verdict=GO`, the section ends with "GO recommendation: pursue GPU backend milestone in v0.4.0+".

    After either path: confirm `docs/05-GPU-POC-RESULTS.md` has all six required fields (speedup_ratio, transfer_pct, device, jdk, hardware, environment) and an explicit go/no-go recommendation.

    EXPECTED OUTCOMES:
    - Local run on i7-1255U without -Dtornado.device: GPU_ABSENT labeled, NO-GO verdict (CPU baseline captured; this IS a useful negative result — confirms the local box can't test GPU without external hardware).
    - Colab run with -Dtornado.device=nvidia:0:0: real GPU vs CPU numbers. GO if speedup_ratio>=2.0 AND transfer_pct<50.0. NO-GO if either budget fails.
 </how-to-verify>
  <resume-signal>Type the path you ran (LOCAL or COLAB), paste the labeled stdout, and confirm docs/05-GPU-POC-RESULTS.md is written with all six required fields plus a go/no-go recommendation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| -Dtornado.device system property -> GemmBench GPU path | Untrusted device name crosses here; multi-GPU host may have wrong device selected |
| Uploaded POC jar on Colab -> local box | Untrusted Colab session touches local artifacts; out-of-scope for in-JVM trust model |
| Existing trust boundaries from 05-01 / 05-02 | JVM system properties -> BackendSelector; caller-supplied size -> BackendSelector dispatcher (carries forward) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-HW-03 | Tampering / Spoofing | GemmBench GPU path (multi-GPU host picks wrong device) | mitigate | Explicit `-Dtornado.device` opt-in (NO auto-detect). Before timing GPU, GemmBench queries the actual selected device and asserts it matches the requested prefix; aborts with stderr on mismatch. |
| T-05-HW-02 | Denial of Service | Carry-forward from 05-02 | accept | 05-02 added MAX_DISPATCH_N=10^9 ceiling at BackendSelector boundary. POC uses 4096^2 = 1.6e7 cells, well below ceiling. No new DoS surface in 05-03. |
| T-05-HW-01 | Elevation of Privilege | Carry-forward from 05-01 | accept | Allowlist + opt-in flag in BackendSelector. Unchanged. POC does not extend backend selection. |
| T-05-POC-DRIVER | Tampering (driver mismatch) | Colab CUDA toolkit version mismatch with TornadoVM 5.2.0-jdk21 | mitigate | docs/COLAB-RECIPE.md pins CUDA toolkit version + TornadoVM tarball URL from .planning/phases/01-baseline-benchmark/VERSIONS.md. |
| T-05-SC | Tampering | Maven dependency resolution | mitigate | 05-03 adds 3 deps (tornado-api, tornado-runtime, tornado-drivers) ONLY to bench/tornado-poc/pom.xml. modules/numja/pom.xml unchanged (stdlib-only preserved). |

ASVS applicability: V4 Access Control (HW-03 device selection is privileged). V5 Input Validation (carry-forward). Disposition policy: block on high.
</threat_model>

<verification>
- `mvn -q -pl bench/tornado-poc -am package -DskipTests` produces `bench/tornado-poc/target/tornado-poc-jar.jar`.
- Root pom.xml contains `<module>bench/tornado-poc</module>` (grep below).
- `modules/numja/pom.xml` `<dependency>` count unchanged (still 4).
- `docs/05-GPU-POC-RESULTS.md` exists and contains: speedup_ratio, transfer_pct, device, jdk, hardware, environment, go/no-go recommendation.
- `docs/COLAB-RECIPE.md` exists with: install TornadoVM 5.2.0-jdk21 on Ubuntu 22.04, upload jar, run command, capture output, transfer back.
- `grep -c "public static" modules/numja/src/main/java/numja/core/NumJa.java` = 61, `grep -c "public static" modules/numja/src/main/java/numja/core/ArrayOps.java` = 34 (frozen).
- `grep -c "<module>bench/tornado-poc</module>" pom.xml` = 1.
</verification>

<success_criteria>
- bench/tornado-poc/ module structure in place (pom + GemmBench + shaded jar build).
- Either local CPU-only labeled result OR Colab NVIDIA labeled result captured in docs/05-GPU-POC-RESULTS.md with explicit go/no-go.
- docs/COLAB-RECIPE.md authored with the documented execution recipe.
- Default-path modules/numja untouched beyond 05-02 wiring (NumJa=61, ArrayOps=34, stdlib-only deps).
</success_criteria>

<output>
Create .planning/phases/05-hardware-abstraction-layer-gpu-poc/05-03-SUMMARY.md when done
</output>
