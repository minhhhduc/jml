---
phase: 01-baseline-benchmark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pom.xml, modules/numja/pom.xml, .gitignore]
autonomous: false
requirements: [BENCH-01]
user_setup:
  - service: jdk-maven
    why: "Phase 1 cần JDK 11+ và Maven trên PATH để build nhánh dev"
    local_dev:
      - "Kiểm tra: mvn -v và java -version chạy được trong terminal"
---

<objective>
Khôi phục môi trường dev: switch sang nhánh `dev` (chứa source + Maven), verify `mvn test` pass với toàn bộ test suite hiện có.

Purpose: Mọi work sau này diễn ra trên nhánh dev; không build được thì không đo được gì.
Output: Working tree trên `dev`, `mvn test` xanh, ghi nhận trạng thái vào phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/research/SUMMARY.md

Source branch state:
@git-branch: dev (từ revent-backup — chứa modules/*/src, pom.xml, proguard.pro)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <decision>Switch working tree sang nhánh dev</decision>
  <context>Source chỉ tồn tại trên revent-backup. Working tree hiện ở main với uncommitted changes (.gitignore, scripts/build/, sample_advanced.csv). Claude bị hạn chế chạy git write commands trong phiên này.</context>
  <options>
    <option id="commit-then-switch"><name>Commit rồi switch (khuyến nghị)</name><pros>Giữ lại cleanup .gitignore + build scripts trên main trước khi chuyển</pros><cons>Một bước thủ công thêm</cons></option>
    <option id="stash-switch"><name>Stash rồi switch</name><pros>Nhanh hơn</pros><cons>Dễ quên pop stash</cons></option>
  </options>
  <resume-signal>Chạy xong và báo "switched to dev" kèm output của git status</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Verify Maven build + test suite trên nhánh dev</name>
  <files>(none — verification only)</files>
  <read_first>pom.xml, modules/numja/pom.xml, modules/sklearn/pom.xml</read_first>
  <action>Chạy `mvn -q clean test` từ repo root. Nếu lỗi dependency (jars local trong libs/), kiểm tra pom.xml đã khai báo system-scope hay install vào ~/.m2 qua libs/install_into_m2.ps1 (script có sẵn trên nhánh dev). Ghi lại: số test pass/fail, JDK version dùng, thời gian build. KHÔNG sửa code nguồn trong task này — chỉ làm build chạy được.</action>
  <verify>`mvn -q clean test` exit code 0; output chứa kết quả của NDArrayTest, LinAlgTest, NumJaTest, TestSklearn</verify>
  <acceptance_criteria>
    - `mvn clean test` exit 0
    - Output log chứa ít nhất 4 tên test class: NDArrayTest, LinAlgTest, NumJaTest, TestSklearn
  </acceptance_criteria>
  <done>Toàn bộ test suite hiện tại pass trên nhánh dev</done>
</task>

<task type="auto">
  <name>Task 2: Verify research gaps — version check TornadoVM / Vector API / EJML-mt</name>
  <files>.planning/phases/01-baseline-benchmark/VERSIONS.md</files>
  <read_first>.planning/research/SUMMARY.md (phần "Gaps cần verify")</read_first>
  <action>Tra web (WebSearch/WebFetch hoặc ctx7): (1) TornadoVM version mới nhất + JDK tương thích + GPU backend hỗ trợ; (2) trạng thái Java Vector API trên JDK 21–25 (incubator hay final); (3) EJML multithreaded artifact (`ejml-core` mt variant) version tương thích 0.43.1. Ghi kết quả + URL nguồn vào VERSIONS.md, mỗi mục kèm confidence. Đây là input bắt buộc cho Phase 2 (CPU/SIMD) và Phase 5 (GPU POC).</action>
  <verify>VERSIONS.md tồn tại, chứa cả 3 mục với URL nguồn</verify>
  <acceptance_criteria>
    - VERSIONS.md chứa "TornadoVM", "Vector API", "EJML" với version cụ thể + URL
    - Mỗi mục có nhãn confidence (High/Medium/Low)
  </acceptance_criteria>
  <done>3 research gaps đóng, có bằng chứng nguồn</done>
</task>

</tasks>

<verification>
- [ ] `git branch --show-current` trả về `dev`
- [ ] `mvn clean test` exit 0
- [ ] `.planning/phases/01-baseline-benchmark/VERSIONS.md` tồn tại với 3 research gaps đóng
</verification>

<success_criteria>
- Working tree trên nhánh dev, build + test xanh
- Research gaps từ SUMMARY.md đã verify với nguồn
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-benchmark/01-01-SUMMARY.md`
</output>
