---
phase: 01-baseline-benchmark
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified: [modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java, bench/src/main/java/bench/AccuracyProbe.java]
autonomous: true
requirements: [ACC-03]
---

<objective>
Dựng golden-value test infrastructure: so kết quả NumJa với tham chiếu NumPy (golden values precomputed) cho các op benchmarked.

Purpose: Phase 3 hardening accuracy cần có "đúng" được định nghĩa máy móc từ trước — test infra này là cái thước.
Output: GoldenReferenceTest chạy được + quy trình sinh golden values documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-baseline-benchmark/01-01-SUMMARY.md
@modules/sklearn/src/test/java/sklearn/TestSklearn.java
@modules/numja/src/test/java/com/numja/core/NDArrayTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sinh golden values từ NumPy reference</name>
  <files>bench/src/test/resources/golden/*.json, bench/GOLDEN.md</files>
  <read_first>.planning/phases/01-baseline-benchmark/VERSIONS.md, dist/datasets/iris.csv</read_first>
  <action>Viết script Python nhỏ (scripts/golden/generate_golden.py, dùng numpy — .venv có sẵn) sinh JSON golden values với seed cố định (seed=42) cho: matmul 256x256, sum/mean double[10⁶], softmax vector[1000] giá trị biên ±700, LinearRegression fit iris-like data. Format JSON: {op, inputs_hash, expected[], tolerance_rel}. GOLDEN.md ghi quy trình regenerate. Tolerance ghi rõ từng op (ví dụ rel 1e-12 cho matmul float64, 1e-15 cho sum tuần tự).</action>
  <verify>`python scripts/golden/generate_golden.py` chạy ra file JSON; mỗi JSON chứa "tolerance_rel"</verify>
  <acceptance_criteria>
    - scripts/golden/generate_golden.py tồn tại và chạy được
    - ≥4 golden JSON files trong bench/src/test/resources/golden/
    - Mỗi file chứa tolerance_rel cụ thể
  </acceptance_criteria>
  <done>Golden values có nguồn tái tạo được</done>
</task>

<task type="tdd">
  <name>Task 2: GoldenReferenceTest đọc golden values và assert NumJa results</name>
  <files>modules/sklearn/src/test/java/sklearn/accuracy/GoldenReferenceTest.java</files>
  <read_first>bench/src/test/resources/golden/*.json (structure), modules/sklearn/src/test/java/sklearn/TestSklearn.java (test style hiện tại)</read_first>
  <action>RED: viết test trước — JUnit test load từng golden JSON, dựng input cùng seed qua NumJa API, gọi op, assert relative error ≤ tolerance_rel. GREEN: chạy `mvn test` — nếu NumJa sai số vượt tolerance ở op nào, GHI NHẬN vào test report nhưng KHÔNG sửa thuật toán (việc đó là Phase 3); test đánh dấu @Tag("golden") để tách riêng. Test phải pass về mặt cơ chế (chạy được, không crash) dù có assertion soft-fail.</action>
  <verify>`mvn test -Dgroups=golden` chạy hết không crash; report liệt kê pass/fail per op vs NumPy</verify>
  <acceptance_criteria>
    - GoldenReferenceTest.java chứa "@Tag" annotation
    - Test đọc file JSON từ resources/golden/
    - mvn test exit 0 (soft-fail mode, gap ghi nhận vào SUMMARY)
  </acceptance_criteria>
  <done>Cái thước accuracy vận hành — biết chính xác NumJa lệch NumPy bao nhiêu ở từng op</done>
</task>

</tasks>

<verification>
- [ ] `mvn test -Dgroups=golden` chạy được toàn bộ
- [ ] Report cho thấy relative error per op so NumPy
- [ ] GOLDEN.md mô tả cách regenerate
</verification>

<success_criteria>
- Accuracy measurement infrastructure hoạt động trước khi Phase 3 tối ưu
- Baseline sai số đã biết, ghi trong SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-benchmark/01-03-SUMMARY.md`
</output>
