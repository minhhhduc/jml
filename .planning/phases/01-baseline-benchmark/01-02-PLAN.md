---
phase: 01-baseline-benchmark
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [bench/pom.xml, bench/src/main/java/bench/CoreBench.java, bench/src/main/java/bench/PandasBench.java, bench/src/main/java/bench/SklearnBench.java, bench/README.md]
autonomous: true
requirements: [BENCH-01, BENCH-02]
---

<objective>
Dựng JMH benchmark harness đo baseline cho core ops: NDArray matmul/elementwise/reduce, DataFrame groupby, sklearn fit/predict — và ghi baseline số liệu.

Purpose: PERF-01 đứng đầu roadmap vì không có số liệu thì mọi tối ưu là phỏng đoán; các phase 2–5 đều so với baseline này.
Output: Maven module `bench/` với JMH harness + file baseline kết quả committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-baseline-benchmark/01-01-SUMMARY.md
@modules/numja/src/main/java/numja/core/NDArray.java
@modules/numja/src/main/java/numja/linalg/LinAlg.java
@modules/pandas/src/main/java/pandas/DataFrame.java
@modules/sklearn/src/main/java/sklearn/linear_model/LinearRegression.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo Maven module bench/ với JMH</name>
  <files>bench/pom.xml</files>
  <read_first>pom.xml (root), modules/numja/pom.xml</read_first>
  <action>Tạo module `bench` theo đúng pattern pom của modules hiện có: parent = root pom, dependencies = JMH core + generator (`org.openjdk.jmh:jmh-core`, `jmh-generator-annprocess`, dùng version mới nhất ổn định — tra từ VERSIONS.md/central) + module deps numja/pandas/sklearn. Java release khớp root pom. KHÔNG thêm JMH vào jars phát hành — bench là module nội bộ, không nằm trong ProGuard build.</action>
  <verify>`mvn -q -pl bench compile` exit 0</verify>
  <acceptance_criteria>
    - bench/pom.xml chứa "jmh-core" và "jmh-generator-annprocess"
    - bench/pom.xml khai báo parent trỏ về root pom
  </acceptance_criteria>
  <done>Module bench compile được trong build tree hiện tại</done>
</task>

<task type="auto">
  <name>Task 2: Viết benchmark classes cho core ops</name>
  <files>bench/src/main/java/bench/CoreBench.java, bench/src/main/java/bench/PandasBench.java, bench/src/main/java/bench/SklearnBench.java</files>
  <read_first>modules/numja/src/main/java/numja/core/NDArray.java, modules/numja/src/main/java/numja/linalg/LinAlg.java, modules/pandas/src/main/java/pandas/DataFrame.java, examples/TestComprehensive.java (cách dựng dữ liệu mẫu)</read_first>
  <action>Viết 3 class @Benchmark với JMH annotations chuẩn (Mode.AverageTime, @Warmup 5 iterations 1s, @Measurement 5 iterations 1s, @Fork 1, Blackhole consume mọi kết quả để chống dead-code elimination):
  - CoreBench: matmul NxN với N ∈ {256, 1024}; elementwise add/mul trên double[10⁶] và double[10⁷]; reduce sum/mean trên double[10⁷]
  - PandasBench: read_csv dist/datasets/california_housing.csv; groupby mean trên cột category của titanic.csv
  - SklearnBench: LinearRegression.fit + predict trên california_housing (20k rows sample); KMeans.fit iris
  Main class dùng `org.openjdk.jlh.Main` style runner với `include` regex per-suite.</action>
  <verify>`mvn -q -pl bench package` exit 0; chạy smoke: `java -jar bench/target/benchmarks.jar "CoreBench.matmul256" -f 0 -wi 1 -i 1` in ra score</verify>
  <acceptance_criteria>
    - Cả 3 file tồn tại, mỗi class chứa annotation @Benchmark ít nhất 2 method
    - Mọi @Benchmark method consume kết quả qua Blackhole
  </acceptance_criteria>
  <done>Benchmark compile + chạy smoke test ra số</done>
</task>

<task type="auto">
  <name>Task 3: Chạy full baseline và ghi kết quả</name>
  <files>.planning/phases/01-baseline-benchmark/BASELINE.md</files>
  <read_first>bench/README.md (nếu Task 2 tạo), .planning/codebase/TESTING.md</read_first>
  <action>Chạy toàn bộ suite: `java -jar bench/target/benchmarks.jar` với thời gian rút gọn (-wi 3 -i 3 -f 1). Ghi kết quả vào BASELINE.md dạng bảng: op | input size | score | unit | error. Ghi kèm metadata máy: CPU model, cores, RAM, JDK version, OS. Đây là con số tham chiếu cho Phase 2–5.</action>
  <verify>BASELINE.md tồn tại chứa "matmul", "elementwise", "groupby", "fit" scores + metadata máy</verify>
  <acceptance_criteria>
    - BASELINE.md có bảng ≥8 benchmark results
    - BASELINE.md ghi CPU/JDK/OS metadata
  </acceptance_criteria>
  <done>Baseline đo được và lưu lại làm mốc so sánh</done>
</task>

</tasks>

<verification>
- [ ] `mvn -pl bench compile` pass
- [ ] Benchmark jar chạy được, in scores
- [ ] BASELINE.md có đủ ≥8 ops + metadata
</verification>

<success_criteria>
- JMH harness một lệnh chạy được toàn bộ suite
- Baseline numbers ghi lại, sẵn sàng làm mốc Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-benchmark/01-02-SUMMARY.md`
</output>
