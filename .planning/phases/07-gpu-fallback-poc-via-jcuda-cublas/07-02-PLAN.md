---
phase: 07-gpu-fallback-poc-via-jcuda-cublas
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - notebooks/colab-jcuda-poc.ipynb
  - .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md
autonomous: false
requirements: [GPU-04]
user_setup:
  - service: Google Colab
    why: "A real NVIDIA T4 is required to measure the native cuBLAS POC."
    dashboard_config:
      - task: "Select a T4 GPU runtime, then run all cells in notebooks/colab-jcuda-poc.ipynb."
        location: "Colab Runtime > Change runtime type"
must_haves:
  truths:
    - "A maintainer can run the committed Linux-targeted shaded JAR on a Colab T4 with CUDA 12.6 cuBLAS available."
    - "The notebook rejects an incomplete or failed GPU execution rather than presenting a CPU-only result as success."
    - "The phase contains evidence with an explicit GPU-04 GO or NO-GO conclusion from the actual T4 output."
  artifacts:
    - path: "notebooks/colab-jcuda-poc.ipynb"
      provides: "T4 preflight, pinned dependency build, telemetry, benchmark runner, and decision extraction"
      contains: "libcublas.so.12"
    - path: ".planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md"
      provides: "Actual Colab result evidence and explicit GPU-04 conclusion"
      contains: "Conclusion"
  key_links:
    - from: "notebooks/colab-jcuda-poc.ipynb"
      to: "bench/jcuda-poc/target/jcuda-poc-jar.jar"
      via: "Colab Linux Maven build then java -jar execution"
      pattern: "jcuda-poc-jar\\.jar"
    - from: "notebooks/colab-jcuda-poc.ipynb"
      to: "/tmp/poc-gpu.log"
      via: "stderr-preserving tee and result=OK hard gate"
      pattern: "2>&1 \\| tee /tmp/poc-gpu\\.log"
    - from: "07-RESULTS.md"
      to: "GemmBench output"
      via: "recorded key=value metrics and explicit verdict"
      pattern: "cpu_vs_gpu_frob_rel_err"
---

<objective>
## Phase Goal

**As a** NumJa maintainer, **I want to** run an isolated JCuda/cuBLAS GEMM proof of concept, **so that** I can decide from measured numerical and performance gates whether a future GPU integration is justified.

Produce a safe Colab execution path and preserve the actual decision evidence after the committed POC implementation exists. The human T4 run is a blocking verification step, not a request for the user to implement anything.

Purpose: turn the benchmark code into a reproducible, auditable GPU-04 conclusion.
Output: dedicated Colab notebook and completed phase results evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-PATTERNS.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-VALIDATION.md
@notebooks/colab-gpu-poc.ipynb
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-01-SUMMARY.md

<interfaces>
The Plan 01 JAR contract:
- artifact: `bench/jcuda-poc/target/jcuda-poc-jar.jar`
- entry point: `bench.jcudapoc.GemmBench`
- run properties: `bench.env=colab`, `bench.size=4096`
- success marker: `result=OK`
- numeric output: `cpu_vs_gpu_frob_rel_err`, `cpu_baseline_ms`, `gpu_ms`, `transfer_ms`, `speedup_ratio`, `transfer_pct`, `verdict`

The notebook must source `/tmp/poc-env.sh` in every shell cell that needs Java, Maven, or CUDA 12.6 library paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the reproducible Colab T4 execution slice</name>
  <files>notebooks/colab-jcuda-poc.ipynb</files>
  <read_first>@notebooks/colab-gpu-poc.ipynb; @bench/jcuda-poc/pom.xml; @bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-PATTERNS.md</read_first>
  <action>Create a new notebook only at `notebooks/colab-jcuda-poc.ipynb`; do not modify the user-authored `notebooks/colab-gpu-poc.ipynb`, per D-05. Retain the fixed notebook structure: T4 runtime instructions, `%%bash` setup, T4 `nvidia-smi` preflight, NumPy magnitude-only reference, telemetry sampler, key=value decision summary, and rubric. Omit the credential-bearing outbound curl cell entirely—never reproduce its URL, token, or request. Use `set -eu` in piped cells, never `pipefail` with `head`; create `/tmp/poc-env.sh` exporting JDK 21, Maven 3.9.15, and `LD_LIBRARY_PATH=/usr/local/cuda-12.6/lib64:${LD_LIBRARY_PATH:-}`. Install CUDA 12.6 with the existing keyring/apt pattern, then require `test -e "$CUDA12_LIB/libcublas.so.12"` before Java runs. Clone the Phase 7 branch over HTTPS, build `modules/numja` and `bench/jcuda-poc` on Colab Linux with Maven Central resolution, and invoke `java -Dbench.env=colab -Dbench.size="$N" -jar bench/jcuda-poc/target/jcuda-poc-jar.jar`; never upload or build a Windows JAR, install jars manually, declare classifiers, or use a Tornado launcher. Preserve native error visibility using `2>&1 | tee /tmp/poc-gpu.log`, sample `nvidia-smi` during the GPU run, and hard-fail the notebook runner unless `grep -q '^result=OK$' /tmp/poc-gpu.log` succeeds. The decision cell must extract the complete D-04 schema plus telemetry and explain that JCuda `gpu_ms` is kernel-only while `transfer_ms` separately covers H2D+D2H.</action>
  <acceptance_criteria>The new notebook is valid JSON; it contains `/tmp/poc-env.sh`, `set -eu`, `libcublas.so.12`, `jcuda-poc-jar.jar`, `2>&1 | tee /tmp/poc-gpu.log`, and a literal `grep -q '^result=OK$'` gate; it contains no `tokenharbor`, `Authorization: Bearer`, `install:install-file`, `-Dtornado.device`, or `pipefail` text; the Phase 5 notebook is not changed.</acceptance_criteria>
  <verify><automated>python -c "import json; p='notebooks/colab-jcuda-poc.ipynb'; nb=json.load(open(p, encoding='utf-8')); s=''.join(''.join(c.get('source', [])) for c in nb['cells']); assert '/tmp/poc-env.sh' in s and 'libcublas.so.12' in s and 'jcuda-poc-jar.jar' in s and \"grep -q '^result=OK$'\" in s and '2>&1 | tee /tmp/poc-gpu.log' in s and 'set -eu' in s; assert all(x not in s for x in ('tokenharbor', 'Authorization: Bearer', 'install:install-file', '-Dtornado.device', 'pipefail'))"</automated></verify>
  <done>A standalone, secret-free notebook builds the Linux-native shaded POC, preserves native stderr, collects T4 telemetry, and refuses to treat anything except `result=OK` as a successful GPU run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the committed JCuda POC on a Colab T4 and record its conclusion</name>
  <files>notebooks/colab-jcuda-poc.ipynb, .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md</files>
  <read_first>@notebooks/colab-jcuda-poc.ipynb; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-VALIDATION.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-01-SUMMARY.md</read_first>
  <action>After Plan 01 is committed and Task 1 has produced the dedicated notebook, run that committed branch in a Google Colab T4 runtime and write the actual evidence into `07-RESULTS.md`. Record the execution date; T4/driver/JDK/CUDA environment; complete CPU/GPU key=value blocks; telemetry; Frobenius/max/MAE values; speedup and transfer calculations; exact output verdict; explicit `Conclusion: GO` or `Conclusion: NO-GO`; the fact that production paths remain untouched; and the fact that JCuda `gpu_ms` is kernel-only (`cublasDgemm + cudaDeviceSynchronize`) while `transfer_ms` is H2D+D2H, unlike Phase 5 Tornado timing. Per D-09, a native/device/numeric/performance failure ends in a documented NO-GO and no third backend is attempted.</action>
  <what-built>The committed, isolated JCuda/cuBLAS POC and a Colab T4 notebook that verifies CUDA 12.6, builds Linux-native JCuda dependencies, captures telemetry, and hard-gates a successful GPU computation.</what-built>
  <how-to-verify>
    1. Confirm Plan 01 is committed and the Phase 7 branch contains `notebooks/colab-jcuda-poc.ipynb`; only then open it in Google Colab.
    2. Set Runtime > Change runtime type > T4 GPU, then Run all cells.
    3. Confirm the setup logs show a T4, CUDA 12.6 `libcublas.so.12`, and a Colab-built `jcuda-poc-jar.jar`.
    4. Inspect `/tmp/poc-gpu.log` and retain its complete key=value block plus telemetry. An `result=OK` run must include `cpu_vs_gpu_frob_rel_err <= 1e-9`; compare the emitted `speedup_ratio` and `transfer_pct` with the exact GPU-04 thresholds.
    5. Populate `07-RESULTS.md` with the actual data. Record `Conclusion: NO-GO` if device/native/numeric/perf gates fail, per D-09; no third backend is attempted.
  </how-to-verify>
  <acceptance_criteria>`07-RESULTS.md` contains a dated Colab T4 environment, retained metrics/evidence, an unambiguous GO or NO-GO conclusion, the timing-composition caveat, and confirmation of no production changes. GO is accepted only with `result=OK`, Frobenius <= 1e-9, speedup >= 2.0, and transfer_pct < 50.0; any real failure is accepted only as an explicit NO-GO with its evidence.</acceptance_criteria>
  <verify><automated>grep -Eq '^Conclusion: (GO|NO-GO)$' .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md &amp;&amp; grep -q 'cpu_vs_gpu_frob_rel_err' .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md &amp;&amp; grep -q 'kernel-only' .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md</automated></verify>
  <done>Results evidence holds enough actual Colab data to close GPU-04 as GO or NO-GO, and every NO-GO failure retains its native/CUDA or gate evidence instead of masquerading as success.</done>
  <resume_signal>Type `approved` after committing the completed results evidence, or provide the observed Colab logs/issues so the POC can be corrected or concluded NO-GO.</resume_signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Colab | The notebook checks out and executes the committed phase branch in a cloud runtime. |
| Colab environment → native JCuda | CUDA libraries and self-extracted JCuda JNI binaries execute native code. |
| Notebook output → results evidence | Measured key=value logs inform a future product decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Colab dependency build | mitigate | Build from pinned Maven Central dependencies on the Phase 7 branch; no jar downloads, manual local installs, or classifiers. |
| T-07-02 | Information Disclosure | Notebook cells | mitigate | Omit the Phase 5 credential-bearing curl cell; do not embed tokens or secrets; clone only over HTTPS. |
| T-07-03 | Elevation | CUDA/JCuda native boundary | mitigate | Preflight T4 and CUDA 12.6 `libcublas.so.12`, preserve stderr through tee, and execute only the isolated POC jar. |
| T-07-04 | Repudiation | `07-RESULTS.md` conclusion | mitigate | Preserve key=value logs, telemetry, environment metadata, exact gates, and stated timing composition before conclusion. |
| T-07-SC | Tampering | Maven package installs | mitigate | Use only Plan 01's legitimacy-audited exact packages; block any unaudited installation path. |
</threat_model>

<verification>
Validate the notebook JSON/static contract before the checkpoint. At the checkpoint, retain actual Colab output in `07-RESULTS.md`; run `mvn test` after the results file is committed to reconfirm the production suite remains intact.
</verification>

<success_criteria>
- The dedicated notebook is secret-free, Linux-builds the shaded JCuda jar, and validates CUDA 12.6/cuBLAS before execution.
- The notebook preserves stderr, T4 telemetry, and uses `result=OK` as a hard completion gate.
- `07-RESULTS.md` contains actual run evidence and clearly closes GPU-04 as GO or NO-GO, including the JCuda/Tornado timing-composition distinction.
- The user-modified Phase 5 notebook remains untouched.
</success_criteria>

<output>
Create `.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-02-SUMMARY.md` when done.
</output>
