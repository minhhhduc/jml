---
phase: 07-gpu-fallback-poc-via-jcuda-cublas
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - notebooks/colab-jcuda-poc.ipynb
  - .planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md
autonomous: true
requirements: [GPU-04]
must_haves:
  truths:
    - "A future maintainer has a secret-free T4 notebook and precise deferred execution record for the isolated POC."
    - "The authored notebook refuses to present incomplete GPU output as `result=OK`."
    - "A future local or Colab run has explicit non-masking criteria for recording GO or NO-GO evidence."
  artifacts:
    - path: "notebooks/colab-jcuda-poc.ipynb"
      provides: "Unexecuted T4 preflight, build, telemetry, benchmark, and hard-gate notebook source"
      contains: "libcublas.so.12"
    - path: ".planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md"
      provides: "Deferred evidence template and truthful future-run protocol"
      contains: "PENDING FUTURE EXECUTION"
  key_links:
    - from: "notebooks/colab-jcuda-poc.ipynb"
      to: "bench/jcuda-poc/target/jcuda-poc-jar.jar"
      via: "Documented future Colab Linux build and java invocation"
      pattern: "jcuda-poc-jar\\.jar"
    - from: "notebooks/colab-jcuda-poc.ipynb"
      to: "/tmp/poc-gpu.log"
      via: "stderr-preserving tee and hard result gate"
      pattern: "2>&1 \\| tee /tmp/poc-gpu\\.log"
    - from: "07-RESULTS.md"
      to: "GemmBench output"
      via: "Future recorded D-04 metrics and conclusion"
      pattern: "cpu_vs_gpu_frob_rel_err"
---

<objective>
## Phase Goal

**As a** NumJa maintainer, **I want to** author the isolated JCuda/cuBLAS POC's Colab materials, **so that** a future authorized T4 run can decide whether GPU integration is justified.

Deliver notebook and evidence-template source only. Per D-03 (revised 2026-09-11): do not manually download JCuda JARs, run Maven, use `install:install-file`, resolve Maven dependencies, or execute the Colab notebook in this code-only session. A future build/Colab run may resolve the Plan 01 pinned Maven Central declarations.

Purpose: provide reproducible, secret-free future-run material without turning execution into a blocking task.
Output: `notebooks/colab-jcuda-poc.ipynb` and an unfilled deferred results template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-PATTERNS.md
@notebooks/colab-gpu-poc.ipynb
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-01-SUMMARY.md

<interfaces>
- Future artifact: `bench/jcuda-poc/target/jcuda-poc-jar.jar`; entry point: `bench.jcudapoc.GemmBench`; properties: `bench.env=colab`, `bench.size=4096`.
- Complete D-04 schema: `cpu_baseline_ms`, `gpu_ms`, `transfer_ms`, `speedup_ratio`, `transfer_pct`, `device`, `result`, `verdict`, `env`, `jdk`, `hardware`, `size`, and `cpu_vs_gpu_frob_rel_err`.
- Shell cells requiring Java, Maven, or CUDA must source `/tmp/poc-env.sh`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Author the secret-free, unexecuted Colab T4 notebook</name>
  <files>notebooks/colab-jcuda-poc.ipynb</files>
  <read_first>@notebooks/colab-gpu-poc.ipynb; @bench/jcuda-poc/pom.xml; @bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md</read_first>
  <action>Create only `notebooks/colab-jcuda-poc.ipynb`; do not modify `notebooks/colab-gpu-poc.ipynb`, per D-05. Author, but do not execute, T4 runtime instructions; `%%bash` setup; T4 `nvidia-smi` preflight; NumPy magnitude-only reference; telemetry sampling; key=value decision summary; and rubric. Omit the credential-bearing outbound curl cell entirely: never reproduce its URL, token, or request. Use `set -eu`, never `pipefail` with `head`; author `/tmp/poc-env.sh` for JDK 21, Maven 3.9.15, and `LD_LIBRARY_PATH=/usr/local/cuda-12.6/lib64:${LD_LIBRARY_PATH:-}`. The future-only setup path may install CUDA 12.6 and must require `test -e "$CUDA12_LIB/libcublas.so.12"` before Java. Its future-only Linux build path clones the Phase 7 branch over HTTPS, invokes Maven for `modules/numja` and `bench/jcuda-poc`, then runs the JAR with `2>&1 | tee /tmp/poc-gpu.log`; do not author JAR downloading, manual installation, classifiers, Windows-JAR upload, or Tornado launcher use. The hard gate must fail unless `grep -q '^result=OK$' /tmp/poc-gpu.log` succeeds, while preserving logs for D-09 NO-GO evidence. State that JCuda `gpu_ms` is kernel-only and `transfer_ms` is H2D+D2H.</action>
  <verify><automated>python -c "import json; from pathlib import Path; p=Path('notebooks/colab-jcuda-poc.ipynb'); nb=json.loads(p.read_text(encoding='utf-8')); s=''.join(''.join(c.get('source', [])) for c in nb['cells']); assert all(x in s for x in ('/tmp/poc-env.sh','libcublas.so.12','jcuda-poc-jar.jar',\"grep -q '^result=OK$'\",'2>&1 | tee /tmp/poc-gpu.log','set -eu')); assert all(x not in s for x in ('tokenharbor','Authorization: Bearer','install:install-file','-Dtornado.device','pipefail'))"</automated></verify>
  <done>The new valid-JSON notebook is secret-free and unexecuted; it contains future-only T4 build/run instructions, stderr capture, telemetry, and a strict `result=OK` completion gate.</done>
</task>

<task type="auto">
  <name>Task 2: Author the deferred evidence template and non-masking run protocol</name>
  <files>.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md</files>
  <read_first>@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-VALIDATION.md</read_first>
  <action>Create an unfilled results template headed `Status: PENDING FUTURE EXECUTION`; it must not claim measurements or a GO/NO-GO conclusion. Include slots for execution date, commit, T4/driver/JDK/CUDA environment, complete D-04 key=value block, stderr, telemetry, Frobenius/max/MAE errors, speedup/transfer calculations, timing-composition caveat, and a future `Conclusion: GO|NO-GO`. Record D-03 (revised 2026-09-11) verbatim in the scope note: this session does not download JARs, run Maven, resolve dependencies, or run Colab. Add a deferred, manual-only protocol: first a future Maven package command must succeed; only after success may a local JAR be run with stdout/stderr captured. Require every D-04 key; require exactly one local `result=GPU_ABSENT` or `result=GPU_INIT_FAILED`, exactly one `verdict=NO-GO`, and no `result=OK`. Require any future negative legacy-JCublas check to be a standalone failure-on-match conditional, with no exit-status fallback that can hide a failed preceding command. For a future Colab run, preserve the hard-gated log and document `result=OK` plus D-06/D-07 measurements for a GO, or retain native/device/numeric/performance evidence for NO-GO. Per D-09, do not try a third backend. Do not perform any of these future instructions now.</action>
  <verify><automated>python -c "from pathlib import Path; s=Path('.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESULTS.md').read_text(); assert 'Status: PENDING FUTURE EXECUTION' in s and 'D-03 (revised 2026-09-11)' in s; assert all(x in s for x in ('cpu_baseline_ms','cpu_vs_gpu_frob_rel_err','GPU_ABSENT','GPU_INIT_FAILED','verdict=NO-GO','result=OK','Maven package command must succeed','Conclusion: GO|NO-GO')); assert 'Conclusion: GO\n' not in s and 'Conclusion: NO-GO\n' not in s"</automated></verify>
  <done>The repository contains only a pending evidence template and exact deferred validation rules; no Colab/package execution, measured result, or human checkpoint is required in this session.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → future Colab | A future notebook checkout executes the committed phase branch in a cloud runtime. |
| Future Colab → native JCuda | CUDA libraries and self-extracted JNI binaries execute native code. |
| Future notebook output → results evidence | Key=value logs inform a future product decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Future Colab dependency resolution | mitigate | Pin audited coordinates in Plan 01 and prohibit manual JAR/local-install paths under D-03 (revised 2026-09-11). |
| T-07-02 | Information Disclosure | Notebook cells | mitigate | Exclude the credential-bearing curl cell and all tokens; future clone uses HTTPS only. |
| T-07-03 | Elevation | Future CUDA/JCuda native boundary | mitigate | Future notebook preflights T4 and CUDA 12.6, retains stderr, and runs only the isolated POC JAR. |
| T-07-04 | Repudiation | Future results conclusion | mitigate | Template requires complete key=value logs, telemetry, environment metadata, thresholds, and timing composition. |
| T-07-SC | Tampering | Future Maven package resolution | mitigate | Use the legitimacy-audited exact packages only; this session performs no package resolution. |
</threat_model>

<verification>
Validate JSON and template text only in this code-only session; do not run Maven, download JARs, resolve dependencies, or execute Colab. Future execution is deferred/manual and non-blocking: package success precedes any JAR assertion; local output is captured before asserting all D-04 fields, exactly one allowed local NO-GO result, and exactly one NO-GO verdict. A failed packaging/run command must stop the protocol, never be hidden by an exit-status fallback.
</verification>

<success_criteria>
- The dedicated, unexecuted notebook is secret-free and does not modify the Phase 5 notebook.
- The pending results template accurately defers all runtime work while retaining exact future evidence requirements.
- No source task or checkpoint requires package installation, Maven execution, or Colab execution in this session.
</success_criteria>

<output>
Create `.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-02-SUMMARY.md` when done.
</output>
