---
phase: 07-gpu-fallback-poc-via-jcuda-cublas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - bench/jcuda-poc/pom.xml
  - bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java
autonomous: true
requirements: [GPU-04]
must_haves:
  truths:
    - "The repository contains an isolated JCuda/cuBLAS POC whose pinned dependencies can be resolved by a future build."
    - "The authored no-device and initialization-failure paths specify complete, truthful NO-GO key=value output."
    - "The authored CUDA path compares cuBLAS Dgemm with the live ArrayOps.dot CPU baseline and applies the numerical and performance gates."
  artifacts:
    - path: "bench/jcuda-poc/pom.xml"
      provides: "Pinned Maven Central JCuda/cuBLAS declarations and shaded POC JAR configuration"
      contains: "<artifactId>jcublas</artifactId>"
    - path: "bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java"
      provides: "CPU-versus-cuBLAS Dgemm benchmark with complete keyed result schema"
      contains: "JCublas2.cublasDgemm"
    - path: "pom.xml"
      provides: "Reactor access to the isolated POC module"
      contains: "<module>bench/jcuda-poc</module>"
  key_links:
    - from: "pom.xml"
      to: "bench/jcuda-poc/pom.xml"
      via: "Maven reactor module declaration"
      pattern: "<module>bench/jcuda-poc</module>"
    - from: "bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java"
      to: "numja.core.ArrayOps.dot"
      via: "CPU reference measurement"
      pattern: "ArrayOps\\.dot"
    - from: "bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java"
      to: "JCublas2.cublasDgemm"
      via: "B-first OP_N row-major mapping"
      pattern: "cublasDgemm"
---

<objective>
## Phase Goal

**As a** NumJa maintainer, **I want to** author an isolated JCuda/cuBLAS GEMM proof of concept, **so that** a future build and T4 run can decide whether GPU integration is justified.

Deliver source only: reactor wiring, pinned dependency declarations, and the complete benchmark implementation. Per D-03 (revised 2026-09-11): do not manually download JCuda JARs, run Maven, use `install:install-file`, resolve Maven dependencies, or execute Colab in this code-only session. Future build/Colab execution may resolve the declared coordinates.

Purpose: prepare the conditional GPU-04 fallback without changing production modules.
Output: `bench/jcuda-poc` source and the one-line root reactor entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md
@.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-PATTERNS.md
@bench/tornado-poc/pom.xml
@bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java

<interfaces>
- `new NDArray(double[][] rows)` creates row-major inputs; `ArrayOps.dot(left, right)` is the production-HAL CPU reference; `result.getData().data` is the flat result buffer.
- Required JCuda call: `JCublas2.cublasDgemm(handle, CUBLAS_OP_N, CUBLAS_OP_N, n, n, n, alpha, dB, n, dA, n, beta, dC, n)`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Author the isolated POC contract and truthful local failure path</name>
  <files>pom.xml, bench/jcuda-poc/pom.xml, bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java</files>
  <read_first>@pom.xml; @bench/tornado-poc/pom.xml; @bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-CONTEXT.md; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md</read_first>
  <action>Add exactly one root module entry after `bench/tornado-poc`. Clone the Tornado POM structure, retaining parent, `numja-core`, shade configuration, signed-resource excludes, ServicesResourceTransformer, Java 21 release, and runnable manifest; rename artifact/finalName/mainClass to `jcuda-poc`/`jcuda-poc-jar`/`bench.jcudapoc.GemmBench`; remove Tornado dependencies and preview flags. Declare only `org.jcuda:jcuda:12.6.0` and `org.jcuda:jcublas:12.6.0`, per D-03 (revised 2026-09-11); do not declare native classifiers, vendor binaries, download JARs, invoke Maven, or use `install:install-file`. Author `GemmBench`: parse `bench.size` before allocation, default to 4096, reject non-numeric/non-positive/greater-than-4096 values as `CONFIG_ERROR` plus `verdict=NO-GO`, generate the D-04 fixed seeds, and author the one-warmup median-of-three `ArrayOps.dot` CPU baseline. Probe CUDA with exceptions enabled; zero devices must yield `result=GPU_ABSENT`, JNI/native initialization failures must print their class and message to stderr then yield `result=GPU_INIT_FAILED`, and both must yield `verdict=NO-GO`. Every return must emit all D-04 fields: `cpu_baseline_ms`, `gpu_ms`, `transfer_ms`, `speedup_ratio`, `transfer_pct`, `device`, `result`, `verdict`, `env`, `jdk`, `hardware`, `size`, and `cpu_vs_gpu_frob_rel_err` (`NA` when no GPU computation occurred). Keep all JCuda code confined to the bench module per D-01/D-02.</action>
  <verify><automated>python -c "from pathlib import Path; root=Path('pom.xml').read_text(); pom=Path('bench/jcuda-poc/pom.xml').read_text(); src=Path('bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java').read_text(); assert root.count('&lt;module&gt;bench/jcuda-poc&lt;/module&gt;') == 1; assert all(x in pom for x in ('org.jcuda','&lt;artifactId&gt;jcuda&lt;/artifactId&gt;','&lt;artifactId&gt;jcublas&lt;/artifactId&gt;','12.6.0')); assert 'install:install-file' not in pom and '&lt;classifier&gt;' not in pom; assert all(x in src for x in ('GPU_ABSENT','GPU_INIT_FAILED','CONFIG_ERROR','verdict','ArrayOps.dot','cpu_vs_gpu_frob_rel_err'))"</automated></verify>
  <done>Source declares the two pinned coordinates without resolving them, and every authored local failure return has the complete, truthful D-04 NO-GO schema.</done>
</task>

<task type="auto">
  <name>Task 2: Author the cuBLAS Dgemm path and gate calculations</name>
  <files>bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java</files>
  <read_first>@bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java; @bench/tornado-poc/src/main/java/bench/tornadopoc/GemmBench.java; @.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-RESEARCH.md</read_first>
  <action>Author the discovered-device branch with `JCublas2` only, never legacy `JCublas`: allocate dA/dB/dC with `(long) n * n * Sizeof.DOUBLE`, create/destroy a `cublasHandle`, and use host `Pointer.to` alpha/beta. Call B-first `cublasDgemm` with two `CUBLAS_OP_N` values and a source comment explaining `(A×B)^T=B^T×A^T`, per D-08. Author separately timed H2D/D2H copies as `transfer_ms`; author one untimed warmup plus median-of-three timing of Dgemm and `cudaDeviceSynchronize` as kernel-only `gpu_ms`. Calculate max errors, MAE, and full-matrix Frobenius relative error; emit `NUMERIC_MISMATCH`/NO-GO when it exceeds 1e-9 per D-06. Apply D-07 exactly: GO only when speedup is at least 2.0 and transfer percentage is below 50.0. Use `finally` to destroy an initialized handle and free all allocated pointers. Log CUDA/native exception class and message before the top-level `GPU_INIT_FAILED` NO-GO output; do not broadly swallow the cause. Do not build or run this source in the code-only session, per D-03 (revised 2026-09-11).</action>
  <verify><automated>python -c "from pathlib import Path; s=Path('bench/jcuda-poc/src/main/java/bench/jcudapoc/GemmBench.java').read_text(); assert 'JCublas2.cublasDgemm' in s and s.count('CUBLAS_OP_N') &gt;= 2 and 'alpha, dB' in s and 'dA, n' in s; assert 'cudaDeviceSynchronize' in s and 'cpu_vs_gpu_frob_rel_err' in s and '1e-9' in s; assert 'speedupRatio &gt;= 2.0' in s and 'transferPct &lt; 50.0' in s; assert 'finally' in s; assert 'import jcuda.jcublas.JCublas;' not in s"</automated></verify>
  <done>Source contains a resource-safe, B-first cuBLAS path with explicit numerical/performance gates and a non-masking GPU initialization-failure result.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future Maven Central resolution → POC build | Pinned third-party JNI artifacts enter only the isolated benchmark. |
| `-Dbench.size` → allocation | Caller-controlled size can otherwise request excessive host/device memory. |
| Java POC → JCuda JNI/cuBLAS | Native code runs outside JVM memory safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering/Elevation | `bench/jcuda-poc/pom.xml` | mitigate | Pin only legitimacy-audited 12.6.0 Maven Central coordinates; revised D-03 forbids manual JAR acquisition and local installs. |
| T-07-02 | Denial of Service | `GemmBench` `bench.size` | mitigate | Validate before allocation and return CONFIG_ERROR/NO-GO above the 4096 ceiling. |
| T-07-03 | Elevation | JCuda JNI/cuBLAS | mitigate | Isolate JNI code in the bench module, retain exception evidence, and never wire production HAL paths (D-01). |
| T-07-SC | Tampering | Future Maven package resolution | mitigate | Use only packages audited in `07-RESEARCH.md`; no additional artifacts or manual binary path. |
</threat_model>

<verification>
This execution is source-only. Use only static task checks; do not run Maven, resolve dependencies, download JARs, create a local JAR, or execute Colab. In a future authorized run, Maven packaging must exit successfully before any JAR-output assertion. Then capture the local run with stderr in a log, require every D-04 key, require exactly one `result=GPU_ABSENT` or `result=GPU_INIT_FAILED`, require exactly one `verdict=NO-GO`, and reject `result=OK`. A legacy-class check must be a standalone failure-on-match conditional, with no exit-status fallback that can hide a failed preceding command.
</verification>

<success_criteria>
- Only source and pinned Maven Central declarations are delivered in this session, exactly as revised D-03 requires.
- The isolated POC authors complete local NO-GO output and the CUDA correctness/performance path without production API or module changes.
- Future execution instructions require successful packaging before truthful, complete local-output assertions.
</success_criteria>

<output>
Create `.planning/phases/07-gpu-fallback-poc-via-jcuda-cublas/07-01-SUMMARY.md` when done.
</output>
